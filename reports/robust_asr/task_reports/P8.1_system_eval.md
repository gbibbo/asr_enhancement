# P8.1 — System evaluation and Decision D (Outcome E)

Routing branch: B (deterministic selector — agent plan §4053-§4077,
§4.7, §5.6). Reason: `OUTCOME_E_DETERMINISTIC_SELECTOR` active;
deployable transcript-producing backend count = 1
(`whisper_base_ct2_int8` only); `claims_enabled.cloud_tradeoff=false`,
`claims_enabled.ood_real=false`, `claims_enabled.positive_lora=false`.

## Outcome

`PASS` with sentinel `OK_SYSTEM_EVAL:false` on stdout (and as the last
non-blank line of `reports/robust_asr/system/system_eval.md`),
sentinel `OK_REPORT_SHAPE` from `validate_report_shape.py`, and
`pytest tests/robust_asr` 137 passed.

Decision D: `Decision_D_positive_system.outcome = false`.
`tracker.claims_enabled.positive_system` transitions `pending -> false`
per agent plan §4061 / §5.6.

## Inputs

- Selected router:
  `artifacts/robust_asr/router/selected_router`
  (kind=`deterministic_selector`,
  version=`deterministic_selector_v1`).
- Selector evidence parquet:
  `artifacts/robust_asr/router/selector_evidence.parquet`
  (53 230 rows; produced by P6.1; sha256 matches recorded
  `artifacts.selector_evidence_parquet.sha256`).
- Declared baselines: `[whisper_base_ct2_int8]` (the only deployable
  transcript-producing backend under OUTCOME_E; `assemblyai` and
  `whisper_lora_ct2_int8` are non-deployable).
- Backend table:
  `artifacts/robust_asr/eval_tables/whisper_base_ct2_int8.parquet`
  (per-`audio_id` WER joined to the evidence table; parity drift
  vs. `whisper_base_ct2_int8_wer` column max abs diff = 0.0).
- Profile: `battery_aware` (config default; the only profile whose
  eligible action set
  `[whisper_base_ct2_int8, ask_repeat]` is reachable when both
  `assemblyai_available=false` and `lora_available=false`).
- Bootstrap policy (§5.7): method=`BCa` with `percentile_fallback` when
  the bootstrap distribution is degenerate, 10 000 iterations,
  confidence=0.95, seed=20250514.
- `ask_repeat` WER policy (primary): 1.0 (full-miss convention; §5.4
  cost coefficients `alpha=1.0`, `beta=gamma=delta=0.0`).
- Sensitivity policy: 0.5.

## Implementation

- `scripts/robust_asr/evaluate_system.py` (sha256
  `8735d888d45c6c92eab187fbb207b3da334b24d3115e88f7d9e2a3cd89a0802b`):
  loads the selected router metadata, joins
  `selector_evidence.parquet` to
  `eval_tables/whisper_base_ct2_int8.parquet` on `audio_id`, asserts
  WER parity, computes per-row `regret_selector_i` and
  `regret_baseline_i`, runs paired BCa bootstrap (with percentile
  fallback when degenerate) and Wilcoxon signed-rank, evaluates the
  §5.6 Decision D predicate, writes
  `reports/robust_asr/system/system_eval.md` with
  `positive_system: <true|false>` on its first line, and emits
  `OK_SYSTEM_EVAL:<true|false>` on stdout.
- `slurm/jobs/p8_1_system_eval.sh` (sha256
  `85c3052f58fd78b9b63eabfedc8976e836e1b2c03161f57ff80f13a72868e8e5`):
  CPU-only Apptainer wrapper (`robust_asr_py311_cuda12.sif`; no
  `--nv`); 30-minute wall-clock budget, 4 CPUs, 8 GB RAM; logs under
  `$ASR_TRAINING_ROOT/logs/`.

## Commands

```
$ ./slurm/tools/on_submit.sh sbatch \
    slurm/jobs/p8_1_system_eval.sh
Submitted batch job 2132279

$ ./slurm/tools/on_submit.sh sacct -j 2132279 \
    --format=JobID,JobName,State,Elapsed,MaxRSS,ExitCode
JobID        JobName    State      Elapsed    MaxRSS   ExitCode
2132279      asr_p8_1_+ COMPLETED  00:00:09             0:0
2132279.bat+ batch      COMPLETED  00:00:09   7012K     0:0

$ head -n 1 reports/robust_asr/system/system_eval.md
positive_system: false

$ grep -E "^OK_SYSTEM_EVAL:(true|false)$" \
    /mnt/fast/nobackup/scratch4weeks/gb0048/asr_enhancement_training/logs/asr_p8_1_system_eval_2132279.out
OK_SYSTEM_EVAL:false

$ python3 scripts/robust_asr/validate_report_shape.py \
    --schemas docs/plans/state_packet_schemas_v1.yaml \
    --fixtures artifacts/robust_asr/state_packets/report_shape_fixtures
OK_REPORT_SHAPE

$ ./slurm/tools/on_submit.sh sbatch <pytest helper>
Submitted batch job 2132280

$ ./slurm/tools/on_submit.sh sacct -j 2132280 \
    --format=JobID,JobName,State,Elapsed,ExitCode
2132280      asr_p8_1_+ COMPLETED  00:00:09             0:0
# tests/robust_asr — 137 passed, 1 warning in 6.21s.
```

## Headline numbers (Decision D, primary policy ask_repeat_wer=1.0)

| metric | value |
|---|---|
| n (audio_id rows) | 53 230 |
| selector action mix | `whisper_base_ct2_int8` 53 202 (99.9474 %), `ask_repeat` 28 (0.0526 %), `assemblyai` 0, `whisper_lora_ct2_int8` 0 |
| mean WER selector | 0.209797 |
| mean WER baseline (`whisper_base_ct2_int8`) | 0.209797 |
| mean_regret_selector | 0.000000 |
| mean_regret_baseline | 0.000000 |
| paired bootstrap method | `percentile_fallback` (BCa degenerate: all deltas == 0) |
| paired 95% CI on (regret_selector − regret_baseline) | [0.000000, 0.000000] |
| Wilcoxon signed-rank (nonzero deltas) | n = 0; statistic = NaN; p = NaN |
| predicate (i) `mean_regret(chosen) < mean_regret(b)` | False |
| predicate (ii) CI excludes 0 | False |
| predicate (iii) no disabled-flag dependency | True |
| **positive_system** | **false** |

Reason: all 28 `ask_repeat` rows already have
`whisper_base_ct2_int8_wer = 1.0`, so at the primary
`ask_repeat_wer=1.0` policy the per-row delta is identically zero;
§5.6 predicate (i) cannot hold strictly and predicate (ii) is not
evaluable as a positive claim. This is the documented
`OUTCOME_E_NARROWED_SCOPE` consequence and is consistent with the
P7.3 `selector_final_eval.md` NEUTRAL_EVIDENCE framing.

## Sensitivity check (ask_repeat_wer = 0.5)

| metric | value |
|---|---|
| mean WER selector | 0.209534 |
| mean WER baseline | 0.209797 |
| mean_regret_selector | -0.000263 |
| paired bootstrap method | BCa |
| paired 95% CI | [-0.000385, -0.000188] |
| Wilcoxon n / stat / p | 28 / 0.0 / 1.213e-07 |
| strict_lt / CI_excludes_0 / hypothetical_positive_system | True / True / True |

Reported as a sensitivity probe only; it does not affect Decision D.
The primary policy under agent plan §5.4 (α=1.0) keeps
`positive_system=false`.

## Validation

- `OK_SYSTEM_EVAL:false` (stdout and report final line).
- `OK_REPORT_SHAPE` from `validate_report_shape.py --schemas
  docs/plans/state_packet_schemas_v1.yaml --fixtures
  artifacts/robust_asr/state_packets/report_shape_fixtures`.
- `pytest tests/robust_asr -q` → 137 passed, 1 warning in 6.21 s
  (Slurm job 2132280).
- `python -c "import yaml; yaml.safe_load(open('docs/progress/
  robust_asr_progress.yaml'))"` → parses; sentinel
  `OK_PROGRESS_YAML_PARSE` confirmed earlier this session.

## Tracker mutations recorded by this task

- `tasks.P8.1.status = PASS`; `tasks.P8.1.next_task = P8_GATE`;
  `tasks.P8.1.marker = OUTCOME_E_NARROWED_SCOPE`;
  `tasks.P8.1.sentinels = [OK_SYSTEM_EVAL_false, OK_REPORT_SHAPE,
  pytest_tests/robust_asr_137_of_137]`.
- `last_completed_task` advanced `P7.3 -> P8.1`.
- `current_task` advanced `P8.1 -> P8_GATE`.
- `claims_enabled.positive_system` transitioned `pending -> false`.
- `decisions.Decision_D_positive_system.outcome = false`;
  `decisions.Decision_D_positive_system.decided_at_task = P8.1`.
- New artifact entries: `system_eval_report`,
  `evaluate_system_script`, `p8_1_system_eval_job`.
- `latest_approval_packet`=APPROVE_PLAN(P8.1) on
  `f23a27024ccb284175ed120030920220abc3e51c` (next P8_GATE);
  APPROVE_EXECUTION(P8.1-scope-change) demoted to
  `prior_approval_packet_p8_1_scope_exec` on the same commit (next
  P8.1); CHANGE_SCOPE(P8.1) demoted to
  `prior_approval_packet_p8_1_change_scope` on `440f2fa…` (next
  P8.1); PHASE_APPROVE(P7) demoted to
  `prior_approval_packet_p7_phase`.
- `state_transport.last_accepted_report_commit` HELD at
  `d009c318acd99041de3175af26b91df2adddffa6` (NOT advanced to this
  P8.1 implementation commit, matching the P5.1 / P6.1 / P7.3
  pattern).
- `state_transport.expected_next_task = P8_GATE`.

Held: `current_phase=P8`,
`markers=[BLOCKED_OOD_PUBLIC, BLOCKED_API,
OUTCOME_E_DETERMINISTIC_SELECTOR]` (none cleared),
`blocked=false`, `blocker=null`,
`claims_enabled.{ood_real:false, cloud_tradeoff:false,
positive_lora:false}`, `router_status=SELECTOR_PACKAGED`,
`deterministic_selector_version=deterministic_selector_v1`,
`lora_status=SKIPPED_BY_DECISION_A`,
`tasks.P4.1=P4.2=P4.3=SKIPPED_BY_DECISION_A`,
`tasks.P6.1=PASS`, `tasks.P6.2=SKIPPED_BY_OUTCOME_E`,
`tasks.P7.1=SKIPPED_BY_OUTCOME_E`,
`tasks.P7.2=SKIPPED_BY_OUTCOME_E`, `tasks.P7.3=PASS`,
`normalization_version=normalization_v1`,
`metrics_version=metrics_v1`,
`degradation_version=degradation_v1`,
`phase_summary={P0:PASS, P1:PASS, P2:PASS, P3:PASS, P5:PASS,
P6:PASS, P7:PASS}`.

P8.2 not started. P8_GATE not started. No real-provider call. No
audio/model/secret writes.
