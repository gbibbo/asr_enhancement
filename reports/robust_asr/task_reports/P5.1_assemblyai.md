# P5.1 — AssemblyAI cache or API block

## Outcome

`HALTED` with marker `BLOCKED_API`, reason `key_unset`.

`claims_enabled.cloud_tradeoff` → `false`. Routing implication (owned by
P5 gate, not P5.1): deployable transcript-producing backend count = 1
(`whisper_base_ct2_int8` only); P3 already set `include_lora_in_router=false`.
The P5 gate will therefore activate `OUTCOME_E_DETERMINISTIC_SELECTOR` per
Section 8.

## Inputs

- Pricing config: `configs/robust_asr/pricing_v1.yaml`
  (`assemblyai_pricing_checked_date=2026-05-13`,
  `unit_price_usd_per_audio_second=0.0001028`,
  `max_total_cost_usd=50.0`,
  source URL `https://www.assemblyai.com/pricing`).
- Eval manifests config: `configs/robust_asr/eval_manifests_v1.yaml`
  (assemblyai `backend_endpoints` entry appended; existing
  `whisper_base_ct2_int8` entry preserved).
- Manifests considered for budget estimation:
  `degradation_v1_id_eval.parquet` (enabled), `degradation_v1_ood_param_eval.parquet`
  (enabled). `ood_real_locked.parquet` skipped (`enabled=false`,
  `claims_enabled.ood_real=false`).

## Probe

```
$ python3 scripts/robust_asr/probe_assemblyai_runtime.py
ASSEMBLYAI_RUNTIME=false reason=key_unset
```

`ASSEMBLYAI_API_KEY` is unset in the datamove1 environment. Per
agent plan §3786–3805, the cache populate / evaluate steps are skipped.
The probe never logs or persists the key.

## Cost / pricing status

- Pricing config validates: `validate_pricing()` reports 0 missing fields.
- Freshness: `assemblyai_pricing_checked_date=2026-05-13` ≤ 90 days from
  today (2026-05-13). `pricing_is_fresh()` → True.
- `populate_assemblyai_cache.py` was NOT invoked beyond the key-unset
  guard. No pre-spend summary computed against live data; no AssemblyAI
  requests issued; no upload performed; no `cache_summary.json` written.

## Cache / eval status

- `BLOCKED_API` (key_unset). No `artifacts/robust_asr/eval_tables/assemblyai.parquet`
  produced.
- `OK_ASSEMBLYAI_CACHE`, `OK_ASSEMBLYAI_EVAL`, `OK_EVAL_TABLE` are
  intentionally NOT emitted on this run; agent plan §3800–3805 accepts
  `BLOCKED_API` recorded with reason in `{key_unset, auth, quota,
  health_check_failed}` and `claims_enabled.cloud_tradeoff=false` as a
  valid completion path.

## Decision rules applied (agent plan §3814–3820)

Rule 1 fires: `populate_assemblyai_cache.py` was invoked only via the
mode-2 fast-fail key-unset guard (exit 8 path is the same as if it had
run); `status=HALTED`, marker=`BLOCKED_API` (`reason=key_unset`),
`claims_enabled.cloud_tradeoff=false`. Rules 2, 3 do not fire.

## Deliverables (this commit)

| Path | sha256 | Role |
|------|--------|------|
| `configs/robust_asr/pricing_v1.yaml` | `c1e61077ea4c025b147d0bbc9ce42e5b8cbca83a3cb360bc5724bdd99a61a815` | new |
| `configs/robust_asr/eval_manifests_v1.yaml` | `ab7d16ec3998dd94a20eccf74d3cf2ba39800baef2851eb01d23ce5d1423cfc4` | amended (append-only) |
| `scripts/robust_asr/probe_assemblyai_runtime.py` | `6f1530db9769eae79cd5a013b9d4e7535667bd6e2d86e21201cc6b559d98657c` | new |
| `scripts/robust_asr/populate_assemblyai_cache.py` | `af21fb41a94c77431f7d9c948383c6a4c090dbb9329589291532372a9db89217` | new |
| `scripts/robust_asr/evaluate_assemblyai_from_cache.py` | `20c2e0b3a0df9b57cbc52951450d8cb38cef46ecf631dc16c58ea4756a78e20e` | new |
| `tests/robust_asr/test_assemblyai.py` | `18a35a280baba0f3dab463c901b7132c6ade44e5ee51748789eafed02aba8348` | new |

## Verification

- `python3 scripts/robust_asr/probe_assemblyai_runtime.py`
  → `ASSEMBLYAI_RUNTIME=false reason=key_unset` (exit 0).
- `python3 -m pytest tests/robust_asr/test_assemblyai.py -q`
  → `15 passed in 1.97s`.
- `python3 -m pytest tests/robust_asr/ -q`
  → `124 passed in 3.79s` (was 109 pre-P5.1; +15 new in
  `test_assemblyai.py`).
- `python3 scripts/robust_asr/validate_report_shape.py --schemas
  docs/plans/state_packet_schemas_v1.yaml --fixtures
  artifacts/robust_asr/state_packets/report_shape_fixtures`
  → `OK_REPORT_SHAPE`.
- `BLOCKED_API` evidence is recorded in the live tracker on the P5.1
  acceptance commit.

## Paid-API guard evidence (no spend on this run)

- Probe returned `key_unset`; no request to AssemblyAI was made.
- `populate_assemblyai_cache.py` enforces, in order:
  1. **KEY_UNSET** (exit 8) when `ASSEMBLYAI_API_KEY` is missing.
  2. **PENDING_PRICING_VERIFICATION** (exit 12) when
     `assemblyai_pricing_checked_date` is older than 90 days.
  3. **Pre-spend summary** with estimated total cost printed BEFORE any
     upload.
  4. **BUDGET_EXCEEDED** (exit 11) when the estimated cost exceeds
     `max_total_cost_usd`, or when a per-row running-cost check would
     exceed the cap mid-run.
  5. The API key is never logged or written to disk; it is read only as
     a request header.
- `tests/robust_asr/test_assemblyai.py` exercises all five guards
  without network calls.

## Tracker mutations (P5.1 commit)

- `tasks.P5.1.status = HALTED`
- `tasks.P5.1.markers += BLOCKED_API`
- `tasks.P5.1.reason = key_unset`
- `claims_enabled.cloud_tradeoff = false`
- `current_task = P5.1` (held until APPROVE_EXECUTION(P5.1))
- `last_completed_task = P3.2` (held)
- `state_transport.last_accepted_report_commit` STAYS at
  `26db72df3215355a68029927980389216353526e` per orchestrator instruction
  (NOT advanced to the P5.1 implementation commit).
- `state_transport.expected_next_task = P5_GATE` (set by APPROVE_PLAN(P5.1)).
- `BLOCKED_OOD_PUBLIC` held non-blocking; not cleared.

## Stop condition

`BLOCKED_API` evidence recorded. Awaits orchestrator
`APPROVE_EXECUTION(P5.1)` before any P5_GATE work.
