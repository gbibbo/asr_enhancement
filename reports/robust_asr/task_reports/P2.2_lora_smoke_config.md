# P2.2 — LoRA smoke split config (PASS)

- Task: P2.2
- Phase: P2
- Branch: feature/robust-asr-lora-router-datamove1-v1
- Status: PASS
- Marker: none active for P2.2; BLOCKED_OOD_PUBLIC held at the project level (non-blocking; OOD-real splits remain `enabled=false`).

## Approval packets accepted

- `APPROVE_EXECUTION(P2.2-scope-change)` on
  `accepted_report_commit=d78678aa9f7a18ddfd00743789bcf797fb191a98`,
  `next_expected_task=P2.2`. Rewritten touch_policy P2.2 row is now
  binding (allows `configs/robust_asr/lora_smoke.yaml`, the new
  `P2.2_lora_smoke_config.md` task report, and the read paths needed
  for manifest-membership validation).
- `APPROVE_PLAN(P2.2)` on
  `accepted_report_commit=d78678aa9f7a18ddfd00743789bcf797fb191a98`,
  `next_expected_task=P2_GATE`. Plan accepted on the scope-change
  commit; `state_transport.last_accepted_report_commit` is NOT
  advanced to the P2.2 implementation commit per orchestrator
  instruction.

## Deliverables produced (PASS)

- `configs/robust_asr/lora_smoke.yaml`
  - bytes: 43,252
  - sha256: `4d7ae4489587937e841df9ca172e9b9933e4647ddbe06edf3b00adc713e00cff`
  - top-level keys: `version`, `seed`, `manifests`, `smoke_split`,
    `smoke_eval_split`, `hyperparameters`, `eval_decode_defaults`,
    `timeouts`.

## Smoke splits

Deterministic, RNG-free, stratified-by-speaker sampling. For each source
manifest the rule is: group by `speaker_id`, sort speakers by integer
ID, sort each speaker's utterances by `audio_id` lexicographically,
take the first `per_speaker` utterances per speaker.

| field | smoke_split | smoke_eval_split |
|---|---|---|
| source manifest | `librispeech_lora_train.parquet` | `librispeech_validation.parquet` |
| source rows total | 22,507 | 2,703 |
| source speakers total | 200 | 40 |
| per_speaker | 3 | 5 |
| n_rows | **600** | **200** |
| n_speakers | **200** | **40** |
| total duration (s) | 7,647.93 | 1,730.07 |
| total duration (h) | 2.1244 | 0.4806 |
| sha256 (source manifest) | `7896175ecf9631ef949e504ecc3f442d342a44ae53f8f28ef5a34949f3484d4a` | `977a6f01d72171e9cfb9ce8aee71d4961a99d66397784225c71efbcf1d53733f` |

Both splits are speaker-aligned with the canonical `data_v1.yaml`
partitions (lora_train = 200 train-clean-100 speakers; validation = 40
dev-clean speakers) and speaker-disjoint by construction with
`router_train` and `locked_test`. No demo / OOD-real audio is
referenced.

## Hyperparameters and decode defaults

Hyperparameters (P3.1-binding):

```text
steps_max: 200
learning_rate: 1.0e-4
batch_size: 8
lora_rank: 8
lora_alpha: 16
lora_dropout: 0.05
target_modules: [q_proj, k_proj, v_proj, out_proj]
seed: 42
warmup_steps: 20
optimizer: adamw
weight_decay: 0.0
gradient_accumulation_steps: 1
fp16: true
```

Fixed eval decode defaults (Section 3):

```text
task: transcribe
language: en
condition_on_previous_text: false
without_timestamps: true
beam_size: 1
temperature: 0.0
```

Timeouts:

```text
training_timeout_seconds: 14400   # 4 h, plan §3510
eval_timeout_seconds:     1800    # 30 min, plan §3510
```

## Verifications (login node; no Slurm, no Apptainer)

| # | sentinel | command | result |
|---|---|---|---|
| 1 | `OK_LORA_SMOKE_CONFIG_PARSE` | `python3 -c "import yaml; c=yaml.safe_load(open('configs/robust_asr/lora_smoke.yaml')); ..."` | exit 0; required top-level keys all present |
| 2 | `OK_LORA_SMOKE_MANIFEST_REFS` | `python3 -c "...pyarrow.parquet membership check..."` | smoke_split: 600 unique audio_ids OK; smoke_eval_split: 200 unique audio_ids OK |
| 3 | `OK_LORA_SMOKE_HPARAMS` | `python3 -c "...hparams keys + steps_max..."` | exit 0; `steps_max=200`; all required keys present |
| 4 | `OK_LORA_SMOKE_DECODE_DEFAULTS` | `python3 -c "...Section 3 decode defaults..."` | exit 0; `task=transcribe`, `language=en`, `condition_on_previous_text=false`, `without_timestamps=true` |
| 5 | `OK_LORA_SMOKE_TIMEOUTS` | `python3 -c "...timeouts..."` | exit 0; `training_timeout_seconds=14400`, `eval_timeout_seconds=1800` |
| 6 | `OK_REPORT_SHAPE` | `python scripts/robust_asr/validate_report_shape.py --schemas docs/plans/state_packet_schemas_v1.yaml --fixtures artifacts/robust_asr/state_packets/report_shape_fixtures` | exit 0 |
| 7 | pytest non-regression | `pytest -q tests/robust_asr/` | 85/85 PASS |

## Tracker state after P2.2 PASS

- `current_phase = P2` (held)
- `current_task = P2.2` (held until orchestrator `APPROVE_EXECUTION(P2.2)` advances to `P2_GATE`)
- `last_completed_task = P2.1` (held)
- `tasks.P2.2.status = PASS`
- `tasks.P2.2.next_task = P2_GATE`
- `tasks.P2.2.marker = null`
- `markers = [BLOCKED_OOD_PUBLIC]` (held, non-blocking)
- `blocked = false`
- `blocker = null`
- `claims_enabled.ood_real = false` (held)
- `claims_enabled.cloud_tradeoff = true` (held)
- `degradation_version = degradation_v1`
- `normalization_version = normalization_v1`
- `metrics_version = metrics_v1`
- `phase_summary = {P0: PASS, P1: PASS, P2: null}` (held)
- `orchestrator_approvals = {P0: PHASE_APPROVE, P1: PHASE_APPROVE, P2: null}` (held)
- `state_transport.last_accepted_report_commit` stays
  `83dd911bc124a9f1dd53bab17cc4812dbf0cf932` (the P2.2 commit is NOT
  accepted per orchestrator instruction).
- `state_transport.expected_next_task = P2.2` held.
- `latest_approval_packet = APPROVE_PLAN(P2.2)` on
  `d78678aa9f7a18ddfd00743789bcf797fb191a98` (next P2_GATE).
- `prior_approval_packet = APPROVE_EXECUTION(P2.2-scope-change)` on
  `d78678aa9f7a18ddfd00743789bcf797fb191a98` (next P2.2).
- `prior_approval_packet_p2_2_change_scope = CHANGE_SCOPE(P2.2)` on
  `821893c69eb3e651a94e6bddde49ca5a22be7354` (next P2.2).
- `prior_approval_packet_p2_1_exec = APPROVE_EXECUTION(P2.1)` on
  `83dd911bc124a9f1dd53bab17cc4812dbf0cf932` (next P2.2).

## P2 gate predicate (Section 8 reference)

All P2-gate prerequisites are now satisfiable on the artifact side:

- `tasks.P2.1.status == PASS` ✓ (held from P2.1)
- `tasks.P2.2.status == PASS` ✓ (this task)
- `artifacts/robust_asr/eval_tables/whisper_base_ct2_int8.parquet`
  exists with checksum AND `validate_eval_table.py` → `OK_EVAL_TABLE` ✓
  (held from P2.1)
- `reports/robust_asr/baseline_whisper_base.md` exists ✓ (held from P2.1)
- `configs/robust_asr/lora_smoke.yaml` exists and references valid
  manifest rows ✓ (this task)
- No active marker in `{MISSING_EVIDENCE, PLAN_CONFLICT}` ✓

P2_GATE not started in this task. Awaiting orchestrator
`APPROVE_EXECUTION(P2.2)` and then `PHASE_APPROVE(P2)` before P3.1 may
begin.

## What this task did NOT do

- No Slurm submission; no Apptainer exec; no GPU; no external API.
- No edits to `libs/**`, `scripts/robust_asr/**`, `slurm/**`,
  `services/**`, or `infra/**`.
- No model weights touched, no audio I/O, no `pip install`.
- No `tasks.P3.1` or `tasks.P_GATE` records created.
- `BLOCKED_OOD_PUBLIC` NOT cleared.
- `state_transport.last_accepted_report_commit` NOT advanced.

## Stop condition

After commit and push of the LoRA smoke split config and this task
report. Awaiting orchestrator `APPROVE_EXECUTION(P2.2)`.
