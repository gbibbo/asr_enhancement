# P3.2 Execution Report — Decision A

- Task: P3.2 (Decision A from P3.1 smoke metrics)
- Branch: feature/robust-asr-lora-router-datamove1-v1
- Phase: P3
- Status: PASS
- Sentinel: `OK_LORA_SMOKE_DECISION:FAIL`
- Decision_A_smoke.outcome: FAIL

## Summary

P3.2 runs the mechanical Section 5.1 decision over the P3.1 smoke metrics
(`reports/robust_asr/lora/lora_smoke_result.json`) and the export-smoke
outcome (`artifacts/robust_asr/lora_smoke/export_smoke_result.json`).

Decision A = **FAIL**:

- `macro_wa_gain = -0.12843 < 0.005`
- `clean_wa_regression = 0.11345 > 0.010` (and `> 0.020`)
- `max_family_wa_gain = -0.11345 < 0.010` → cannot rescue to PARTIAL
- `per_family_wa_gain_variance = 1.79e-4 ≠ 0` → degenerate guard not active;
  FAIL is on the booleans themselves
- `export_smoke_result.outcome = PASS` → not HALTED, no EXPORT_BLOCKED

## Files changed

- `scripts/robust_asr/decide_lora_smoke.py` — new (mechanical §5.1 decision; CLI
  contract per agent plan §1263–§1285).
- `tests/robust_asr/test_decide_lora_smoke.py` — new (12 unit tests covering all
  four outcomes, threshold boundaries, degenerate-zero-variance guard, CLI
  exit codes).
- `reports/robust_asr/lora/lora_smoke_report.md` — new (first line: `FAIL`).
- `reports/robust_asr/lora/decision_a_smoke.md` — new (decision narrative +
  expected P3-gate side effects).
- `reports/robust_asr/task_reports/P3.2_decision_a.md` — this file.
- `docs/progress/robust_asr_progress.yaml` — tracker update.
- `docs/progress/robust_asr_progress.md` — narrative update.
- `docs/progress/robust_asr_state_capsule.md` — capsule update.

## Commands run

```
python3 -m pytest -q tests/robust_asr/test_decide_lora_smoke.py
# -> 12 passed in 0.11s

python3 scripts/robust_asr/decide_lora_smoke.py \
  --input reports/robust_asr/lora/lora_smoke_result.json \
  --export-input artifacts/robust_asr/lora_smoke/export_smoke_result.json \
  --out reports/robust_asr/lora/lora_smoke_report.md
# stdout: OK_LORA_SMOKE_DECISION:FAIL
# exit 0
# reports/robust_asr/lora/lora_smoke_report.md first line: FAIL

python3 -m pytest -q tests/robust_asr/
# -> 109 passed in 4.22s   (was 97 pre-P3.2; +12 new tests)

python3 scripts/robust_asr/validate_report_shape.py \
  --schemas docs/plans/state_packet_schemas_v1.yaml \
  --fixtures artifacts/robust_asr/state_packets/report_shape_fixtures
# stdout: OK_REPORT_SHAPE
# exit 0
```

## Verification result

- `OK_LORA_SMOKE_DECISION:FAIL` emitted by `decide_lora_smoke.py`.
- `lora_smoke_report.md` first line equals `FAIL`.
- `decision_a_smoke.md` present.
- 109/109 robust_asr tests pass (97 prior + 12 new).
- `validate_report_shape.py` emits `OK_REPORT_SHAPE`.

## Tracker state after

- `current_phase: P3`
- `current_task: P3.2` (held until `APPROVE_EXECUTION(P3.2)`)
- `last_completed_task: P3.1` (held)
- `tasks.P3.2.status = PASS`
- `tasks.P3.2.sentinels = [OK_LORA_SMOKE_DECISION:FAIL]`
- `decisions.Decision_A_smoke.outcome = FAIL`
- `decisions.Decision_A_smoke.decided_at_task = P3.2`
- `markers: [BLOCKED_OOD_PUBLIC]` (held, non-blocking)
- `blocked: false`
- `claims_enabled.ood_real: false` (held)
- `claims_enabled.positive_lora: pending` (P3.2 does NOT mutate this; P3 gate enacts)
- `lora_status: SMOKE_DONE` (P3.2 does NOT transition; P3 gate enacts)
- `state_transport.last_accepted_report_commit: 096fe43371f7357007ce41317ed758499d1ff131` (STAYS per orchestrator instruction; not advanced to the P3.2 implementation commit)
- `state_transport.expected_next_task: P3_GATE`

## P4 task statuses

P3.2 does NOT touch `tasks.P4.1`, `tasks.P4.2`, `tasks.P4.3`. These transitions
to `SKIPPED_BY_DECISION_A` are enacted at the P3 gate (`PHASE_APPROVE(P3)`)
per Section 0.1 routing and Section 5.10 fallback.

## Blocked status

- Not blocked. `BLOCKED_OOD_PUBLIC` is held as a non-blocking marker (does not
  gate P3.2 or P3_GATE).
- `claims_enabled.ood_real = false`.

## Next pending task

P3_GATE (Section 8 P3 phase-gate evaluation).

## Notes for Gabriel

- Decision A is mechanically FAIL. This is not unexpected: the smoke run only
  trained 200 steps on a small sampled split (P2.2 design); the LoRA adapter is
  underfit and regresses every family WA against the baseline. The agent plan
  treats this as the normal P3.1 → P3.2 → P3_GATE → P5.1 path (Section 0.1).
- LoRA is the smoke artifact only; no claims about LoRA correctness will be
  asserted. The router work (P5–P7) and AssemblyAI baseline (P5.1) proceed
  without LoRA in the candidate set, per Section 5.10.
- All P4 task statuses, the `lora_status` transition, and `claims_enabled.positive_lora`
  remain unchanged until the orchestrator runs the P3 gate.
