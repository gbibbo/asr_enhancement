# P1.1 — Data root inventory and dataset configs (rerun)

Task: P1.1 (rerun after operator restored LibriSpeech audio)
Phase: P1
Date: 2026-05-08
Mode: EXECUTION
Outcome: **PARTIAL** (marker `BLOCKED_OOD_PUBLIC`)

## Inputs

- Pre-state at rerun: `tasks.P1.1.status=HALTED`, `markers=[MISSING_EVIDENCE]`,
  `blocked=true` (from prior P1.1 attempt at commit
  `1470ebc1da1a1cc70fdf9965480a070a0c248e4d`).
- Operator action between attempts: restored LibriSpeech audio via
  `wget` + `tar -xzf` of `train-clean-100.tar.gz` and
  `test-clean.tar.gz` from `https://www.openslr.org/resources/12/` into
  `/mnt/fast/nobackup/scratch4weeks/gb0048/sources/librispeech/LibriSpeech/`.
  Reported integrity: gzip OK, tar exit codes 0.
- Approval-packet chain unchanged from prior P1.1: `latest_approval_packet`
  = APPROVE_PLAN(P1.1) on `e4a567707286ecbb367deb7d6a87eec97cca675d`.

## Actions performed

1. Verified file counts at canonical LibriSpeech root:
   - `train-clean-100`: 251 spk dirs, 28 539 `.flac`, 585 `.trans.txt`, 6.3 GiB.
   - `dev-clean`: 40 spk dirs, 2 703 `.flac`, 97 `.trans.txt`, 349 MiB.
   - `test-clean`: 40 spk dirs, 2 620 `.flac`, 87 `.trans.txt`, 356 MiB.
   - `train-clean-360`: absent (not required at P1.1).
2. Re-checked OOD-real candidates: Common Voice EN `clips/` empty and no
   `.tsv` transcripts; TED-LIUM R3 and CHiME-6 absent. Same as prior.
3. Verified cross-split speaker disjointness within LibriSpeech:
   `train-clean-100 ∩ dev-clean = ∅`, `train-clean-100 ∩ test-clean = ∅`,
   `dev-clean ∩ test-clean = ∅`.
4. Computed deterministic speaker partition for `train-clean-100`:
   sort 251 speaker IDs as integers; assign every 5th (51 speakers) to
   `router_train`; assign the remaining 200 to `lora_train`.
5. Estimated hours via `soundfile.info` stride sample (n=200/split):
   train-clean-100 ≈ 102.30 h; test-clean ≈ 5.47 h; dev-clean = 5.388 h
   (exact, from existing manifest).
6. Rewrote `configs/robust_asr/data_v1.yaml` with populated splits and
   restored subset metadata.
7. Rewrote `reports/robust_asr/data_inventory.md` with the post-restore
   inventory.
8. Updated tracker yaml/md/state_capsule to record PARTIAL outcome,
   marker `BLOCKED_OOD_PUBLIC`, `claims_enabled.ood_real=false`,
   `blocked=false`, `markers=[BLOCKED_OOD_PUBLIC]`. Approval-packet
   chain held.

## Verification

| Command | Sentinel | Result |
|---|---|---|
| data_v1.yaml parse + splits + ood_real check + non-empty splits | `OK_DATA_V1_CONFIG` | OK |
| `check_speaker_disjoint.py --config configs/robust_asr/data_v1.yaml` | `OK_SPEAKER_DISJOINT` | OK (non-trivial — 4 non-empty splits) |
| `check_speaker_disjoint.py … --splits lora_train router_train validation locked_test` | `OK_SPEAKER_DISJOINT` | OK (non-trivial) |
| `validate_report_shape.py …` | `OK_REPORT_SHAPE` | OK (no schema regression) |
| `test -f configs/robust_asr/data_v1.yaml` | — | OK |
| `test -f reports/robust_asr/data_inventory.md` | — | OK |
| `test -f reports/robust_asr/task_reports/P1.1_data_inventory.md` | — | OK |

`OK_SPEAKER_DISJOINT` now passes **non-trivially** (every pair of the
four required splits has a non-empty speaker set; intersections are
`∅`).

## Decision rules applied

- Decision rule 1 of P1.1 (LibriSpeech missing → HALTED + MISSING_EVIDENCE):
  **does not apply**; audio is restored. Prior HALT is cleared.
- Decision rule 2 of P1.1 / Section 1.1 rule 4 (no Section 1.1 OOD-real
  source resolves → PARTIAL, marker `BLOCKED_OOD_PUBLIC`,
  `claims_enabled.ood_real=false`): **applies**; this is the operative
  outcome.

## Files changed

- `configs/robust_asr/data_v1.yaml` (rewritten with populated splits)
- `reports/robust_asr/data_inventory.md` (rewritten)
- `reports/robust_asr/task_reports/P1.1_data_inventory.md` (this file, rewritten)
- `docs/progress/robust_asr_progress.yaml` (tracker mutation)
- `docs/progress/robust_asr_progress.md` (prose update)
- `docs/progress/robust_asr_state_capsule.md` (capsule update)

`scripts/robust_asr/check_speaker_disjoint.py` unchanged from prior P1.1.

## Tracker mutations (recorded in this commit)

- `tasks.P1.1.status: PARTIAL`
- `tasks.P1.1.marker: BLOCKED_OOD_PUBLIC`
- `markers: [BLOCKED_OOD_PUBLIC]`
- `blocked: false`, `blocker: null`
- `claims_enabled.ood_real: false`
- `current_task: P1.1` (held — orchestrator finalizes per protocol)
- `last_completed_task: P0.5` (held until orchestrator advances)
- `state_transport.latest_planning_report:` `reports/robust_asr/task_reports/P1.1_data_inventory.md`
- `state_transport.latest_execution_report:` `reports/robust_asr/task_reports/P1.1_data_inventory.md`
- `state_transport.last_accepted_report_commit:` STAYS
  `3129c11edd5105d7c247b48eb1a170d7c1507cde` (NOT advanced to the
  rerun commit, per orchestrator instruction).
- Approval-packet chain unchanged.

## Compute

None. No Slurm submission. No Apptainer execution. No GPU. No external
API call. All operations were `ls`/`stat`/`find` against authorized
read-only data roots, `soundfile.info` reads on a sampled subset of
LibriSpeech FLAC files, and pure-Python file writes/edits inside the
robust_asr-owned tree.

## Next expected step

Stop. Awaiting orchestrator decision: either APPROVE_EXECUTION(P1.1)
finalizing the PARTIAL outcome and advancing `current_task` to P1.2, or
direct an OOD-real source restoration and a third P1.1 rerun.

P1.2 NOT started.
