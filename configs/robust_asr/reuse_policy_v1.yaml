# Robust ASR — Reuse Policy v1
#
# Produced by: P0.2
# Date: 2026-05-08
# Schema: docs/plans/robust_asr_agent_plan_v3_4_7.md Section 2.2
#
# Required fields per row:
#   path, class, permitted_use, allowed_tasks, validator,
#   checksum_required, large_artifact, commit_allowed, notes
#
# Default for any unclassified path:
#   class: no_touch | permitted_use: forbidden | allowed_tasks: []
#   validator: none | checksum_required: false | large_artifact: unknown
#   commit_allowed: false
#
# Classification rules (binding):
#   - Legacy trackers and plans: legacy_state, permitted_use: read_only,
#     commit_allowed: false. NOT forbidden, because P10.3 must read them.
#   - Mandatory no-touch patterns: no_touch, permitted_use: forbidden.
#   - Existing runtime code: implementation_reuse, permitted_use: read_only
#     (or extend_in_place with CHANGE_SCOPE approval for specific files).
#   - Active robust_asr paths: active_state, permitted_use: read_write.
#   - External host paths (non-git): data_root (datasets) or no_touch (binaries).

# ---------------------------------------------------------------------------
# A. Active robust_asr state paths
# ---------------------------------------------------------------------------

- path: docs/progress/robust_asr_progress.yaml
  class: active_state
  permitted_use: read_write
  allowed_tasks: [P0.0, P0.1, P0.2, P0.3, P0.4, P0.5, P1.1, P1.2, P1.3, P1.4,
                  P2.1, P2.2, P3.1, P3.2, P4.1, P4.2, P4.3, P5.1, P6.1, P6.2,
                  P7.1, P7.2, P7.3, P8.1, P8.2, P9.0, P9.1, P9.2, P10.1, P10.2, P10.3]
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: true
  notes: Live tracker; every task updates this file

- path: docs/progress/robust_asr_progress.md
  class: active_state
  permitted_use: read_write
  allowed_tasks: [P0.1, P0.2, P0.3, P0.4, P0.5, P1.1, P1.2, P1.3, P1.4,
                  P2.1, P2.2, P3.1, P3.2, P4.1, P4.2, P4.3, P5.1, P6.1, P6.2,
                  P7.1, P7.2, P7.3, P8.1, P8.2, P9.0, P9.1, P9.2, P10.1, P10.2, P10.3]
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: true
  notes: Prose tracker; updated each task

- path: docs/progress/robust_asr_state_capsule.md
  class: active_state
  permitted_use: read_write
  allowed_tasks: [P0.1, P0.2, P0.3, P0.4, P0.5, P1.1, P1.2, P1.3, P1.4,
                  P2.1, P2.2, P3.1, P3.2, P4.1, P4.2, P4.3, P5.1, P6.1, P6.2,
                  P7.1, P7.2, P7.3, P8.1, P8.2, P9.0, P9.1, P9.2, P10.1, P10.2, P10.3]
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: true
  notes: State capsule; updated each task

- path: reports/robust_asr/**
  class: active_state
  permitted_use: read_write
  allowed_tasks: [P0.1, P0.2, P0.3, P0.4, P0.5, P1.1, P1.2, P1.3, P1.4,
                  P2.1, P2.2, P3.1, P3.2, P4.1, P4.2, P4.3, P5.1, P6.1, P6.2,
                  P7.1, P7.2, P7.3, P8.1, P8.2, P9.0, P9.1, P9.2, P10.1, P10.2, P10.3]
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: true
  notes: All robust_asr reports; each task writes its designated report

- path: artifacts/robust_asr/**
  class: active_state
  permitted_use: read_write
  allowed_tasks: [P0.2, P0.3, P0.4, P0.5, P1.2, P1.3, P1.4, P2.1, P2.2,
                  P3.1, P3.2, P4.1, P4.2, P4.3, P5.1, P6.1, P6.2, P7.1, P7.2, P7.3,
                  P8.1, P8.2, P9.0, P9.1, P9.2]
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: true
  notes: Eval tables, manifests, router, handoff; large binary artifacts not committed

- path: configs/robust_asr/**
  class: active_state
  permitted_use: read_write
  allowed_tasks: [P0.2, P0.3, P0.4, P1.1, P1.2, P1.4, P2.1, P5.1, P6.1]
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: true
  notes: Robust ASR configs including reuse_policy_v1.yaml. P5.1 scope-change
         (CHANGE_SCOPE 45b6cac) authorizes writing configs/robust_asr/pricing_v1.yaml
         and appending the assemblyai backend_endpoints entry to
         configs/robust_asr/eval_manifests_v1.yaml. P6.1 scope-change
         (CHANGE_SCOPE 9ffc885) authorizes writing
         configs/robust_asr/router_v1.yaml (deterministic-selector config).

# Specific override of configs/robust_asr/** (P5.1 scope-change). New file
# authored by robust_asr at P5.1 and read by P10.1 (final verification).
- path: configs/robust_asr/pricing_v1.yaml
  class: active_state
  permitted_use: read_write
  allowed_tasks: [P5.1, P10.1]
  validator: load_yaml_and_check_required_fields
  checksum_required: true
  large_artifact: false
  commit_allowed: true
  notes: AssemblyAI pricing config (unit price, max_total_cost_usd,
         assemblyai_pricing_checked_date, source URL). Consumed by
         populate_assemblyai_cache.py and evaluate_assemblyai_from_cache.py.

# Specific override of configs/robust_asr/** (P6.1 scope-change). New file
# authored by robust_asr at P6.1 (Outcome E selector-evidence path); read by
# P6.2/P7.1/P7.3 (router/selector packaging) and P10.1 (final verification).
- path: configs/robust_asr/router_v1.yaml
  class: active_state
  permitted_use: read_write
  allowed_tasks: [P6.1, P6.2, P7.1, P7.3, P10.1]
  validator: scripts/robust_asr/validate_selector_evidence.py
  checksum_required: true
  large_artifact: false
  commit_allowed: true
  notes: Router / deterministic-selector config. Holds Section 5.4 cost
         coefficients (alpha, beta, gamma, delta), profile defaults, eligible
         action set under each profile, ask_repeat_wer_threshold,
         router_confidence_min, epsilon_wer, and Section 5.5 deterministic-
         selector constants (ask_repeat_threshold=-1.0, escalate_threshold=-0.5,
         no_speech_threshold=0.6, health_check_ttl_seconds=300). Consumed by
         build_selector_evidence_table.py at P6.1 and by the deterministic
         selector / router package at P7.3.

- path: scripts/robust_asr/**
  class: active_state
  permitted_use: read_write
  allowed_tasks: [P0.2, P0.3, P0.4, P1.1, P1.2, P1.3, P1.4, P2.1, P3.1,
                  P4.1, P4.2, P4.3, P5.1, P6.1, P6.2, P7.2, P7.3, P8.1, P8.2,
                  P10.2, P10.3]
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: true
  notes: Robust ASR scripts. P10.2 added under CHANGE_SCOPE(reuse_policy_p10_2_script_allowance) on accepted_report_commit 9452afcf3c3f6b1874d86650724606d8e902b286 to authorize creation of scripts/robust_asr/final_asset_audit.py per agent plan §4275 (P10.2 Action 1) and §1105–§1119 (final_asset_audit.py contract). P10.3 added under CHANGE_SCOPE(reuse_policy_p10_3_script_allowance) on accepted_report_commit 9f8e4c3687a8f449945e7e1f2d76fd19c082ce29 to authorize creation of scripts/robust_asr/verify_plan_tracker_consistency.py per agent plan §4305 (P10.3 Action 1) and §1057–§1103 (verify_plan_tracker_consistency.py contract). Minimum-necessary amendments; no other tasks added; no class/permitted_use/no-touch change.

- path: tests/robust_asr/**
  class: active_state
  permitted_use: read_write
  allowed_tasks: [P1.1, P1.2, P1.4, P3.1, P4.1, P5.1, P7.3, P8.1, P8.2]
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: true
  notes: Robust ASR tests. P5.1 scope-change (CHANGE_SCOPE 45b6cac) authorizes
         tests/robust_asr/test_assemblyai.py. P7.3 scope-change
         (CHANGE_SCOPE e9ebbfe) authorizes tests/robust_asr/test_router_runtime.py
         for the deterministic-selector runtime regression required by P7 gate
         Branch B. P8.2 scope-change (CHANGE_SCOPE 7f72213) authorizes editing
         tests/robust_asr/test_leakage.py to read the new
         artifacts/robust_asr/demo/demo_examples_manifest.json and assert
         demo audio_id / speaker_id / audio_sha256 disjointness from locked
         eval sets per Section 3 leakage guard.

- path: docs/reports/robust_asr/**
  class: active_state
  permitted_use: read_write
  allowed_tasks: [P0.5, P9.1, P10.1, model_router_card_completion]
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: true
  notes: Model card and router card templates (P0.5 seeded; finalized progressively). model_router_card_completion added under CHANGE_SCOPE on accepted_report_commit d714bac8f4d49e7a5b0ca7616a6ee5758a330d5d to fill or N/A-rewrite every TODO_FILLED_IN_<task_id> placeholder in docs/reports/robust_asr/{model_card_lora.md,router_card.md} per agent plan Section 10 items 11-12, before P10.2 final_asset_audit.py runs. Minimum-necessary amendment; no other tasks added; no class/permitted_use/no-touch change.

- path: docs/profiles/CLAUDE.robust_asr.md
  class: active_state
  permitted_use: read_write
  allowed_tasks: [P0.1]
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: true
  notes: Claude profile for robust_asr; only updated by profile-update tasks

- path: CLAUDE.md
  class: active_state
  permitted_use: read_write
  allowed_tasks: [P0.1]
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: true
  notes: Only the ROBUST_ASR_PROFILE block may be modified; all content outside block is no_touch

# ---------------------------------------------------------------------------
# B. Legacy state paths (read_only; NOT forbidden — P10.3 reads them)
# ---------------------------------------------------------------------------

- path: docs/progress/training_datamove1_progress.md
  class: legacy_state
  permitted_use: read_only
  allowed_tasks: [P10.3]
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: false
  notes: Prior training branch progress tracker; read by P10.3 only

- path: docs/progress/training_datamove1_progress.yaml
  class: legacy_state
  permitted_use: read_only
  allowed_tasks: [P10.3]
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: false
  notes: Prior training branch progress tracker YAML; read by P10.3 only

- path: docs/progress/demo_platform_progress.md
  class: legacy_state
  permitted_use: read_only
  allowed_tasks: [P10.3]
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: false
  notes: Demo platform progress tracker; read by P10.3 only

- path: docs/progress/demo_platform_progress.yaml
  class: legacy_state
  permitted_use: read_only
  allowed_tasks: [P10.3]
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: false
  notes: Demo platform progress tracker YAML; read by P10.3 only

- path: plan.md
  class: legacy_state
  permitted_use: read_only
  allowed_tasks: [P10.3]
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: false
  notes: Legacy root plan placeholder; read by P10.3 only

- path: docs/plans/archive/**
  class: legacy_state
  permitted_use: read_only
  allowed_tasks: [P10.3]
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: false
  notes: Archived demo_platform_plan.md and training_datamove1_plan.md; canonical paths
         were docs/plans/demo_platform_plan.md and docs/plans/training_datamove1_plan.md
         (now absent); read by P10.3 only

# ---------------------------------------------------------------------------
# C. Reusable implementation paths (read_only by default)
# ---------------------------------------------------------------------------

- path: services/api/**
  class: implementation_reuse
  permitted_use: read_only
  allowed_tasks: [P0.4, P5.1]
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: false
  notes: FastAPI service; extend_in_place requires CHANGE_SCOPE approval

- path: services/worker/**
  class: implementation_reuse
  permitted_use: read_only
  allowed_tasks: [P5.1]
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: false
  notes: Celery worker; extend_in_place requires CHANGE_SCOPE approval

- path: services/frontend/**
  class: implementation_reuse
  permitted_use: read_only
  allowed_tasks: []
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: false
  notes: Frontend; not used by training tasks; extend_in_place requires CHANGE_SCOPE

- path: libs/asr_adapter/**
  class: implementation_reuse
  permitted_use: read_only
  allowed_tasks: [P0.4, P2.1, P5.1, P9.0]
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: false
  notes: ASR adapter; read for contract validation and baseline evaluation

- path: libs/audio_pipeline/**
  class: implementation_reuse
  permitted_use: read_only
  allowed_tasks: [P1.2, P1.4, P2.1, P3.1, P4.1, P4.2, P4.3, P8.1]
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: false
  notes: Audio pipeline; read for eval scripts

- path: libs/audio/**
  class: implementation_reuse
  permitted_use: read_only
  allowed_tasks: [P1.1, P1.2, P1.4, P2.1, P3.1, P4.1, P4.2, P4.3, P5.1, P6.1, P8.1]
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: false
  notes: Shared metrics.py, degradations.py, enhancement.py; read for eval and training

# Specific override of libs/audio/** (P1.4 scope-change). P1.4 may append five
# sample_<family> functions to libs/audio/degradations.py per Section 3 of the
# v3.4.7 plan. Existing apply_degradation, DEGRADATION_FAMILIES, and the
# DEGRADATION_VERSION value are preserved (additive change).
- path: libs/audio/degradations.py
  class: existing_runtime_code
  permitted_use: append_functions_only
  allowed_tasks: [P1.4]
  validator: tests/robust_asr/test_degradation_v1.py
  checksum_required: true
  large_artifact: false
  commit_allowed: true
  notes: P1.4 narrow additive patch. Add sample_clean, sample_cafe_noise,
         sample_phone_band, sample_far_field_room, sample_muffled_lowpass per
         Section 3 sample_<family>(input_wav, output_wav, seed, params) -> dict
         interface. Preserve existing apply_degradation, DEGRADATION_FAMILIES,
         and DEGRADATION_VERSION value.

- path: libs/common/**
  class: implementation_reuse
  permitted_use: read_only
  allowed_tasks: [P1.1, P2.1, P3.1, P4.1, P4.2, P4.3, P5.1, P8.1]
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: false
  notes: versions.py; read for version constants

# Specific override of libs/common/** (P0.4 scope-change). New file authored by
# robust_asr; takes precedence over the libs/common/** read_only default.
- path: libs/common/runtime_contract.py
  class: robust_asr_owned_extension
  permitted_use: write
  allowed_tasks: [P0.4, P9.0]
  validator: scripts/robust_asr/validate_runtime_contract.py
  checksum_required: true
  large_artifact: false
  commit_allowed: true
  notes: New runtime contract schema module created by P0.4 and finalized by P9.0.

# Specific overrides of libs/common/** (P1.2 scope-change). Robust_asr authors
# the canonical eval schema, normalization function, and metrics module under
# libs/common/. These take precedence over the libs/common/** read_only default.
- path: libs/common/eval_schema.yaml
  class: robust_asr_owned_extension
  permitted_use: read_write
  allowed_tasks: [P1.2, P9.0]
  validator: scripts/robust_asr/validate_eval_schema.py
  checksum_required: true
  large_artifact: false
  commit_allowed: true
  notes: Canonical 28-column eval schema YAML created by P1.2; finalized by P9.0.

- path: libs/common/normalization.py
  class: robust_asr_owned_extension
  permitted_use: read_write
  allowed_tasks: [P1.2, P2.1, P3.1, P4.2, P4.3, P5.1, P6.1, P7.3, P8.1, P9.0]
  validator: tests/robust_asr/test_normalization_metrics.py
  checksum_required: true
  large_artifact: false
  commit_allowed: true
  notes: Single shared text normalization function for all backends; created by P1.2.

- path: libs/common/metrics.py
  class: robust_asr_owned_extension
  permitted_use: read_write
  allowed_tasks: [P1.2, P2.1, P3.1, P4.1, P4.2, P4.3, P5.1, P6.1, P7.3, P8.1]
  validator: tests/robust_asr/test_normalization_metrics.py
  checksum_required: true
  large_artifact: false
  commit_allowed: true
  notes: Robust_asr WER/CER/WA + paired bootstrap CI helpers; created by P1.2.
         Distinct from libs/audio/metrics.py (training profile), which remains
         read-only.

# P1.2 + P6.1 scope-change overrides for libs/common/versions.py. The file
# already exists under the libs/common/** read_only default; authorized tasks
# may append a narrow constant only. Existing constants must be preserved.
#   - P1.2 (CHANGE_SCOPE before P1.2 implementation) authorized appending
#     NORMALIZATION_VERSION; sentinel NORMALIZATION_VERSION_constant_present.
#   - P6.1 (CHANGE_SCOPE 9ffc885) authorizes appending
#     DETERMINISTIC_SELECTOR_VERSION; sentinel
#     DETERMINISTIC_SELECTOR_VERSION_constant_present.
# METRICS_VERSION, DEGRADATION_VERSION, ENHANCER_VERSION, FEATURES_VERSION,
# ROUTER_VERSION, LORA_VERSION must remain unchanged.
- path: libs/common/versions.py
  class: existing_runtime_code
  permitted_use: append_constants_only
  allowed_tasks: [P1.2, P6.1]
  validator: NORMALIZATION_VERSION_constant_present AND DETERMINISTIC_SELECTOR_VERSION_constant_present
  checksum_required: true
  large_artifact: false
  commit_allowed: true
  notes: P1.2 appended NORMALIZATION_VERSION; P6.1 appends
         DETERMINISTIC_SELECTOR_VERSION (e.g. "deterministic_selector_v1")
         only. METRICS_VERSION, DEGRADATION_VERSION, ENHANCER_VERSION must
         remain unchanged.

- path: libs/observability/**
  class: implementation_reuse
  permitted_use: read_only
  allowed_tasks: []
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: false
  notes: Observability library; not directly used by training tasks

- path: infra/**
  class: implementation_reuse
  permitted_use: read_only
  allowed_tasks: []
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: false
  notes: Infrastructure configs; not used by training tasks

- path: scripts/training/**
  class: implementation_reuse
  permitted_use: read_only
  allowed_tasks: [P3.1, P4.1]
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: false
  notes: Prior training scripts; read for reference only; do not duplicate

- path: slurm/tools/**
  class: implementation_reuse
  permitted_use: read_only
  allowed_tasks: [P0.3, P2.1, P3.1, P4.1, P4.2, P4.3, P5.1, P7.2, P8.1]
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: false
  notes: on_submit.sh Slurm wrapper; used (not modified) by all Slurm tasks

- path: slurm/jobs/**
  class: implementation_reuse
  permitted_use: read_only_with_robust_asr_writes
  allowed_tasks: [P0.3, P1.4, P2.1, P3.1, P4.1, P4.2, P4.3, P5.1, P7.2, P8.1]
  validator: file_basename_matches_p<task_id>_*.sh
  checksum_required: false
  large_artifact: false
  commit_allowed: true
  notes: >
    Existing prior Slurm scripts (t*.sh, reserve_*.sh) are read-only for
    reference. robust_asr tasks may create new scripts named
    p<task_id>_*.sh (e.g. p0_3_runtime_smoke.sh) under slurm/jobs/,
    scoped to the listed allowed_tasks. New robust_asr p*.sh files are
    committable.

- path: tests/**
  class: implementation_reuse
  permitted_use: read_only
  allowed_tasks: []
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: false
  notes: Existing tests (excluding tests/robust_asr/**); must not be deleted or weakened

- path: configs/training/**
  class: implementation_reuse
  permitted_use: read_only
  allowed_tasks: [P3.1, P4.1]
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: false
  notes: Prior training configs; read for reference only

# ---------------------------------------------------------------------------
# D. External host paths (data_root / no_touch)
# ---------------------------------------------------------------------------

- path: /mnt/fast/nobackup/users/gb0048/opro2/pytorch_2.1_cuda12.sif
  class: container_image
  permitted_use: exec_only
  allowed_tasks: [P0.3, P2.1, P3.1, P4.1, P4.2, P4.3, P5.1, P7.2, P8.1]
  validator: sha256_recorded_in_runtime_smoke_job_metadata
  checksum_required: true
  large_artifact: true
  commit_allowed: false
  notes: >
    Apptainer container image. May be executed via `apptainer exec`
    from compute nodes (and probed via `sha256sum` / `stat` from
    datamove1) by the listed tasks. Image bytes are never modified,
    copied into the repo, or committed. Same task list as
    slurm/tools/** (every task that submits a Slurm job).

- path: /mnt/fast/nobackup/scratch4weeks/gb0048/asr_enhancement_training/datasets/**
  class: data_root
  permitted_use: read_only
  allowed_tasks: [P1.1, P1.2, P1.3, P1.4, P2.1, P3.1, P4.1, P4.2, P4.3, P8.1]
  validator: none
  checksum_required: false
  large_artifact: true
  commit_allowed: false
  notes: Librispeech manifests and audio; read-only for eval and training; never committed.
         P1.1 added by scope-change for dataset root inventory (read-only ls/stat/find).
         P1.4 added by scope-change for read access to LibriSpeech source audio inputs;
         P1.4 write access is granted only on the degradation_v1/** subtree below.

# Specific override of /mnt/.../datasets/** (P1.4 scope-change). P1.4 writes
# generated degraded audio under the degradation_v1/** subtree only. The audio
# files are large_artifact and never committed; manifests live under
# artifacts/robust_asr/manifests/ and are committed there.
- path: /mnt/fast/nobackup/scratch4weeks/gb0048/asr_enhancement_training/datasets/degradation_v1/**
  class: data_root
  permitted_use: read_write
  allowed_tasks: [P1.4, P2.1, P3.1, P4.1, P4.2, P4.3, P8.1]
  validator: none
  checksum_required: false
  large_artifact: true
  commit_allowed: false
  notes: P1.4 build_degradation_v1 audio outputs (clean identity links plus
         four non-clean families x ID/OOD-param tiers x LibriSpeech splits).
         Read-only for downstream eval/training tasks. Never committed; subject
         to Section 5.8 budget (max 50 GB scratch).

- path: /mnt/fast/nobackup/scratch4weeks/gb0048/sources/**
  class: data_root
  permitted_use: read_only
  allowed_tasks: [P1.1, P1.2, P1.3, P2.1, P3.1, P4.1, P4.2, P4.3, P8.1]
  validator: none
  checksum_required: false
  large_artifact: true
  commit_allowed: false
  notes: Public dataset source roots for LibriSpeech, Common Voice, TED-LIUM, CHiME and related
         inventory/build tasks. Read-only. Never committed. Added by P1.1 scope-change.

- path: /mnt/fast/nobackup/scratch4weeks/gb0048/asr_enhancement_training/runs/**
  class: no_touch
  permitted_use: forbidden
  allowed_tasks: []
  validator: none
  checksum_required: false
  large_artifact: true
  commit_allowed: false
  notes: Prior training run directories; NOT valid robust_asr evidence until validated

- path: /mnt/fast/nobackup/scratch4weeks/gb0048/asr_enhancement_training/cache/**
  class: no_touch
  permitted_use: forbidden
  allowed_tasks: []
  validator: none
  checksum_required: false
  large_artifact: true
  commit_allowed: false
  notes: Whisper and HuggingFace model caches; used implicitly by containers; never modified

# Specific override of /mnt/.../cache/** (P2.1-model scope-change). Read-only
# access to a single openai-whisper PyTorch checkpoint with a pinned canonical
# sha256, used as the source for offline CT2 INT8 conversion in P2.1.
- path: /mnt/fast/nobackup/scratch4weeks/gb0048/asr_enhancement_training/cache/whisper/base.en.pt
  class: data_root
  permitted_use: read_only
  allowed_tasks: [P2.1]
  validator: sha256==25a8566e1d0c1e2231d1c762132cd20e0f96a85d16145c3a00adf5d1ac670ead
  checksum_required: true
  large_artifact: true
  commit_allowed: false
  notes: openai-whisper base.en PyTorch checkpoint; canonical sha256 pinned;
         read-only source for ctranslate2 OpenAIWhisperConverter in P2.1; never
         modified, never committed. The cache root remains no_touch by default.

# New robust_asr-controlled model output path (P2.1-model scope-change).
# CT2 INT8 model directories (model.bin + config.json + tokenizer + vocabulary
# + provenance.json) live here. Never committed.
- path: /mnt/fast/nobackup/scratch4weeks/gb0048/asr_enhancement_training/runtime/whisper_models/**
  class: model_root
  permitted_use: read_write
  allowed_tasks: [P2.1, P3.1, P4.1, P4.2, P4.3, P5.1, P7.2, P8.1]
  validator: sha256_recorded_in_provenance_json
  checksum_required: true
  large_artifact: true
  commit_allowed: false
  notes: robust_asr model output root. P2.1 writes whisper_base_en_ct2_int8/
         (model.bin + config + tokenizer + vocabulary + provenance.json) from
         offline ctranslate2 OpenAIWhisperConverter on the pinned base.en.pt
         source. Downstream LoRA / system tasks read these weights.

- path: /mnt/fast/nobackup/scratch4weeks/gb0048/asr_enhancement_training/demo_reference_artifacts/**
  class: data_root
  permitted_use: read_only
  allowed_tasks: [P8.2]
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: false
  notes: 5 demo reference examples; read by P8.2 to produce demo_examples_manifest.json

- path: /mnt/fast/nobackup/scratch4weeks/gb0048/asr_enhancement_training/runtime/**
  class: data_root
  permitted_use: read_write
  allowed_tasks: [P0.3]
  validator: none
  checksum_required: false
  large_artifact: true
  commit_allowed: false
  notes: >
    robust_asr runtime build output directory. Holds the rebuilt SIF
    and any throwaway sandbox/cache produced by
    scripts/robust_asr/build_runtime_image.sh. Never committed to git.

# Specific override of /mnt/.../runtime/** (P5.1 scope-change). AssemblyAI
# transcript cache keyed by audio_sha256; one JSON per audio_id plus
# cache_summary.json. Lives on scratch (large_artifact); never committed.
- path: /mnt/fast/nobackup/scratch4weeks/gb0048/asr_enhancement_training/runtime/assemblyai_cache/**
  class: data_root
  permitted_use: read_write
  allowed_tasks: [P5.1]
  validator: cache_summary_json
  checksum_required: false
  large_artifact: true
  commit_allowed: false
  notes: AssemblyAI transcript cache keyed by audio_sha256. Never committed.

- path: /mnt/fast/nobackup/scratch4weeks/gb0048/asr_enhancement_training/runtime/robust_asr_py311_cuda12.sif
  class: container_image
  permitted_use: exec_only
  allowed_tasks: [P0.3, P1.4, P2.1, P3.1, P4.1, P4.2, P4.3, P5.1, P7.2, P8.1]
  validator: sha256_recorded_in_runtime_smoke_job_metadata
  checksum_required: true
  large_artifact: true
  commit_allowed: false
  notes: >
    robust_asr-specific Apptainer container image (Python 3.11 +
    ctranslate2 + faster_whisper + pytest + LightGBM/XGBoost where
    available, plus the same torch/transformers/peft/librosa/numpy/
    pandas/pyarrow/pyyaml set as the prior image). Built from
    configs/robust_asr/runtime/apptainer_robust_asr_v1.def by
    scripts/robust_asr/build_runtime_image.sh and stored under
    scratch4weeks (large artifact, never committed). Same allowed_tasks
    as slurm/tools/**. Replaces the legacy
    /mnt/fast/nobackup/users/gb0048/opro2/pytorch_2.1_cuda12.sif image
    for robust_asr Slurm jobs after P0.3 rerun PASSes; the legacy image
    is preserved untouched.

# ---------------------------------------------------------------------------
# E. Mandatory no-touch patterns (from agent plan Section 2.2)
# ---------------------------------------------------------------------------

- path: ".git/**"
  class: no_touch
  permitted_use: forbidden
  allowed_tasks: []
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: false
  notes: Git internals; never touched directly

- path: ".env"
  class: no_touch
  permitted_use: forbidden
  allowed_tasks: []
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: false
  notes: Secrets file

- path: ".env.*"
  class: no_touch
  permitted_use: forbidden
  allowed_tasks: []
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: false
  notes: "Secrets files (exception: .env.example is safe but not written by robust_asr tasks)"

- path: "*.key"
  class: no_touch
  permitted_use: forbidden
  allowed_tasks: []
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: false
  notes: Key files

- path: "*.pem"
  class: no_touch
  permitted_use: forbidden
  allowed_tasks: []
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: false
  notes: Certificate/key files

- path: "*.token"
  class: no_touch
  permitted_use: forbidden
  allowed_tasks: []
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: false
  notes: Token files

- path: "runs/**"
  class: no_touch
  permitted_use: forbidden
  allowed_tasks: []
  validator: none
  checksum_required: false
  large_artifact: true
  commit_allowed: false
  notes: Legacy training run directories (in-repo path)

- path: ".cache/**"
  class: no_touch
  permitted_use: forbidden
  allowed_tasks: []
  validator: none
  checksum_required: false
  large_artifact: true
  commit_allowed: false
  notes: Cache directories

- path: ".hf_cache/**"
  class: no_touch
  permitted_use: forbidden
  allowed_tasks: []
  validator: none
  checksum_required: false
  large_artifact: true
  commit_allowed: false
  notes: HuggingFace cache

- path: "**/__pycache__/**"
  class: no_touch
  permitted_use: forbidden
  allowed_tasks: []
  validator: none
  checksum_required: false
  large_artifact: false
  commit_allowed: false
  notes: Python bytecode

# Specific override of the generic "*.wav" no_touch rule below (P8.2
# scope-change CHANGE_SCOPE 7f72213). Exactly 8 small public deterministic
# WAV demo files produced by scripts/robust_asr/build_demo_examples.py.
# Audio is LibriSpeech-derived (CC BY 4.0) + degradation_v1; no API audio,
# no model audio, no checkpoints. Read by P9.1 handoff packaging.
- path: artifacts/robust_asr/demo/audio/**
  class: active_state
  permitted_use: write
  allowed_tasks: [P8.2, P9.1]
  validator: demo_examples_manifest_references_all_files
  checksum_required: true
  large_artifact: false
  commit_allowed: true
  notes: Exactly 8 small public deterministic WAV demo files. This is an
         explicit carveout from the generic audio no-touch rule (*.wav).
         Each file is referenced by a row in
         artifacts/robust_asr/demo/demo_examples_manifest.json with a
         recorded audio_sha256.

- path: "*.wav"
  class: no_touch
  permitted_use: forbidden
  allowed_tasks: []
  validator: none
  checksum_required: false
  large_artifact: true
  commit_allowed: false
  notes: Audio files. Specific carveout for
         artifacts/robust_asr/demo/audio/** is declared above (P8.2).

- path: "*.flac"
  class: no_touch
  permitted_use: forbidden
  allowed_tasks: []
  validator: none
  checksum_required: false
  large_artifact: true
  commit_allowed: false
  notes: Audio files

- path: "*.mp3"
  class: no_touch
  permitted_use: forbidden
  allowed_tasks: []
  validator: none
  checksum_required: false
  large_artifact: true
  commit_allowed: false
  notes: Audio files

- path: "*.m4a"
  class: no_touch
  permitted_use: forbidden
  allowed_tasks: []
  validator: none
  checksum_required: false
  large_artifact: true
  commit_allowed: false
  notes: Audio files

- path: "*.pt"
  class: no_touch
  permitted_use: forbidden
  allowed_tasks: []
  validator: none
  checksum_required: false
  large_artifact: true
  commit_allowed: false
  notes: PyTorch weights

- path: "*.pth"
  class: no_touch
  permitted_use: forbidden
  allowed_tasks: []
  validator: none
  checksum_required: false
  large_artifact: true
  commit_allowed: false
  notes: PyTorch weights

- path: "*.ckpt"
  class: no_touch
  permitted_use: forbidden
  allowed_tasks: []
  validator: none
  checksum_required: false
  large_artifact: true
  commit_allowed: false
  notes: Checkpoint files

- path: "*.bin"
  class: no_touch
  permitted_use: forbidden
  allowed_tasks: []
  validator: none
  checksum_required: false
  large_artifact: true
  commit_allowed: false
  notes: Binary model files

- path: "*.safetensors"
  class: no_touch
  permitted_use: forbidden
  allowed_tasks: []
  validator: none
  checksum_required: false
  large_artifact: true
  commit_allowed: false
  notes: Safetensors model files
