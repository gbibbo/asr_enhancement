# Demo and Platform Task Progress

Branch: feature/demo-runtime-rp5-v1
Integration branch: demo-rp5-v1
Parallel training branch: feature/training-datamove1-v1
Current track: B (RP5 setup)
Current phase: Phase B9
Current task: Task B9.1

Note: current_task was corrected from stale B5.4 to B6.1 at the start of this task. No B5.4
exists in docs/plans/demo_platform_plan.md. Phase B5 ended at B5.3. After B6.5.2 the plan
advances to Phase B8 (Reproducible cache); there is no B6.6 and no B7.2.

## Completed

- B8.1: done on 2026-05-03. §30 Reproducible cache scaffolded with three top-level CLI scripts that read from the existing B6.5.1 baseline report and never run live ASR or contact any network service. Created: scripts/prewarm_cache.py (CLI: --examples, --artifacts-root, --baseline-report, --db, --asr-provider, --asr-model, --enhancer, --source, --dry-run; rejects --asr-provider assemblyai with exit 2 and message "AssemblyAI cache prewarm is deferred to a later quota-aware task."; rejects --source run with exit 2 and message pointing back to --source reports; rejects any --enhancer other than bypass with exit 2 and a T4.1 reference; reads the planned (example_id, variant) set from config/demo_examples.json including the implicit clean variant; for each pair looks up reports/demo/b6_5_1_rp5_results.json by (example_id, degradation_id), recomputes audio_sha256 from disk and aborts the entire run with exit 1 if any pair drifts from the baseline; computes cache_key via libs.demo.cache.build_cache_key with explicit enhancer_version="bypass"; stores artifact_root as the absolute artifacts root and result_json.audio_path_relative including the example_id directory; upserts via libs.demo.persistence.upsert_cache_entry; writes nothing under --dry-run). Created: scripts/validate_cache.py (CLI: --db, --artifacts-root, --report-json, --report-md, --strict, --mark-validated; per-row outcomes ok / stale_version / key_mismatch / missing_artifact / sha256_mismatch / bad_payload; checks stale_version before key_mismatch so version drift surfaces correctly; resolves audio as Path(row["artifact_root"]) / payload["audio_path_relative"] and asserts containment + sha256; writes JSON+MD report; --mark-validated only updates validated_at on ok rows; --strict exits 1 if any non-ok row exists). Created: scripts/invalidate_cache.py (CLI: --db, --by-degradation-version, --by-metrics-version, --by-enhancer-version, --by-asr-provider, --by-asr-model-version, --by-example-id (repeatable), --mode {match,not-match}, --apply, --quiet; refuses to run with no filter; default is dry-run; never touches audio files; transactional DELETE only with --apply). Modified: libs/demo/persistence.py (added narrow cache_entries helpers: upsert_cache_entry returning "inserted" or "updated", list_cache_entries, delete_cache_entries with where_sql/params/dry_run, mark_cache_entry_validated; existing get_cache_entry, init_schema, and DDL untouched). Created: tests/demo/test_b8_1_cache_scripts.py (20 tests covering prewarm row count and key/columns invariants, --dry-run zero-write, sha mismatch abort, missing-in-report skip, AssemblyAI/--source-run/MetricGAN+ deferral guards, artifact_root contract — value equals the artifacts root and audio_path_relative includes the example_id directory; validate ok/stale_version/missing_artifact/sha256_mismatch outcomes, --strict exit, --mark-validated only on ok; invalidate refuse-without-filter, dry-run no-op, --apply removes only matching rows, audio files survive deletion, --mode not-match; persistence helpers roundtrip including ValueError on empty where clause). Generated: reports/demo/b8_1_cache_validation.json and .md (host=ede511dfa6a0, generated_at=2026-05-03T..., db_path=/home/gbibbo/asr_enhancement_runtime/db/demo.db, artifacts_root=/home/gbibbo/asr_enhancement_runtime/artifacts/examples, versions snapshot DEGRADATION_VERSION=degradation_v1 / METRICS_VERSION=1.0 / DEFAULT_ENHANCER_VERSION=1.0, total_rows=30, outcomes ok=30 stale_version=0 key_mismatch=0 missing_artifact=0 sha256_mismatch=0 bad_payload=0). Validation inside the demo container: container syntax check (ast.parse) clean for all four files; 20/20 new B8.1 tests pass; tests/demo/test_cache_key.py + tests/demo/test_persistence.py pass alongside (55/55 combined); 456/456 targeted regression tests pass (tests/demo + tests/audio + tests/asr + tests/common = 436 pre-B8.1 baseline + 20 new). Real RP5 SQLite end-to-end inside ephemeral demo-api containers: prewarm wrote 30 rows (inserted=30, updated=0, missing_in_report=0, sha_mismatch=0); validate --mark-validated --strict reported ok=30 with zeros across stale_version/key_mismatch/missing_artifact/sha256_mismatch/bad_payload and exit 0; dry-run invalidate --by-degradation-version degradation_v0 --mode match matched 0 rows. Protected diff empty (libs/common/versions.py, libs/audio/, libs/demo/cache.py, libs/demo/examples.py, libs/asr/, services/, config/, reports/demo/b6_5_1_rp5_results.json, reports/demo/b6_5_1_comparison.json, reports/demo/b6_5_2_enhancer_validation.json, reports/demo/b6_5_1_comparison.md, reports/demo/b6_5_2_enhancer_validation.md, pyproject.toml, requirements.lock.arm64, requirements.lock.x86_64, infra/compose/Dockerfile.demo, infra/compose/docker-compose.demo.yml all untouched). No audio files in git status. AssemblyAI prewarm and --source run remain explicitly deferred to a future quota-aware task; usage_ledger is not read or written by B8.1. Validation commands used `docker compose -f infra/compose/docker-compose.demo.yml run --rm` with `${REPO}/scripts:/app/scripts:ro` for all script invocations and `${REPO}/reports:/out/reports` for the validate run that wrote b8_1_cache_validation.json/.md back to the host filesystem. Advance to B9.1 (Implement upload validation) per docs/plans/demo_platform_plan.md §31.

- B6.5.2: done on 2026-05-03. §29 RP5 enhancer behavior validation closed with decision `default_enhancer="bypass"`. Created: scripts/demo/run_enhancer_validation.py (CLI runner: `--examples`, `--candidates`, `--artifacts`, `--baseline-rp5`, `--output`, `--md`, `--enhanced-output-root`; per (example_id, degradation_id) pair calls `BypassEnhancer().enhance()`, hashes both input and reported output, transcribes the enhanced output via faster-whisper tiny.en `WhisperAdapter`, looks up the matching B6.5.1 row by (example_id, degradation_id), computes `delta_wa_vs_baseline` to 6 decimals; for `MetricGANPlusEnhancer` makes a single probe call on `ex001/clean.wav` and records `status="not_implemented"` + `error_class` + `error_message`; emits both JSON and Markdown in one invocation; exits 1 if any bypass row breaks strict tolerance, exits 2 if MetricGAN+ does not raise NotImplementedError; reads versions dynamically from libs.common.versions; opens the B6.5.1 baseline read-only and never modifies it). Created: tests/demo/test_b6_5_2_enhancer.py (9 tests covering the 30-row contract + frozen-version stamps, per-row `input_equals_output`/sha equality/preset/`enhanced_flag`/`enhancement_fallback`/`delta_wa_vs_baseline==0.0`, MetricGAN+ probe records `not_implemented` + `NotImplementedError` + T4.1 reference, decision section pins `default_enhancer="bypass"` and `blocked_on_upstream_task="T4.1 in feature/training-datamove1-v1"`, `main()` returns 0 on success and writes both JSON+MD, `main()` returns 1 when a single perturbed baseline row breaks strict tolerance and the offending row appears in `enhancers.bypass.offenders`, baseline file is opened read-only and is byte-stable across runs, `_resolve_variant_path` for clean+degraded, `_baseline_index` ignores rows with missing keys; all tests load the runner via importlib.util.spec_from_file_location). Generated: reports/demo/b6_5_2_enhancer_validation.json (host=ede511dfa6a0, generated_at=2026-05-03T02:01:43Z, asr_engine=faster-whisper, asr_model=tiny.en, degradation_version=degradation_v1, metrics_version=1.0, default_enhancer_version=1.0, scope=[ex001,ex003,ex004,ex007,ex010], variant_ids=[clean,far_field_room,cafe_background,phone_call,muffled,broadband_hiss], baseline_rp5_path=/out/reports/demo/b6_5_1_rp5_results.json with baseline_rp5_generated_at=2026-05-03T00:58:12Z); BypassEnhancer aggregates: n_rows=30, n_input_equals_output=30, n_rows_with_nonzero_delta=0, max_abs_delta_wa_vs_baseline=0.0, mean_delta_wa_vs_baseline=0.0, mean_enhance_latency_seconds=1.1e-05, p95_enhance_latency_seconds=1.4e-05, mean_asr_latency_seconds=1.76236, p95_asr_latency_seconds=1.950359; MetricGANPlusEnhancer probe: status=not_implemented, error_class=NotImplementedError, error_message="MetricGAN+ implementation is owned by task T4.1 in the training branch. Use BypassEnhancer until T4.1 is merged into demo-rp5-v1." Decision: default_enhancer=bypass, ui_label_for_bypass="honest passthrough", ui_label_for_metricgan_plus="unavailable", blocked_on_upstream_task="T4.1 in feature/training-datamove1-v1". Generated: reports/demo/b6_5_2_enhancer_validation.md (mirrors the report contents in human-readable form; explicit footnote that DSP presets `light_clean`, `denoise`, `denoise_dereverb` are out of scope for B6.5.2 because they are pipeline presets in `apply_preset()` rather than `EnhancerAdapter` subclasses, and that real MetricGAN+ inference depends on T4.1). Done-when criteria: (1) RP5 enhancer path "works or fails explicitly" satisfied — bypass path works honestly with byte-identical input/output and zero delta on every row; MetricGAN+ path fails explicitly with NotImplementedError; (2) improvement or limitation documented in reports/demo/b6_5_2_enhancer_validation.{json,md}; (3) latency recorded — bypass enhance latency is sub-microsecond (mean 11 µs, p95 14 µs) which is the expected near-zero overhead for a no-op, and ASR latency on RP5 is ~1.76 s mean / 1.95 s p95 per row; (4) UI wording can be honest — default_enhancer=bypass with ui_label_for_bypass="honest passthrough" and ui_label_for_metricgan_plus="unavailable". Plan §29 Decision rule 3 applied (enhancer too slow or broken / not yet available → keep bypass default and document limitation). Validation inside the demo container: 9/9 new B6.5.2 unit tests pass; 436/436 targeted regression tests pass (tests/demo + tests/audio + tests/asr + tests/common = 427 B6.5.1 baseline + 9 new). Protected diff empty (libs/audio/enhancement.py, libs/common/versions.py, libs/audio/degradations.py, libs/audio/metrics.py, libs/asr/, libs/demo/, services/, config/, reports/demo/b6_5_1_rp5_results.json, reports/demo/b6_5_1_surrey_results.json, reports/demo/b6_5_1_comparison.json, reports/demo/b6_5_1_comparison.md, reports/demo/demo_examples_asr_drop.json, reports/demo/demo_examples_asr_drop.md, pyproject.toml, requirements.lock.arm64, requirements.lock.x86_64, infra/compose/Dockerfile.demo, infra/compose/docker-compose.demo.yml all untouched). No audio files in git status (only the 4 new files: scripts/demo/run_enhancer_validation.py, tests/demo/test_b6_5_2_enhancer.py, reports/demo/b6_5_2_enhancer_validation.json, reports/demo/b6_5_2_enhancer_validation.md). No new dependency added; pyproject.toml and lockfiles unchanged; speechbrain not introduced (real MetricGAN+ remains owned by T4.1 in feature/training-datamove1-v1 per CLAUDE.md §10.4). Validation commands used the `docker compose run --rm` ephemeral pattern with `${REPO}/scripts:/app/scripts:ro`, `${REPO}/tests:/app/tests:ro` for the test runs and `${REPO}/reports:/out/reports` for the validation run. Advance to B8.1 (Implement cache scripts) per docs/plans/demo_platform_plan.md §30.

- B6.5.1: done on 2026-05-02. §29 RP5 vs Surrey ASR cross-validation closed with verdict `comparable`; no docs/model_card.md update required. Inputs: reports/demo/b6_5_1_rp5_results.json (faster-whisper tiny.en on RP5, host a6488873933a, 30 rows, generated_at 2026-05-03T00:58:12Z) and reports/demo/b6_5_1_surrey_results.json (openai-whisper tiny.en on Surrey/datamove1, host aisurrey03.surrey.ac.uk, 30 rows, generated_at 2026-05-03T01:29:54Z). Surrey JSON delivered manually via Windows bridge from datamove1 (datamove1 cannot reach asr-rp5 over SSH and the Anaconda 3.9 env on datamove1 is intentionally not used for openai-whisper); openai-whisper executed inside the Surrey Apptainer image (torch + openai-whisper provisioned). Surrey JSON validated against the runner contract: engine=openai-whisper, model=tiny.en, degradation_version=degradation_v1, metrics_version=1.0, len(results)==30 with 30 unique (example_id, degradation_id) pairs. Generated reports/demo/b6_5_1_comparison.json and reports/demo/b6_5_1_comparison.md inside the demo-api container via scripts/demo/compare_rp5_surrey.py. Successful completion of the comparator implies audio_sha256 matched on every (example_id, degradation_id) pair across the two reports — the script raises CompareError on any mismatch. Aggregates: degraded_mean_delta_wa=-0.0024 over the 25 degraded rows (RP5 minus Surrey), degraded_mean_abs_delta_wa=0.0024, degraded_max_abs_delta_wa=0.0588 driven by a single (ex007, muffled) pair where openai-whisper scored 0.8235 vs faster-whisper 0.7647; clean_mean_delta_wa=0.0000 across all 5 clean rows; per-degradation mean_delta_wa: broadband_hiss/cafe_background/far_field_room/phone_call all 0.0000, muffled -0.0118; verdict thresholds COMPARABLE_THRESHOLD=0.02 and DISCREPANT_THRESHOLD=0.05 (in [0,1] WA units); abs(degraded_mean_delta_wa)=0.0024 << 0.02 → verdict comparable; requires_model_card_update=false. Latency: total RP5 53.24 s vs total Surrey 37.66 s on the 30 rows (informational; not part of the §29 verdict). Validation inside the demo container: 19/19 B6.5.1 unit tests pass (tests/demo/test_b6_5_1_runner.py + tests/demo/test_b6_5_1_compare.py); 427/427 targeted regression tests pass (tests/demo + tests/audio + tests/asr + tests/common). Protected diff empty (libs/common/versions.py, libs/audio/degradations.py, libs/audio/metrics.py, libs/audio/enhancement.py, libs/demo/cache.py, libs/asr/whisper_provider.py, libs/asr/demo_assemblyai_provider.py, services/api/app/demo_main.py, services/worker/app/demo_worker_main.py, config/demo_examples.json, config/demo_example_candidates.json, reports/demo/demo_examples_asr_drop.json, reports/demo/demo_examples_asr_drop.md, scripts/demo/materialize_demo_examples.py, scripts/demo/select_demo_candidates.py, pyproject.toml, requirements.lock.arm64, requirements.lock.x86_64, infra/compose/Dockerfile.demo, infra/compose/docker-compose.demo.yml all untouched). No audio files in git status. Decision branch taken: comparable → no model card update; B6.5.1 marked done; advance to B6.5.2 (Validate enhancer behavior on RP5).

- B6.5.1 (Milestone A, blocked): on 2026-05-02. RP5-side artifacts for the §29 RP5 vs Surrey ASR cross-validation pushed; task remains blocked until Surrey/datamove1 returns the openai-whisper reference report. Created: scripts/demo/run_reference_whisper.py (CLI runner with --engine faster-whisper / --engine openai-whisper; openai-whisper is imported lazily so the module loads cleanly on RP5 without it; reads config/demo_examples.json + config/demo_example_candidates.json; processes 5 public examples × 6 audio variants = 30 rows; records hypothesis, WER, word_accuracy, latency_seconds, audio_sha256, example_id, source_recording_id, degradation_id; stamps engine, model, host, generated_at, examples_config, candidates_config, artifacts_root, degradation_version, metrics_version, default_enhancer_version, degradation_parameters_signature, scope, variant_ids; --resume reuses already-present rows; uses libs.audio.metrics.compute_metrics; reads versions dynamically from libs.common.versions). Created: scripts/demo/compare_rp5_surrey.py (verifies matching degradation_version, metrics_version, (example_id, degradation_id) row set, and audio_sha256 per row; computes signed delta_wa and abs_delta_wa per pair; emits primary aggregates over the 25 degraded rows only — degraded_mean_delta_wa, degraded_mean_abs_delta_wa, degraded_max_abs_delta_wa, degraded_per_degradation_mean_delta_wa; auxiliary aggregates over clean rows and all rows; thresholds COMPARABLE_THRESHOLD=0.02 and DISCREPANT_THRESHOLD=0.05 in [0,1] units; verdict comparable / caution / discrepant from abs(degraded_mean_delta_wa); aux warnings if clean rows disagree above the comparable threshold or if degraded_mean_abs_delta_wa exceeds discrepant threshold while signed mean stays comparable; renders Markdown with verdict, per-degradation table, and per-pair detail; CompareError on mismatch returns exit code 2). Created: tests/demo/test_b6_5_1_runner.py (8 tests covering 30-row contract, frozen-version stamps, audio_sha256 + non-negative latency on every row, --resume skip behavior, openai-whisper module not imported when faster-whisper engine is selected, openai-whisper code path uses the whisper module, _resolve_variant_path for clean and degraded variants, _degradation_parameters_signature stability) and tests/demo/test_b6_5_1_compare.py (11 tests covering version mismatches, row-set mismatches, audio_sha256 mismatches, all three verdicts, clean-disagreement aux warning, threshold constants, Markdown render, and main() exit codes); both test modules load the scripts via importlib.util.spec_from_file_location so they do not require /app/scripts to be on sys.path inside the demo container. Generated: reports/demo/b6_5_1_rp5_results.json with 30 rows from a live faster-whisper tiny.en run inside the demo-worker container against /home/gbibbo/asr_enhancement_runtime/artifacts/examples; degradation_version=degradation_v1, metrics_version=1.0, default_enhancer_version=1.0; word_accuracy values match the B6.2 audit (reports/demo/demo_examples_asr_drop.json) within 0.001 on all 30 overlapping rows. Validation inside the demo container: 19/19 new B6.5.1 unit tests pass; 427/427 targeted regression tests pass (tests/demo + tests/audio + tests/asr + tests/common); full suite 676 passed (B7.1 baseline 657 + 19 new) / 10 failed / 11 errors with all failures and errors being the pre-existing platform/observability/db/storage/smoke tests requiring Postgres+MinIO+Grafana, unchanged from B7.1; build_cache_key sanity returned ex001|phone_call|degradation_v1|whisper|tiny.en|1.0|1.0; protected diff empty (libs/common/versions.py, libs/audio/degradations.py, libs/audio/metrics.py, libs/audio/enhancement.py, libs/demo/cache.py, libs/asr/whisper_provider.py, libs/asr/demo_assemblyai_provider.py, services/api/app/demo_main.py, services/worker/app/demo_worker_main.py, config/demo_examples.json, config/demo_example_candidates.json, reports/demo/demo_examples_asr_drop.json, reports/demo/demo_examples_asr_drop.md, scripts/demo/materialize_demo_examples.py, scripts/demo/select_demo_candidates.py, pyproject.toml, requirements.lock.arm64, requirements.lock.x86_64, infra/compose/Dockerfile.demo, infra/compose/docker-compose.demo.yml all untouched); no audio files in git status. Blocker: awaiting Surrey/datamove1 openai-whisper reference run producing reports/demo/b6_5_1_surrey_results.json. The other window must (1) check out scripts/demo/run_reference_whisper.py + scripts/demo/compare_rp5_surrey.py + the two config files from origin/feature/demo-runtime-rp5-v1, (2) source the same audio (preferably rsync from asr-rp5:/home/gbibbo/asr_enhancement_runtime/artifacts/examples for ex001/ex003/ex004/ex007/ex010), (3) run scripts/demo/run_reference_whisper.py --engine openai-whisper --model tiny.en, and (4) return reports/demo/b6_5_1_surrey_results.json. After the Surrey JSON arrives, Milestone B will run scripts/demo/compare_rp5_surrey.py on the demo-api container to produce reports/demo/b6_5_1_comparison.{json,md}, apply the §29 verdict, edit docs/model_card.md only if the verdict is discrepant, and only then mark B6.5.1 done.

- B7.1: done on 2026-05-02. Identifier-only freeze of the demo degradation contract. Modified: libs/common/versions.py (DEGRADATION_VERSION: str = "1.0" → "degradation_v1"). Modified: tests/common/test_versions_imports.py (added test_degradation_version_is_frozen_at_v1 pinning the frozen value). METRICS_VERSION and DEFAULT_ENHANCER_VERSION unchanged (both "1.0"). Scope: libs/audio/degradations.py untouched (parameters, registry, dispatch, DegradationResult shape all frozen); libs/audio/metrics.py, libs/audio/enhancement.py, libs/demo/cache.py untouched (build_cache_key already reads versions.DEGRADATION_VERSION at call time, so cache-key behavior changes only through the constant); services/, config/demo_examples.json, config/demo_example_candidates.json, reports/demo/demo_examples_asr_drop.json, reports/demo/demo_examples_asr_drop.md untouched (B6.2 5-example public set and ASR drop audit unchanged; the audit's scope_note remains a faithful record of the pre-freeze identifier under which it ran); pyproject.toml and both lockfiles untouched; no audio regenerated; no ASR drop rerun. Validation: docker image rebuilt to pick up the source change; smoke import inside ephemeral demo-api container returned `degradation_v1 1.0 1.0`; 75/75 targeted tests pass (tests/common/, tests/audio/test_audio_degradations.py, tests/audio/test_audio_degradations_imports.py, tests/audio/test_audio_metrics_imports.py, tests/demo/test_cache_key.py) including the new pin test and the dynamic-read tests that monkeypatch DEGRADATION_VERSION; full suite 657 passed (B6.2 baseline 656 + new pin test) / 10 failed / 11 errors with all failures and errors being pre-existing platform/observability/db/storage/smoke tests requiring Postgres+MinIO+Grafana not present in the demo container, unchanged from B6.2 baseline; build_cache_key sanity returned `ex001|phone_call|degradation_v1|whisper|tiny.en|1.0|1.0`; protected diff empty (libs/audio/degradations.py, libs/audio/metrics.py, libs/audio/enhancement.py, libs/demo/cache.py, services/, config/demo_examples.json, config/demo_example_candidates.json, reports/demo/demo_examples_asr_drop.json, reports/demo/demo_examples_asr_drop.md, pyproject.toml, requirements.lock.arm64, requirements.lock.x86_64 all untouched). Phase B7 gate cleared: training branch is unblocked for dry-run and full training. Next task is B6.5.1 (Run public examples on RP5 and Surrey reference) per docs/plans/demo_platform_plan.md §29; plan order places Phase B6.5 after Phase B7 and before Phase B8, and B6.5 declares B7 complete as a dependency.

- B6.2: done on 2026-05-02. Implementation: scripts/demo/materialize_demo_examples.py (downloads dev-clean.tar.gz selectively, verifies per-FLAC SHA256 from candidates JSON, converts FLAC → 16 kHz mono PCM_16 WAV via soundfile, applies five registered degradations via libs.audio.degradations.apply_degradation with seed=0, writes config/demo_examples.json; --asr-only mode runs WhisperAdapter tiny.en on every clean and degraded WAV and computes word_accuracy via libs.audio.metrics.compute_metrics; --source-dir uses standard LibriSpeech layout first then recursive search for {source_recording_id}.flac; --allow-download downloads dev-clean.tar.gz from OpenSLR to DEMO_RUNTIME_ROOT/cache then extracts only the 10 needed members and deletes the tarball; --asr-only and main mode use separate /app/config:ro and /out/config writable mounts so runtime services keep config read-only). Modified: infra/compose/docker-compose.demo.yml (added ../../config:/app/config:ro to x-demo-volumes so /demo/examples can read config/demo_examples.json from inside the container without rebuilding). Modified: tests/demo/test_demo_candidates.py (removed obsolete test_demo_examples_json_does_not_exist gate). Created: tests/demo/test_demo_examples_config.py (12 tests for the final 5-example config; preserves existing tests/demo/test_demo_examples.py). Generated: config/demo_examples.json with 5 entries (ex001, ex003, ex004, ex007, ex010). Generated reports: reports/demo/demo_examples_asr_drop.json and .md. Scope amendment: B6.1 candidate pool was 10 datamove1 reserved examples; ASR drop verification with faster-whisper tiny.en under DEGRADATION_VERSION 1.0 produced overall_pass on 5 of the 10 candidates and no measurable drop on the other 5 (ex002 clean WA=0.818 with possessive apostrophe transcribed wrong, all degradations produce equal or higher WA; ex005/ex006/ex008/ex009 clean WA=1.000 and all degradations produce 1.000 because sentences are too clear or too short to cause tiny.en errors at current degradation parameters). Decision: keep degradation parameters unchanged, do not bump DEGRADATION_VERSION, do not replace examples; final B6.2 public demo set is the 5 passing examples. Excluded examples ex002, ex005, ex006, ex008, ex009 remain reserved in candidates JSON, remain excluded from training, but are not exposed via /demo/examples. Original example IDs preserved (no renumbering) for traceability back to candidates JSON and ASR drop report. Validation: 12/12 new config tests pass; full suite inside demo container 651 passed / 0 new failures (pre-existing platform failures in observability+db+storage+smoke unrelated to B6.2 still present); GET /demo/examples returned 5 entries; GET /demo/examples/ex001/audio/clean → HTTP 200; GET /demo/examples/ex001/audio/degraded/phone_call → HTTP 200; reports/demo/demo_examples_asr_drop.json overall_pass=true with results section listing the 5 passing examples and excluded_examples section listing the 5 with reason and best observed drop; protected diff empty (services/api/, services/worker/, libs/demo/persistence.py, libs/demo/examples.py, libs/common/demo_settings.py, libs/asr/, libs/audio/, libs/common/versions.py, pyproject.toml, both lockfiles, infra/compose/Dockerfile.demo all untouched); no audio files committed.

- B6.1: done on 2026-05-02. Revised implementation: initial attempt scanned a local LibriSpeech test-clean root (blocked because that path was absent on RP5); revised implementation imports the 10 reserved examples from the datamove1 branch instead. No LibriSpeech download required. Source of truth: origin/feature/training-datamove1-v1:configs/training/reserved_public_demo_examples.yaml (commit 73808ed). Note: stale current_task B5.4 was corrected — no B5.4 exists in docs/plans/demo_platform_plan.md; phase B5 ended at B5.3. Revised: scripts/demo/select_demo_candidates.py (replaced LibriSpeech+soundfile scanner with git-show importer; reads YAML via subprocess, parses with yaml.safe_load, validates 10 entries, maps transcript→ground_truth, source_split=dev-clean, source_dataset=librispeech, excluded_from_training=True, speaker_gender="unknown" for all entries — gender metadata not tracked in datamove1; assigns example_id ex001–ex010; includes audio_sha256 from datamove1; records datamove1 ref, commit SHA, selection policy, and gender limitation in selection_notes; all CLI args have defaults). Revised: tests/demo/test_demo_candidates.py (20 tests: updated duration range [3.0,10.0], exactly 10 distinct speakers, source_split=dev-clean, added test_all_recording_ids_match_datamove1_reserved_set with exact ordered list, added test_all_speaker_genders_unknown, added test_audio_sha256_present, added test_demo_examples_json_does_not_exist, added audio_sha256 to required provenance fields). Generated: config/demo_example_candidates.json (10 entries, all public_content_review=passed_manual_review after Gabriel's explicit approval). Scope: no worker/API/ASR/audio changes; pyproject.toml and both lock files unchanged; Dockerfile.demo and docker-compose.demo.yml unchanged; config/demo_examples.json not created (audio sourcing is B6.2). Validation: 20/20 candidate tests pass including gate test; 654 pass / 0 new failures (pre-existing platform/infra errors unchanged); GET /demo/examples → {"examples":[],"total":0,"note":"No curated examples loaded. Run Phase B6 to populate."} (correct — demo_examples.json not yet created); protected diff empty.

- B5.3: implemented enhancer interface, bypass, and MetricGAN+ hook on 2026-05-02. Modified: libs/audio/enhancement.py (added `import abc`; added `BYPASS_ENHANCER_VERSION = "bypass"` and `METRICGAN_PLUS_ENHANCER_VERSION = "metricgan_plus_pretrained"` constants; added `EnhancerAdapter(abc.ABC)` abstract base class with `enhancer_version: str` abstract property and `enhance(audio_path, output_dir, job_id) -> EnhancementResult` abstract method; added `BypassEnhancer(EnhancerAdapter)` — honest no-op, delegates to `apply_preset(BYPASS_PRESET_ID, audio_path, output_dir)`, enhancer_version="bypass"; added `MetricGANPlusEnhancer(EnhancerAdapter)` — empty hook, enhance() raises NotImplementedError with T4.1 ownership message, enhancer_version="metricgan_plus_pretrained"; PRESET_REGISTRY unchanged — still 4 entries; DEFAULT_ENHANCER_VERSION in libs/common/versions.py unchanged; cache-key/default-version alignment deferred to worker-integration task). Modified: libs/audio/__init__.py (added EnhancerAdapter, BypassEnhancer, MetricGANPlusEnhancer, BYPASS_ENHANCER_VERSION, METRICGAN_PLUS_ENHANCER_VERSION to imports and __all__). Created: tests/audio/test_audio_enhancer_interface.py (16 pure unit tests: EnhancerAdapter instantiation raises TypeError, BypassEnhancer/MetricGANPlusEnhancer issubclass checks, enhancer_version constants and property values, BypassEnhancer.enhance() returns EnhancementResult with enhanced=False/fallback=False/output_path=input_path, MetricGANPlusEnhancer.enhance() raises NotImplementedError, both share callable enhance method, package-level exports from libs.audio validated). Scope: demo-worker unchanged (B3.2 placeholder); no API route changes; no new PyPI deps; pyproject.toml and both lock files unchanged; libs/common/versions.py unchanged; all ASR providers unchanged. Validation: 16/16 new tests pass; 164/164 audio tests pass including test_registry_has_exactly_four_entries; 625 total tests pass / 0 failed (pre-existing platform/infra tests in observability+db+storage+smoke are unrelated to B5.3 and require platform infrastructure not present in demo container); protected diff empty (demo_worker_main.py, demo_main.py, main.py, cache.py, persistence.py, versions.py, libs/asr/, pyproject.toml, both lock files, Dockerfile.demo, docker-compose.demo.yml all untouched); docker compose config exit 0. All B5.3 done-when criteria satisfied.

- B5.2: adapted AssemblyAI provider for demo mode on 2026-05-02. New files: libs/asr/demo_assemblyai_provider.py (DemoAssemblyAIAdapter subclasses AssemblyAIAdapter; shorter demo defaults: max_wait_seconds=120.0, upload_timeout_seconds=30.0; quota_exhausted: bool=False constructor arg — raises AdapterTranscriptionError(_QUOTA_MESSAGE) before calling super() when True; transcribe() overrides with retry-once policy: catches AdapterHTTPError with status_code>=500 or AdapterTimeoutError, sleeps 2s via self._sleep, retries once — on second failure raises AdapterTranscriptionError(_SAFE_ERROR_MESSAGE); HTTP 4xx re-raises without retry; InputFileNotFoundError re-raises without retry; plain AdapterTranscriptionError from provider errors / poll budget propagates unchanged; get_status(api_key) staticmethod returns STATUS_DISABLED for None/"", STATUS_AVAILABLE otherwise), tests/asr/test_demo_assemblyai_provider.py (14 mock-based tests: subclass checks, status disabled/available, success no retry, 5xx retry, timeout retry, retry sleep=2s, 4xx no retry, missing file no retry, double 5xx safe message, quota guard blocks super(), no silent fallback). Modified: libs/asr/errors.py (added AdapterHTTPError(AdapterTranscriptionError) with status_code: int attribute; added AdapterTimeoutError(AdapterTranscriptionError)), libs/asr/assemblyai_provider.py (added AdapterHTTPError and AdapterTimeoutError to imports; in _upload/_submit/_poll: added except httpx.TimeoutException → AdapterTimeoutError before except httpx.HTTPStatusError → AdapterHTTPError with status_code; message strings preserved unchanged; all 31 original tests pass), libs/asr/__init__.py (added DemoAssemblyAIAdapter import and __all__ entry; added AdapterHTTPError, AdapterTimeoutError exports), tests/asr/test_assemblyai_provider.py (added 3 new typed-error tests at end: upload 4xx → AdapterHTTPError.status_code==401, submit 5xx → AdapterHTTPError.status_code==503, upload timeout → AdapterTimeoutError; all 31 original tests unchanged and passing), tests/asr/test_asr_package_imports.py (added DemoAssemblyAIAdapter, AdapterHTTPError, AdapterTimeoutError imports; extended test_error_hierarchy with subclass assertions; added test_demo_assemblyai_adapter_is_asr_adapter). Scope: demo worker unchanged (B3.2 placeholder); no API route changes; no usage_ledger or cost tracking; no live AssemblyAI calls; no new dependencies; pyproject.toml and both lock files unchanged; .env.demo.example unchanged. Validation: 14/14 new demo adapter tests pass; 34/34 assemblyai tests pass (31 original + 3 new); 360 passed / 0 failed in tests/asr+demo+audio+common; protected diff empty (main.py, tasks.py, demo_main.py, demo_worker_main.py, pyproject.toml, both lock files untouched); docker compose config exit 0. All B5.2 done-when criteria satisfied.

- B5.1: implemented Whisper local ASR provider on 2026-05-02. New files: libs/asr/whisper_provider.py (WhisperAdapter implementing ASRAdapter; MODEL_NAME = "tiny.en"; conditional import try/except with _FASTER_WHISPER_AVAILABLE flag so module imports cleanly without faster-whisper; _model injection point for mocked tests; is_file() guard rejects missing paths and directories; lazy model load on first transcribe() via _get_model(); model cache controlled via model_cache_dir param or WHISPER_MODEL_CACHE env var using faster-whisper download_root kwarg; language_warning added to raw_payload when info.language != "en" and language_probability > 0.5 using exact plan-specified message; normalizes to ASRResult with provider="whisper", provider_job_id=None, words=[]), tests/asr/test_whisper_provider.py (14 mock-based tests: subclass check, MODEL_NAME constant, missing file, directory input, English transcription, schema shape, segment normalization, empty segments, raw_payload model key, non-English warning above threshold, non-English below threshold, English above threshold, unavailability guard, live import check with skipif). Modified: tests/asr/test_asr_package_imports.py (added WhisperAdapter import and test_whisper_provider_is_asr_adapter; reordered imports alphabetically), pyproject.toml (added [project.optional-dependencies] demo = ["faster-whisper>=1.0"]), requirements.lock.arm64 (regenerated with --extra=dev --extra=demo; adds faster-whisper, ctranslate2, onnxruntime, huggingface-hub, tokenizers, tqdm, av and transitive deps; no changes to requirements.lock.x86_64), .env.demo.example (added WHISPER_MODEL_CACHE= placeholder). demo-worker unchanged (B3.2 placeholder still fails all jobs with "processing not yet implemented (B5)"). Validation: 14/14 new tests pass; full suite tests/demo+tests/asr all pass (no regressions); git diff requirements.lock.x86_64 empty; docker compose config exit 0; docker compose build exit 0 (faster-whisper visible in build output); GET /demo/health → {"status":"ok","mode":"demo","db_ok":true,"queue_depth":0}; smoke transcription inside demo container (1-second 440 Hz sine WAV, model downloaded to /home/gbibbo/asr_enhancement_runtime/cache/whisper) → smoke PASSED, text='', language='en', duration_seconds=1.0, segments=0; Dockerfile.demo unchanged (no missing shared library error); protected diff empty (main.py, tasks.py, db.py, models.py, docker-compose.yml, demo_main.py, demo_worker_main.py all untouched). All B5.1 done-when criteria satisfied.

- B4.3: implemented job submission endpoints on 2026-05-02. New files: libs/demo/upload.py (DEMO_ALLOWED_EXTENSIONS frozenset {.wav,.mp3,.m4a,.flac}; UnsupportedExtensionError; FileTooLargeError; async save_upload() with temp-then-rename pattern: writes to uuid.hex+".tmp", renames to uuid.hex+ext on success, unlinks .tmp on any exception; upload_dir.mkdir(parents=True, exist_ok=True) defensive; 65 536 B chunk streaming with byte limit enforcement), tests/demo/test_demo_upload_helper.py (9 unit tests using asyncio.run() + MagicMock UploadFile: wav/mp3 accepted, .txt rejected, no-extension rejected, size limit enforced, original filename absent from saved path, no .tmp after success, no partial file after FileTooLargeError), tests/demo/test_demo_jobs_api.py (18 API integration tests across 3 fixtures: GET /demo/jobs/{id} 404/200, GET /demo/jobs/{id}/result 404/202/200-completed/200-failed, POST /demo/run-cached 404-unknown-example/404-unknown-degradation/200-cache-miss/200-cache-miss-no-job-created/200-cache-hit, POST /demo/upload 202-queued/202-input-artifact-path/413/415/503-no-orphan, GET /admin/stats 401-no-credentials/401-no-password). Modified: libs/demo/persistence.py (extended try_create_job with input_artifact_path: Optional[str] = None and extended INSERT; added get_cache_entry, get_jobs_stats, set_admin_state, get_admin_state_value), services/api/app/demo_main.py (lifespan records startup_time via set_admin_state; added _RunCachedRequest Pydantic model; _ASR_MODEL_DEFAULTS dict; _basic = HTTPBasic(auto_error=False); 5 new routes: GET /demo/examples/{id}/audio/clean already existed from B4.2; POST /demo/run-cached, POST /demo/upload with file cleanup on QueueFullError/unexpected error, GET /demo/jobs/{id}, GET /demo/jobs/{id}/result, GET /admin/stats with uniform 401 for all auth failures), tests/demo/test_persistence.py (6 new tests: input_artifact_path stored, get_cache_entry none/dict, get_jobs_stats all zeros, set+get admin_state, admin_state missing returns None). Validation: 117 tests passed / 0 failed (84 pre-existing + 33 new); both Compose configs exit 0; build exit 0 (no new pip packages); GET /demo/health → {"status":"ok","mode":"demo","db_ok":true,"queue_depth":0}; POST /demo/run-cached with no examples config → 404; GET /admin/stats without auth → 401 {"detail":"Unauthorized"} with WWW-Authenticate: Basic; POST /demo/upload minimal WAV → 202 {"job_id":...,"status":"queued"}; GET /demo/jobs/{id} → job row with input_artifact_path set; GET /demo/jobs/{id}/result → {"status":"failed","error":"processing not yet implemented (B5)"} (worker B3.2 placeholder as expected); platform isolation diff empty (main.py, tasks.py, db.py, models.py, docker-compose.yml, pyproject.toml, both lock files untouched); docker compose down clean. All B4.3 done-when criteria satisfied.

- B4.2: implemented demo audio endpoints on 2026-05-02. Modified: libs/demo/examples.py (added get_safe_audio_path(audio_root, path_str) helper — rejects None, empty string, absolute paths, and traversal attempts via Path.is_relative_to(); returns resolved Path if file exists, None otherwise), services/api/app/demo_main.py (added GET /demo/examples/{example_id}/audio/clean and GET /demo/examples/{example_id}/audio/degraded/{degradation_id}; both use FileResponse with media_type="audio/wav"; audio root derived as settings.demo_artifacts_dir / "examples"; 404 for unknown example, missing clean_audio_path, unknown degradation_id, absolute path in config, traversal attempt, missing file). Created: tests/demo/test_demo_audio.py (21 new tests: 7 unit tests for get_safe_audio_path covering None, empty, absolute, traversal, missing, valid, subdir; 7 API tests for clean audio endpoint; 7 API tests for degraded audio endpoint). Validation: 84 tests passed / 0 failed (63 previous + 21 new); demo Compose config exit 0; platform Compose config exit 0; build exit 0 (no new pip packages, requirements.lock.arm64 unchanged); GET /demo/health → {"status":"ok","mode":"demo","db_ok":true,"queue_depth":0}; GET /demo/examples → explicit empty response; GET /demo/examples/nonexistent/audio/clean → 404; protected diff empty (platform files untouched); docker compose down clean. All B4.2 done-when criteria satisfied.

- B4.1: implemented demo health and example listing on 2026-05-02. New files: libs/demo/examples.py (DemoExample Pydantic model with example_id, title, description, duration_seconds, degradation_ids, ground_truth, audio_available, clean_audio_path, degraded_audio_paths; load_examples() returns [] when config absent), tests/demo/test_demo_examples.py (8 new tests covering absent file, empty array, valid JSON, all default field values, platform isolation check). Modified: libs/common/demo_settings.py (added demo_examples_config: Path = Path("config/demo_examples.json")), services/api/app/demo_main.py (GET /demo/health expanded to report db_ok and queue_depth by calling count_active_jobs inside try/except; GET /demo/examples new route reading settings.demo_examples_config via load_examples()), tests/demo/test_demo_api.py (added DEMO_EXAMPLES_CONFIG to env isolation list, updated test_health_returns_ok to check key fields, added test_health_db_ok_is_true, test_health_queue_depth_is_integer, test_examples_returns_200, test_examples_returns_empty_list_when_no_config, test_examples_note_is_set_when_empty, test_examples_note_is_null_when_populated), tests/demo/test_demo_settings.py (added DEMO_EXAMPLES_CONFIG to env isolation list, added test_default_examples_config). Corrected stale tracker entry: B3.3 does not exist in the plan; phase B3 ended at B3.2; tracker corrected to current_task B4.2. Validation: 63 tests passed / 0 failed (48 previous + 15 new); demo Compose config exit 0; platform Compose config exit 0; build exit 0 (no new pip packages, requirements.lock.arm64 unchanged); GET /demo/health → {"status":"ok","mode":"demo","db_ok":true,"queue_depth":0}; GET /demo/examples → {"examples":[],"total":0,"note":"No curated examples loaded. Run Phase B6 to populate."}; protected diff empty (platform files untouched); docker compose down clean. All B4.1 done-when criteria satisfied.

- B3.1: created demo Compose runtime on 2026-05-02. New files: infra/compose/Dockerfile.demo (python:3.11-slim, installs from requirements.lock.arm64), infra/compose/docker-compose.demo.yml (Compose project name asr-demo, two services: demo-api on host port 8001, demo-worker; no platform services; Python stdlib urllib.request healthcheck; bind-mount /home/gbibbo/asr_enhancement_runtime), .env.demo.example (placeholder only, no secrets), libs/common/demo_settings.py (DemoSettings with model_validator filling derived paths from DEMO_RUNTIME_ROOT), services/api/app/demo_main.py (minimal FastAPI, no platform imports, GET /demo/health stub), services/worker/app/demo_worker_main.py (SIGTERM+SIGINT handlers, clean shutdown). Added .env.demo to .gitignore. Validation: docker compose config exit 0; docker compose build exit 0 (arm64 native, all packages from requirements.lock.arm64); docker compose up -d: demo-api healthy, demo-worker Up; curl http://localhost:8001/demo/health → {"status":"ok","mode":"demo"}; worker log: "Demo worker started (idle, waiting for B3.2 job queue)"; platform config: exit 0; tests/demo/ inside demo image: 19 passed / 0 failed (8 cache key + 11 new demo settings); docker compose down: clean exit. Seven protected diffs all empty. All B3.1 done-when criteria satisfied.



- B2.5: generated requirements.lock.arm64 on RP5 on 2026-05-02. Docker container: python:3.11-slim linux/arm64 (native aarch64, no emulation). pip-tools: 7.5.3. Generation command: `docker run --rm --platform linux/arm64 -v "$(pwd)":/workspace -w /workspace python:3.11-slim sh -c "pip install --quiet pip-tools==7.5.3 && pip-compile --extra=dev --generate-hashes --output-file=requirements.lock.arm64 --strip-extras pyproject.toml"`. Container architecture check: platform.machine()=aarch64, Python 3.11.15. Sanity checks: no file:// paths, no secrets, fastapi==0.136.1 / celery==5.6.3 / pytest==9.0.3 pinned with hashes (1364 hash lines). Install from lock in bare arm64 container: install OK. Pure-Python test suites: tests/audio/ + tests/common/ + tests/demo/ + tests/asr/ = 218 passed / 0 failed in 3.05 s. Protected diff checks: requirements.lock.x86_64, pyproject.toml, libs/, services/, tests/, infra/, .github/ all empty (no unintended changes). All B2.5 done-when criteria satisfied.
- B2.2: Git and VS Code Remote SSH verified on 2026-05-02. SSH to asr-rp5: ok. Repo ~/code/asr_enhancement fetched and fast-forwarded from 99495ad to f3400cf (19 commits). Branch: feature/demo-runtime-rp5-v1. Remote: git@github.com:gbibbo/asr_enhancement.git. GitHub SSH auth: ok (Hi gbibbo! authenticated via id_ed25519_github_rp5). Working tree: clean. Git identity: Gabriel Bibbó <gabobibbo@gmail.com>. merge.ours.driver: set to true. VS Code Remote SSH: Gabriel confirmed repo opens in VS Code remote window with branch feature/demo-runtime-rp5-v1 and tip f3400cf. All B2.2 done-when criteria satisfied.
- B2.1: base system dependencies verified on 2026-05-02. apt-get update: exit 0, fetched 707 kB in 3 s from deb.debian.org (trixie, trixie-updates, trixie-security) and archive.raspberrypi.com (trixie). Git: 2.47.3. Docker: 26.1.5+dfsg1 build a72d7cd. Docker Compose: 2.26.1-4. gbibbo already in docker group (gid 105) — no usermod required. docker ps without sudo: exit 0. docker run --rm alpine echo docker-ok: pulled alpine:latest from Docker Hub, printed docker-ok. All B2.1 done-when criteria satisfied.
- B1.1: OS headless verified on 2026-05-02. RP5 was already installed and running before B0.x checks. SSH confirmed as gbibbo@asr-rp5. OS: Debian GNU/Linux 13 (trixie) with Raspberry Pi kernel 6.12.75+rpt-rpi-2712 (BCM2712 / RP5). Architecture: aarch64. Hostname: asr-rp5. User: gbibbo. SSH service: active. Headless confirmed (lightdm inactive, DISPLAY not set). Timezone: America/Montevideo. Locale: en_GB.UTF-8. Disk: mmcblk0p2 ext4 rootfs 50.2 GB, 9% used, mounted at /. eth0 UP 192.168.1.241/24 is runtime path. wlan0 UP 192.168.1.240/24 is active but not runtime path per B1.1 decision rule 3 (keep Ethernet as runtime path). All B1.1 done-when criteria satisfied.
- S0.1: inspected repository state and preserved MVP tag.
- S0.2: created integration and demo feature branches.
- S0.3: added split plan files and root plan router.
- S0.4: created demo/platform trackers.
- S0.5: confirmed all four split branches on origin (master, demo-rp5-v1, feature/demo-runtime-rp5-v1, feature/training-datamove1-v1) and verified RP5 host, Docker 26.1.5, and Compose 2.26.1 on asr-rp5.
- R0.1: verified platform mode locally on 2026-05-01 from feature/demo-runtime-rp5-v1 (tip 7f0d98a). Built and brought up infra/compose/docker-compose.yml; all 8 services running, postgres/redis/minio healthy, api and worker started cleanly. Pytest inside the api container: general suite (tests excluding tests/smoke and tests/integration) 377 passed / 0 failed / 0 skipped; smoke suite 1 passed. Host-side checks: GET /health 200, GET /metrics 200, POST /v1/transcribe round-trip returned the deterministic fake transcript. Classification P (all green). Stack torn down with docker compose down (volumes preserved).
- R0.2: added migration status note to README.md ## Status section on 2026-05-01. Note references platform-mvp-v0 tag (verified present) and demo-rp5-v1 branch (verified present). No live demo URL added.
- R1.1: created libs/asr/ package (7 files: __init__.py, schema.py, errors.py, base.py, fake_provider.py, assemblyai_provider.py, factory.py) and tests/asr/test_asr_package_imports.py on 2026-05-01. Import smoke inside api container: OK. tests/asr/ 5 passed. tests/asr_adapter/ 51 passed (old package untouched). Full non-smoke/non-integration suite 382 passed / 0 failed. Safety diffs confirmed libs/asr_adapter/, tasks.py, main.py, and pyproject.toml unchanged. Stack torn down with docker compose down (volumes preserved).
- R1.2: updated imports and tests for ASR package on 2026-05-01. services/worker/app/tasks.py line 15 updated from libs.asr_adapter.factory to libs.asr.factory. 5 worker test files updated to libs.asr.errors / libs.asr.schema. tests/integration/test_live_assemblyai.py updated to libs.asr.assemblyai_provider / libs.asr.schema (gate still skips in CI). 3 new behavioral test files created: tests/asr/test_fake_provider.py (17 tests), tests/asr/test_assemblyai_provider.py (31 tests), tests/asr/test_asr_factory.py (3 tests). Safety diffs: libs/asr_adapter/, services/api/, pyproject.toml, infra/, .github/workflows/ all empty (no unintended changes). tests/asr/ 56 passed. tests/asr_adapter/ 51 passed (old package untouched). tests/worker/ 52 passed. Integration gate: 1 skipped / 0 failed. Full non-smoke/non-integration suite 433 passed / 0 failed (382 baseline + 51 new). Smoke suite 1 passed. Endpoint round-trip: /v1/transcribe returned deterministic fake transcript. Final import search: no libs.asr_adapter outside libs/asr_adapter/ and tests/asr_adapter/. Stack torn down with docker compose down (volumes preserved).
- R1.3: removed legacy libs/asr_adapter/ (7 files) and tests/asr_adapter/ (4 files) on 2026-05-01. Pre-deletion and post-deletion host-side Select-String confirmed zero active imports of libs.asr_adapter in services/, tests/, libs/. Removed 3 completed ASR mapping rows from CLAUDE.md §9 and docs/profiles/CLAUDE.demo.md §9; audio pipeline rows (libs/audio_pipeline/) retained for R2.x. pytest inside api container: tests/asr/ 56 passed, tests/worker/ 52 passed, full suite 382 passed / 0 failed (433 − 51 deleted), smoke 1 passed. GET /health: {"status": "ok"}. /v1/transcribe round-trip: deterministic fake transcript confirmed. Stack torn down with docker compose down (volumes preserved).
- B0.2: account and connectivity checklist completed on 2026-05-02. AssemblyAI latency from RP5: 0.707 s / 0.715 s / 0.710 s — classification: ok (all < 5 s, AssemblyAI viable in v1). AssemblyAI account: ok (logged in, API key available). Credits: $49.97 remaining on free plan. Autopay: off. Cloudflare account: ok (logged in, account active). Domain: trycloudflare.com for development (plan default). RP5 → api.assemblyai.com: reachable (HTTP 401 × 3). No blockers. All B-phase tasks may continue.
- B0.1: hardware and LAN checklist completed on 2026-05-02. Windows-to-RP5 LAN reachability confirmed (ping and SSH TCP/22, IPv6 2800:a4:951:4f00:da3a:ddff:fea5:199e, round-trip 7–17 ms). SSH confirmed as gbibbo@asr-rp5. SD card: mmcblk0 59.5 GB; root partition 58 GB (5.4 GB used / 51 GB free, 10%). eth0: UP, IPv4 192.168.1.241/24, IPv6 2800:a4:951:4f00:da3a:ddff:fea5:199e/64. CPU temperature: 40.6°C at idle (vcgencmd confirms). Docker 26.1.5+dfsg1, Docker Compose 2.26.1-4, Git 2.47.3. Internet from RP5: HTTP/2 200 (github.com). Repo ~/code/asr_enhancement exists on RP5. Manual hardware: USB-C power supply 65 W (adequate, >27 W requirement); cooling: official Raspberry Pi 5 Active Cooler (fan + heatsink); Ethernet cable physically connected to router/switch (eth0 primary, Wi-Fi also active); UPS: none currently, planned for future (not required by plan, accepted). No blockers.
- R4.1: created libs/audio/degradations.py on 2026-05-02 with DegradationSpec, DegradationResult, DEGRADATION_REGISTRY, KNOWN_DEGRADATION_IDS, UnknownDegradationError, get_degradation, and apply_degradation. Five deterministic DSP degradations: far_field_room (decaying exponential reverb + gain reduction), cafe_background (band-limited Gaussian noise at SNR 10 dB), phone_call (bandpass Butterworth 300–3400 Hz), muffled (lowpass Butterworth 1000 Hz), broadband_hiss (white noise at SNR 30 dB). All noise functions use np.random.default_rng(seed); DEGRADATION_VERSION read via import libs.common.versions as versions at call time; monkeypatch test confirms no hardcoding. Updated libs/audio/__init__.py to re-export all seven new symbols. Created tests/audio/test_audio_degradations_imports.py (11 tests) and tests/audio/test_audio_degradations.py (40 tests, including parametrized suite over DEGRADATION_IDS ordered list and np.allclose determinism checks). Safety diffs: pyproject.toml, requirements.lock.x86_64, services/api/app/main.py, services/worker/app/tasks.py, libs/demo/cache.py, libs/common/versions.py, infra/, .github/workflows/ all empty. tests/audio/ 148 passed / 0 failed. Full non-smoke/non-integration suite 487 passed / 0 failed (436 baseline + 51 new). Smoke 1 passed. Stack torn down with docker compose down (volumes preserved).
- R3.1: created libs/audio/metrics.py on 2026-05-02 with MetricsResult (frozen dataclass), normalize_text, compute_metrics, and _word_edit_distance. Pure-Python token-level Levenshtein WER — no third-party metric library added, pyproject.toml and requirements.lock.x86_64 unchanged. METRICS_VERSION read via `import libs.common.versions as versions` inside compute_metrics() at call time; monkeypatch test confirms no hardcoding. Updated libs/audio/__init__.py to re-export MetricsResult, compute_metrics, normalize_text. Created tests/audio/test_audio_metrics_imports.py (9 tests including monkeypatch) and tests/audio/test_audio_metrics.py (25 tests covering normalization, WER, Word Accuracy bounds, unavailable reference, frozen dataclass). Safety diffs: pyproject.toml, requirements.lock.x86_64, services/api/app/main.py, services/worker/app/tasks.py, infra/, .github/workflows/ all empty. tests/audio/ 97 passed / 0 failed. Full non-smoke/non-integration suite 436 passed / 0 failed (402 baseline + 34 new). Smoke 1 passed. Stack torn down with docker compose down (volumes preserved).
- R2b.2: attempted arm64 dependency lock on 2026-05-02. Docker buildx on Windows host does not support linux/arm64 — both builders (default, desktop-linux) report linux/amd64 only; `docker run --platform linux/arm64 python:3.11-slim` fails with `exec format error` (QEMU binfmt not configured). Deferral to B2.5 is the plan-defined path. Task B2.5 ("Generate arm64 lock on RP5 if missing") is explicitly required and will execute during Phase B2 RP5 setup. arm64 lock generation command recorded for B2.5: `docker run --rm --platform linux/arm64 -v $(pwd):/workspace -w /workspace python:3.11-slim sh -c "pip install --quiet pip-tools==7.5.3 && pip-compile --extra=dev --generate-hashes --output-file=requirements.lock.arm64 --strip-extras pyproject.toml"`. Install verification command for B2.5: `docker run --rm --platform linux/arm64 -v $(pwd):/workspace -w /workspace python:3.11-slim sh -c "pip install --quiet -r requirements.lock.arm64 && python -c 'import fastapi, celery, pytest; print(\"OK\")'". No requirements.lock.arm64 committed at this stage.
- R2b.1: generated requirements.lock.x86_64 at repo root on 2026-05-02 using pip-compile 7.5.3 inside a linux/amd64 python:3.11-slim container. Lock includes main + dev dependencies with --generate-hashes and --strip-extras. Architecture check confirmed x86_64. Sanity checks: no file:// paths, no secrets, fastapi==0.136.1 / celery==5.6.3 / pytest==9.0.3 pinned with hashes. Install from lock in bare container: OK. Import check (fastapi, celery, pytest, libs.demo.cache, libs.common.versions, libs.audio.enhancement, libs.asr.factory): OK. Pure-Python test suites: tests/audio/ 63 passed, tests/common/ 6 passed, tests/demo/ 8 passed, tests/asr/ 56 passed = 133 passed / 0 failed.
- R2.5.2: created libs/demo/__init__.py, libs/demo/cache.py, tests/demo/__init__.py, tests/demo/test_cache_key.py on 2026-05-01. build_cache_key() uses import libs.common.versions as versions and reads versions.DEGRADATION_VERSION, versions.METRICS_VERSION, and versions.DEFAULT_ENHANCER_VERSION at call time. enhancer_version=None defaults to versions.DEFAULT_ENHANCER_VERSION. Output is pipe-delimited: example_id|degradation_id|DEGRADATION_VERSION|asr_provider|asr_model_version|enhancer_version|METRICS_VERSION. Host import smoke: OK. tests/demo/ 8 passed (including 2 monkeypatch tests that fail if versions are hardcoded). tests/common/ 6 passed (unchanged). Full non-smoke/non-integration suite 402 passed / 0 failed (394 baseline + 8 new). Smoke 1 passed. Stack torn down with docker compose down (volumes preserved).
- R2.5.1: created libs/common/versions.py on 2026-05-01. 3 new constants: DEGRADATION_VERSION="1.0", METRICS_VERSION="1.0", DEFAULT_ENHANCER_VERSION="1.0". Created tests/common/__init__.py and tests/common/test_versions_imports.py (6 tests). Import smoke inside api container: all three constants OK. Duplicate constant search confirmed no definitions outside libs/common/versions.py. tests/common/ 6 passed. Full non-smoke/non-integration suite 394 passed / 0 failed (388 baseline + 6 new). Smoke 1 passed. Stack torn down with docker compose down (volumes preserved).
- R2.3: removed legacy libs/audio_pipeline/ (4 files) and tests/audio_pipeline/ (3 files) on 2026-05-01. Pre-deletion and post-deletion Select-String confirmed zero active imports of libs.audio_pipeline in services/, tests/, libs/. Removed completed audio_pipeline mapping block from CLAUDE.md §9 and docs/profiles/CLAUDE.demo.md §9. pytest inside api container: tests/audio/ 63 passed, tests/worker/ 52 passed, full suite 388 passed / 0 failed (445 − 57 deleted), smoke 1 passed. GET /health: {"status": "ok"}. /v1/transcribe round-trip: deterministic fake transcript confirmed. /v1/enhance-and-transcribe round-trip (preset=bypass): deterministic fake transcript confirmed. /v1/enhance-and-transcribe (preset=bad_preset): 400 unknown_preset confirmed. Stack torn down with docker compose down (volumes preserved).
- R2.2: updated imports and tests for audio package on 2026-05-01. services/api/app/main.py lines 20-21 updated from libs.audio_pipeline.errors/presets to libs.audio.enhancement (combined import). services/worker/app/tasks.py line 16 updated from libs.audio_pipeline.pipeline to libs.audio.enhancement. tests/worker/test_enhance_task.py and test_worker_metrics.py updated to libs.audio.enhancement. 2 new behavioral test files created: tests/audio/test_audio_enhancement.py (35 tests), tests/audio/test_audio_presets.py (22 tests). Safety diff confirmed zero libs.audio_pipeline imports in services/ or tests/worker/. tests/audio/ 63 passed. tests/audio_pipeline/ 57 passed (unchanged). tests/worker/ 52 passed. Full non-smoke/non-integration suite 445 passed / 0 failed (388 baseline + 57 new). Smoke suite 1 passed. Endpoint checks: GET /health 200, POST /v1/transcribe 202, POST /v1/enhance-and-transcribe (preset=bypass) 202, POST /v1/enhance-and-transcribe (preset=bad_preset) 400 unknown_preset. Stack torn down with docker compose down (volumes preserved).
- R2.1: created libs/audio/ package and enhancement module on 2026-05-01. 4 new files: libs/audio/__init__.py, libs/audio/enhancement.py (self-contained: UnknownPresetError + preset registry + EnhancementResult + DSP pipeline, no import from libs.audio_pipeline), tests/audio/__init__.py, tests/audio/test_audio_enhancement_imports.py. Active imports in services/ and tests/worker/ unchanged on libs.audio_pipeline (copy-only phase). Import smoke inside api container: libs.audio.enhancement OK, libs.audio __init__ OK. tests/audio/ 6 passed. tests/audio_pipeline/ 57 passed (unchanged). tests/worker/ 52 passed. Full non-smoke/non-integration suite 388 passed / 0 failed (382 baseline + 6 new). Smoke suite 1 passed. Host-side /v1/transcribe round-trip: deterministic fake transcript confirmed. Host-side /v1/enhance-and-transcribe round-trip (preset=bypass): deterministic fake transcript confirmed. Safety diffs: libs/audio_pipeline, services/api/app/main.py, services/worker/app/tasks.py, pyproject.toml, infra/, .github/workflows all empty (no unintended changes). libs.audio_pipeline import baseline identical before and after (15 matches, same files and lines). Stack torn down with docker compose down (volumes preserved).

- B3.2: implemented demo persistence and queue limit on 2026-05-02. New files: libs/demo/persistence.py (stdlib sqlite3 only; no SQLAlchemy; QueueFullError; ensure_runtime_dirs creates 5 runtime dirs; init_schema runs CREATE TABLE IF NOT EXISTS for all 4 tables with exact plan-contract column names and TIMESTAMP type names; count_active_jobs for diagnostics; try_create_job with BEGIN IMMEDIATE for atomic queue-max enforcement; get_job; update_job_status; claim_next_job with BEGIN IMMEDIATE), tests/demo/test_persistence.py (21 new tests covering schema idempotency, create/get/update/claim, queue-full boundary, runtime dirs, isolation checks), tests/demo/test_demo_api.py (7 new API tests using TestClient with isolated tmp_path DB: health, 202 create, DB persistence, 503 queue full, completed/failed rows do not block, platform-settings isolation). Modified: services/api/app/demo_main.py (lifespan now calls ensure_runtime_dirs + init_schema, stores settings on app.state; added POST /demo/jobs stub with atomic queue check), services/worker/app/demo_worker_main.py (startup calls ensure_runtime_dirs + init_schema; polling loop claims jobs via claim_next_job and immediately marks them failed with error_message="processing not yet implemented (B5)" — deliberate B3.2 lifecycle placeholder, no ASR/enhancer/audio imports). Also fixed libs/common/demo_settings.py: fill_derived_paths now treats Path(".") (from empty-string env vars in Compose) as unset, using _unset() helper; added test_empty_string_db_path_falls_back_to_derived. Fixed infra/compose/docker-compose.demo.yml: changed demo-worker command from python -u script.py to python -m module.path so Python adds /app to sys.path. Validation: tests/demo/ 48 passed / 0 failed (8 cache key + 12 settings + 7 API + 21 persistence); demo-api healthy; demo-worker Up; GET /demo/health → {"status":"ok","mode":"demo"}; POST /demo/jobs → 202 with job_id; worker log shows lifecycle placeholder messages; queue-max=10 enforced: 10×202 then 503 on 11th; SQLite tables: admin_state, cache_entries, jobs, usage_ledger; protected diff empty (platform files untouched); docker compose down clean. All B3.2 done-when criteria satisfied.

## Current blocker

None.

## Training handoff status

Training branch feature/training-datamove1-v1 already exists on origin from demo-rp5-v1. Split workflow active.

## Next task

Task B9.1.
