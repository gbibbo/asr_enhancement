# Demo and Platform Task Progress

Branch: feature/demo-runtime-rp5-v1
Integration branch: demo-rp5-v1
Parallel training branch: feature/training-datamove1-v1
Current track: B (RP5 setup)
Current phase: Phase B12
Current task: Task B12.2

Note: current_task was corrected from stale B5.4 to B6.1 at the start of this task. No B5.4
exists in docs/plans/demo_platform_plan.md. Phase B5 ended at B5.3. After B6.5.2 the plan
advances to Phase B8 (Reproducible cache); there is no B6.6 and no B7.2.

## Completed

- B12.1: done on 2026-05-05. §B12.1 (`docs/plans/demo_platform_plan.md` lines 1985-2015) closed by adding JSON logs with rotation across `demo-api` and `demo-worker` and by enriching the Basic-auth-protected `/admin/stats` with all 14 plan-required operational fields. **Privacy contract enforced at the formatter layer**: a centralized hard-deny field set (`_DROP_FIELDS` in `libs/observability/logging.py`, single source of truth) drops `transcript|text|hypothesis|reference|reference_text|ground_truth|gt|raw_payload|provider_payload|session_id|session_id_hash|ledger_id|audio_path|input_path|degraded_path|enhanced_path|input_artifact_path|degraded_artifact_path|enhanced_artifact_path|result_path|filename|original_filename|upload_filename|cloudflared_url|tunnel_token` from JSON output BEFORE serialization, regardless of whether a caller passes them via `extra={...}` (defense in depth — production code does not pass these); the existing `_REDACT_PATTERNS` (`key|secret|token|authorization|auth_header`) continue to redact (not drop) generic credential identifiers; `JSONFormatter` and `error_buffer._ErrorBufferHandler` both go through the shared `sanitize_extra(extra)` helper so the two paths cannot drift. **`/admin/stats` shape delta**: existing keys (`startup_time`, `uptime_seconds`, `queue_depth`, `jobs_by_status`, `provider_state`) preserved byte-identical; new keys added inside the auth gate — `request_count_total` (in-process counter), `cache: {entries_total, hits_total, misses_total, hit_rate}`, `disk: {scope: "demo_runtime_root", total_bytes, used_bytes, free_bytes, used_percent}` (**no `path` key, no `/home/`, no per-subdir paths anywhere in the response — `disk.scope` is a fixed label**, the actual sampled path stays inside `read_disk_usage()`), `cpu_temperature: {state: available|unavailable, celsius?}` (reads `/sys/class/thermal/thermal_zone0/temp` and falls back to `unavailable` per decision rule 2), `last_errors: []` (demo-api process-local deque, maxlen=20 by default; demo-worker errors NOT aggregated — they live exclusively in `demo.worker.log`; B12.1 does NOT introduce a cross-process error store), `cloudflare_tunnel: {state: "unavailable"}` (default per decision rule 3 — B14.1 will populate later), `last_cleanup_at: ISO|null` (read from existing `admin_state.last_cleanup_at` written by B9.4). **File logging**: opt-in at the library level (`DemoSettings.demo_log_to_file=False`) so unit tests stay stderr-only, **opt-in at the demo Compose level** (`DEMO_LOG_TO_FILE: ${DEMO_LOG_TO_FILE:-true}` in `infra/compose/docker-compose.demo.yml`) so the RP5 runtime container rotates by default and B12.1 done-when 4 ("logs rotate") is satisfied without operator action; rotation is `RotatingFileHandler` with `maxBytes=10485760` × `backupCount=5` (env-overridable as `DEMO_LOG_MAX_BYTES` / `DEMO_LOG_BACKUP_COUNT`); filename pattern `${DEMO_LOG_FILENAME_PREFIX:-demo}.${short_service}.log` where `short_service` is the `configure_logging(service=...)` argument with the leading `demo-` stripped, giving exactly `demo.api.log` and `demo.worker.log` (resolves the rev-1 `demo_log_filename` vs `demo_log_filename_prefix` contradiction — the single setting is `demo_log_filename_prefix`, no separate `demo_log_filename` exists). Live runtime confirmed: `/home/gbibbo/asr_enhancement_runtime/logs/demo.api.log` (5193 bytes after smoke) and `demo.worker.log` (740 bytes) both written under the runtime root, outside the repo, and `*.log` is in `.gitignore` line 35 so no rotated artifact can stage. **`configure_logging` idempotency**: handlers are tagged with `_asr_managed = True`, `_asr_managed_kind ∈ {stream, file, buffer, extra}`, `_asr_managed_service` so a repeated `configure_logging("demo-api")` call (e.g. when a TestClient lifespan re-runs) drops only the previously installed managed handlers for that service and closes file handles cleanly without removing pytest's caplog handler or handlers from a different service; the legacy `_asr_json_logging` tag is still set on the stream handler for backward compatibility. **`demo-worker` parity**: switched from `logging.basicConfig(...)` (line 9 pre-B12.1) to `configure_logging("demo-worker")` plus the rotating file handler when `DEMO_LOG_TO_FILE=true`, so worker JSON logs land in `demo.worker.log` and pass through the same hard-deny redaction. **Demo-api request middleware**: a single `@app.middleware("http")` emits one `demo.api.request` log record per request with `method`, `path`, `status_code`, `duration_ms`; the `Authorization` and `X-Demo-Session-Id` headers are NEVER added to log fields (verified by `tests/demo/test_b12_1_request_middleware.py::test_request_middleware_omits_credential_headers`); `request_count_total` is pre-incremented before `call_next` so the request being served is included in the count the admin response sees. **Cache hit/miss counters** bumped from the existing `submitCached → /demo/run-cached` branches in `services/api/app/demo_main.py` (no schema, no DB read change); `cache.entries_total` reads `count_cache_entries(db_path)` (new helper added to `libs/demo/persistence.py`, single COUNT(*) query, no row materialization). **Public privacy invariants preserved**: `/demo/health` shape unchanged (`{db_ok, mode, queue_depth, status}`); `/demo/providers/assemblyai/status` shape unchanged (`{"assemblyai": {"state", "cap_state"}}` only — no spend, caps, key_configured, as_of, ledger, session); the public-shape regression test does NOT hardcode `state == "disabled"` so it passes regardless of whether `ASSEMBLYAI_API_KEY` is configured. Created: `libs/observability/log_rotation.py` (RotatingFileHandler factory with permission-failure fallback to stderr-only + warning), `libs/observability/error_buffer.py` (process-local `_ErrorBufferHandler` with module-local deque, sanitizer-filtered, UUID-only `job_id` whitelist), `libs/demo/probes.py` (`read_cpu_temp_celsius`, `read_disk_usage` with `scope` label only, `read_cloudflare_tunnel_state` returning `unavailable`), `libs/demo/admin_stats.py` (pure builder composing persistence + provider admin view + probes + counters). Modified: `services/api/app/demo_main.py` (request middleware, hit/miss counters, `build_admin_stats` call, lifespan wiring of `error_buffer` + `file_handler` via `configure_logging(extra_handlers=...)`); `services/worker/app/demo_worker_main.py` (`configure_logging("demo-worker")` + rotating file handler in `main()`); `libs/observability/logging.py` (`_DROP_FIELDS`, `sanitize_extra`, idempotent `configure_logging` with `extra_handlers`); `libs/common/demo_settings.py` (5 new logging/admin settings with the `model_validator` defaulting `demo_admin_disk_usage_path` to `demo_runtime_root`); `libs/demo/persistence.py` (one new `count_cache_entries` helper, no DDL change); `infra/compose/docker-compose.demo.yml` (5 new env vars in `x-demo-env` block, `DEMO_LOG_TO_FILE` defaulted to `true`); `.env.demo.example` (5 new operator-overridable keys with safe defaults). Created tests: `tests/demo/test_b12_1_admin_stats_fields.py` (15 tests — every plan-required field, disk has no `path` key and no `/home/` substring, public provider-state membership-only assertion, B10.1 spend invariants intact, full-string scan for transcript/GT/session_id_hash/ledger_id/raw_payload/audio_path/filename/original_filename leakage, existing 401 cases preserved); `tests/demo/test_b12_1_request_middleware.py` (5 tests — event emission, method/status/duration, NO authorization/auth_header/x-demo-session-id fields on any record, counter increments); `tests/demo/test_b12_1_logging_redaction.py` (6 tests — `_DROP_FIELDS` covers every sentinel, `sanitize_extra` drops vs redacts correctly, `JSONFormatter` drops deny-listed extras and redacts generic secrets, idempotency: 3× `configure_logging` calls produce 1 emit per `log.info` and 1 stream handler tagged for the service, stdlib `LogRecord.filename` collision handled via direct setattr); `tests/demo/test_b12_1_log_rotation.py` (4 tests — `short_service` strip, disabled-when-flag-off, writes-and-rotates with `maxBytes=512` × `backupCount=2`, filename matches `demo.api.log` / `demo.worker.log`); `tests/demo/test_b12_1_error_buffer.py` (6 tests — module-local deque, ERROR-only routing, hard-deny sentinel drop, non-UUID `job_id` rejection, maxlen, no exceptions on bad records); `tests/demo/test_b12_1_worker_logging.py` (4 tests — static guards on `services/worker/app/demo_worker_main.py` source: imports `configure_logging`, calls with `"demo-worker"`, NO `logging.basicConfig`, attaches rotating handler). Validation: compose-first `python -m compileall` over all 9 touched modules → exit 0; targeted pytest of all 6 new test files → **42 passed**; adjacent regression (`tests/demo/test_demo_jobs_api.py tests/demo/test_persistence.py tests/demo/test_b10_1_usage_ledger.py tests/demo/test_b10_2_upload_assemblyai_rules.py tests/demo/test_b9_2_upload_processing.py tests/demo/test_b9_4_cleanup_uploads.py`) → **188 passed + 1 skipped**; broad regression (`pytest -q tests/demo tests/asr tests/audio tests/common`) → **712 passed + 2 skipped** (rev-1 baseline 670+2 plus the +42 new B12.1 tests, exactly accounted for); existing `tests/demo/test_demo_jobs_api.py:479-488` 401 cases pass byte-identical. **One implementation regression caught and fixed during validation**: my initial pruning of unused imports in `services/api/app/demo_main.py` accidentally removed `count_active_jobs` from the imports while it was still referenced from the `/demo/health` handler — caught when `test_demo_api.py::test_health_returns_ok` and `test_health_db_ok_is_true` reported `db_ok=False` after the broad-regression run; restored the import, re-built the demo containers, re-ran the broad regression to **712/2** clean, and verified the runtime container's `/demo/health` returns `{"status":"ok","db_ok":true}` again before the final live smoke. Live smoke after rebuild: `curl /demo/health` → `{"status":"ok","mode":"demo","db_ok":true,"queue_depth":0}` with sorted keys `[db_ok, mode, queue_depth, status]`; unauthenticated `curl /admin/stats` → HTTP 401 (HARD GATE); authenticated smoke deliberately NOT run (no out-of-band credential channel — the unit TestClient test in `tests/demo/test_b12_1_admin_stats_fields.py` is authoritative; the live shell does not read or print `$ADMIN_STATS_PASSWORD`); `curl /demo/providers/assemblyai/status` → `{"assemblyai":{"state":"disabled","cap_state":"below"}}` and shape validator confirmed top-level keys exactly `["assemblyai"]`, `assemblyai` keys exactly `["cap_state","state"]`, `state` and `cap_state` in their plan-defined membership sets, and absence of `daily_usd|total_usd|reserved_usd|key_configured|as_of|daily_soft_cap_usd|warning_cap_usd|hard_cap_usd|usd_per_second_estimate`; `ls $DEMO_LOGS_DIR` inside the demo-api and demo-worker containers shows `demo.api.log` (5193 bytes) and `demo.worker.log` (740 bytes), both outside the repo. **No live AssemblyAI call** anywhere. **No Cloudflare touched**: tunnel state field reads `{"state": "unavailable"}` per decision rule 3 — the read-side contract is wired but no cloudflared install or config exists in B12.1; B14.1 owns that. **No frontend change**: `services/frontend/` byte-untouched; no `npm` step ran; the frontend lockfile diff is empty. **No schema migration**: `usage_ledger`, `jobs`, `cache_entries`, `admin_state` DDL is identical to B11.5; `count_cache_entries` is a read-only helper. **No non-stdlib dependency**: rotation uses `logging.handlers.RotatingFileHandler`, disk uses `shutil.disk_usage`, ring buffer uses `collections.deque`, all stdlib; `pyproject.toml` and `requirements.lock.*` byte-untouched. Privacy / runtime / secret guards before commit: protected-paths probe (`.env.demo.example` correctly carved out by anchoring `^(\.env|\.env\.demo|\.env\.local)$`) → empty; runtime-artifact probe → empty; log-file probe → empty; runtime-tree probe → empty; assignment-shape secret scan over additions (`ASSEMBLYAI_API_KEY=…|sk_live_…|sk-…|aai_…|password=…`) → empty; cloudflare-touch probe → empty; transcript-literal probe → empty; protected-path public provider-state probe → empty; admin/probe filesystem-path-leakage probe (`"path"\s*:|"/home/|"/var/|"/tmp/|"/etc/|"/opt/|"/mnt/`) → empty (admin_stats.py and probes.py contain no literal paths or `"path"` keys); the only `session_id|session_id_hash` addition flagged by the broad logging-surface scan is the centralized `_DROP_FIELDS` denylist itself in `libs/observability/logging.py` — i.e. the names of fields the formatter must DROP, not log surfaces emitting them; no production code path emits these fields via `extra={...}`, and the redaction tests (`tests/demo/test_b12_1_logging_redaction.py`) prove the formatter strips them even when an `extra={...}` call passes them in. Diff scope confined to: `.env.demo.example`, `infra/compose/docker-compose.demo.yml`, `libs/common/demo_settings.py`, `libs/demo/admin_stats.py` (new), `libs/demo/persistence.py`, `libs/demo/probes.py` (new), `libs/observability/error_buffer.py` (new), `libs/observability/log_rotation.py` (new), `libs/observability/logging.py`, `services/api/app/demo_main.py`, `services/worker/app/demo_worker_main.py`, all 6 new B12.1 test files, plus the two tracker files. Tracker advanced: `current_phase=B12`, `current_task="B12.2"`, `last_completed_task="B12.1"`, `blocked: false`, `blocker: null`, `tasks["B12.1"]: done`. Next pending task: B12.2 (Add email alerts) per docs/plans/demo_platform_plan.md §B12.2 (lines 2017-2042).

- B11.5: done on 2026-05-05. §B11.5 (`docs/plans/demo_platform_plan.md` lines 1962-1979) closed by adding the RP5 deployment photo at `assets/rp5_deployment_photo.jpg` (JPEG, JFIF standard 1.01, baseline, 1536×1482, 8-bit, 298 096 bytes — well under the 1.5 MB soft cap and the 4 MB hard cap; `git check-ignore` confirmed the path is not ignored). The photo is referenced from this completion entry per plan §B11.5 done-when 3 (`image is referenced from documentation`). README.md deliberately not modified — the platform-era README has no RP5 deployment section yet, so the photo reference lives in this tracker rather than inventing a placement that would predate the future deployment section. Privacy review explicitly confirmed by Gabriel against the full nine-item checklist before staging: (1) no human faces, (2) no passwords, tokens, API keys, AssemblyAI keys, Cloudflare credentials, or other secrets visible, (3) no QR codes, (4) no LAN IP addresses, hostnames, MAC addresses, SSIDs, router admin pages, or other network identifiers visible, (5) no legible serial numbers (RP5 SoC, MicroSD, monitor, power supply), (6) no private postal/address details, name plaques, or otherwise identifying environment, (7) no `/admin/stats`, shell prompt, Cloudflare tunnel URL, session token, or private filesystem path visible on any screen in frame, (8) no filenames revealing private uploads, (9) EXIF/GPS/device metadata stripped or approved for commit as-is — Gabriel's confirmation message itemised every item explicitly. The image is the actual RP5 unit currently used for the demo (CLAUDE.md §2: `feature/demo-runtime-rp5-v1`); not fabricated, not stock, not the B11.2 mobile screenshot. No edit, crop, resize, recompress, rotate, or EXIF-strip was performed on the file by Claude — committed exactly as Gabriel placed it on the host (CLAUDE.md §22: `change one layer only`; B11.5 plan: `If photo contains private information, retake or crop it` — required only on failure). B11.2 screenshot left untouched: `git diff -- assets/screenshots/b11_2_mobile.png` empty, file still 66 064 bytes from commit `12bb4b8`. No code/service/runtime/CI change: protected-path probe `git diff --name-only HEAD | grep -E '^(libs/|services/|infra/|apps/|scripts/|tests/|\.github/|docker-compose|pyproject|requirements|poetry\.lock|package-lock\.json|\.env)'` returned empty; runtime-artefact probe `git diff --name-only HEAD | grep -E '\.(wav|mp3|flac|ogg|m4a|sqlite|db|log|tmp)$'` returned empty; Cloudflare-touch probe `git diff --name-only HEAD | grep -Ei 'cloudflare|cloudflared|tunnel'` returned empty; text-diff suspicious-string probe over `docs/progress/demo_platform_progress.md docs/progress/demo_platform_progress.yaml` against `(ASSEMBLYAI|API[_ ]?KEY|TOKEN|SECRET|password|cloudflared|trycloudflare|192\.168\.|10\.|172\.(1[6-9]|2[0-9]|3[0-1])\.)` returned empty (the JPEG bytes were intentionally not text-grepped — same false-positive policy honoured for `b11_2_mobile.png` in B11.2). No live AssemblyAI call. No Cloudflare Tunnel touched (B14.1 remains future work). No backend, schema, jobs, cache_entries, usage_ledger, ASR, worker, processing, libs, scripts, infra, config, reports, package, lockfile, `.env`, or test change. Diff scope confined to `assets/rp5_deployment_photo.jpg`, `docs/progress/demo_platform_progress.md`, `docs/progress/demo_platform_progress.yaml`. Tracker advanced: `current_phase=B12`, `current_task="B12.1"`, `last_completed_task="B11.5"`, `blocked: false`, `blocker: null`, `tasks["B11.5"]: done`. Next pending task: B12.1 (Implement logs and admin stats) per docs/plans/demo_platform_plan.md §34.

- B11.2: done on 2026-05-05. §B11.2 mobile-validation gate cleared on top of the cache-key hotfix already shipped in commit `a8e0894` (B11.2 prep). Operator-driven 360 px viewport validation by Gabriel — all 10 acceptance criteria from plan §B11.2 + this profile's checklist passed: initial load under 5 s; no horizontal scroll at 360 px; curated clean and degraded `<audio>` players play and pause; example/degradation/provider selectors usable; GT textarea usable with the virtual keyboard; submit button not covered by the keyboard; comparison panel readable with Raw + Enhanced transcripts side-by-side and Word Accuracy / WER below each; Pipeline details `<details>` collapses cleanly on mobile; warning banners legible; curated cached run displays "Loaded from cache" (not the pre-hotfix `cache_miss` banner) — proving the FE→BE cache-key alignment ships in the actual rendered UI, not just in the static guard / proxy curl. Screenshot evidence committed at `assets/screenshots/b11_2_mobile.png` (PNG 585×1030, 8-bit RGBA, 66 064 bytes) — Gabriel reviewed the screenshot before approving the commit and confirmed it contains no API keys, no AssemblyAI session strings, no raw upload filenames, no secrets in the address bar, and no private local filesystem paths; only the rendered `/demo` curated-mode view with `MISTER QUILTER…` ground truth, the Whisper bypass transcripts (Word Accuracy 94.1%, WER 0.06 for the broadband_hiss degradation on ex001), and the expanded Pipeline details block (cache status="Loaded from cache", asr_model_version="tiny.en", enhancer_version="bypass", degradation_id="broadband_hiss", degradation_version="degradation_v1", metrics_version="1.0", latency_seconds=1.481). Path-only screenshot policy honoured: filename matches `assets/screenshots/b11_2_mobile.png`; `find assets -type f ! -name "*.png" ! -name "*.jpg" ! -name "*.jpeg" ! -name "*.webp"` returned 0 entries; PNG bytes were not text-grepped (false-positive hazard with binary content). No backend, schema, jobs, cache_entries, usage_ledger, ASR, worker, processing, libs, scripts, infra, config, reports, package, lockfile, or .env change. Platform UI (`services/frontend/app/page.tsx`, `services/frontend/app/layout.tsx`) and the frontend proxy (`services/frontend/app/api/[...path]/route.ts`) both byte-untouched. No live AssemblyAI call. Cloudflare Tunnel deliberately not configured — that remains B14.1 and now becomes unlocked for scheduling per plan §35.4. Tracker advanced to Case A: `current_phase=B11`, `current_task="B11.5"`, `last_completed_task="B11.2"`, `blocked: false`, `blocker: null`, `tasks["B11.2"]: done`. Privacy / runtime / secret guards before commit: assignment-shape secret scan over staged additions (`ASSEMBLYAI_API_KEY=…|sk_live_…|sk-…|aai_…|OPENAI_API_KEY=…|ANTHROPIC_API_KEY=…`) returned no matches; no `console.*` added in any frontend diff; no GT identifier added inside any `append`/`FormData`/`JSON.stringify` line; no forbidden public field (`spend|key_configured|ledger|session_id_hash|raw_payload`) added; no runtime artefacts staged after a transient `services/frontend/next-env.d.ts` doc-comment churn from a prior `next build` was reverted; no frontend `.env`; `services/frontend/package-lock.json` md5 byte-identical to HEAD; protected paths empty against `services/api/ services/worker/ libs/ scripts/ infra/ config/ reports/ pyproject.toml services/frontend/app/page.tsx services/frontend/app/layout.tsx services/frontend/package.json services/frontend/package-lock.json`. Diff scope confined to `assets/screenshots/b11_2_mobile.png`, `docs/progress/demo_platform_progress.md`, `docs/progress/demo_platform_progress.yaml`. Advance to B11.5 (Add RP5 deployment photo) per docs/plans/demo_platform_plan.md §33.

- B11.2 (cache hotfix only — task remains BLOCKED pending mobile validation): hotfix landed on 2026-05-05. §B11.2 is a validation gate before public exposure (plan §35.4 explicitly delays Cloudflare cutover until B11.2 passes). The B11.1c headless smoke surfaced a frontend-only cache-key mismatch that would defeat both B11.2 mobile validation and the subsequent public smoke: `scripts/prewarm_cache.py::SUPPORTED_ENHANCER = "bypass"` while `libs/common/versions.py::DEFAULT_ENHANCER_VERSION = "1.0"`, and the cached-mode `submitCached` body in `services/frontend/app/demo/page.tsx` did not pass `enhancer_version`, so `libs/demo/cache.build_cache_key` fell back to the default and never matched the prewarmed `bypass` row → curated examples always returned `cache_miss`. Frontend-only fix shipped: `services/frontend/app/demo/page.tsx::submitCached` now sends `{example_id, degradation_id, provider:"whisper", enhancer_version:"bypass"}`. Backend cache logic, `libs/common/versions.py`, prewarm/validate scripts, `services/api/`, `services/worker/`, `libs/`, `infra/`, `config/`, `reports/`, platform UI (`services/frontend/app/page.tsx`, `layout.tsx`), and frontend lockfile are all byte-untouched. Created: `tests/demo/test_b11_2_cached_mode_enhancer_pin.py` (4 static guards extracting the `submitCached` body literal between `"/api/demo/run-cached"` and the matching `}` of `JSON.stringify({...})` so assertions run only against the cached request body, not the unrelated upload region — pins `enhancer_version: "bypass"`, keeps `provider: "whisper"`, forbids any `assemblyai` reference inside the cached body, and forbids `manualGt`/`ground_truth`/`reference`/`reference_text`/`"gt"` inside the cached body so GT cannot leak into the cached request). Validation: backend regression in ephemeral `demo-api` container with `tests/`, `services/frontend/`, `config/`, `scripts/`, `reports/` bind-mounted RO → **670 passed + 2 skipped** (B11.1c baseline 666 + 4 new B11.2 guards); guard test alone → 4 passed; direct `grep -nA8 -B4 "run-cached" services/frontend/app/demo/page.tsx` and `grep -n "enhancer_version.*bypass"` confirm the new field on line 286 inside the `submitCached` POST body block; frontend lint/typecheck/build in `node:20-bookworm-slim` → ESLint clean, `tsc --noEmit` exit 0, `next build` "Compiled successfully" with `/demo` 5.85 kB / 93 kB (B11.1c baseline byte-identical) and `/` 3.94 kB / 91.1 kB (untouched), `services/frontend/package-lock.json` md5 `cdfd804f19e464e0cdaff1557bfc0ea8` unchanged before/after `npm ci`. Headless smoke: brought up demo backend via `docker compose -f infra/compose/docker-compose.demo.yml up -d` (host port 8001 verified by `docker compose ps`), `/demo/health` returned `{"status":"ok","mode":"demo","db_ok":true,"queue_depth":0}`, `/demo/examples` returned 5 examples with all 5 degradations on ex001, `/demo/providers/assemblyai/status` returned `{"assemblyai":{"state":"disabled","cap_state":"below"}}`; cache_entries inspection on host SQLite (`/home/gbibbo/asr_enhancement_runtime/db/demo.db`) showed 30 prewarmed rows all with `enhancer_version: 'bypass'` (ex001/ex003/ex004/ex007/ex010 × 6 degradations including `clean`); started a durable `node:20-bookworm-slim` frontend container `asr-demo-frontend-b11-2` on port 3000 with `--add-host=host.docker.internal:host-gateway` and `BACKEND_API_BASE_URL=http://host.docker.internal:8001`, ready in 9 s. **POSITIVE smoke through the Next proxy** with the canonical body `{"example_id":"ex001","degradation_id":"muffled","provider":"whisper","enhancer_version":"bypass"}` → `status="cache_hit"`, `cache_key="ex001|muffled|degradation_v1|whisper|tiny.en|bypass|1.0"`, `result.hypothesis="Mr. Crowder is the apostle of the middle classes, and we are glad to welcome his gospel."` — the cached transcript reproduces the prewarmed Whisper output for the muffled-degraded clip on this RP5; **NEGATIVE control** with the pre-fix body (no `enhancer_version`) → `status="cache_miss"`, `detail="No cached result for this configuration."`, confirming the bug existed and the fix is necessary. Whisper upload smoke through the proxy: downloaded ex001/clean.wav via `/api/demo/examples/ex001/audio/clean` (187 404 bytes, RIFF/PCM/16 bit/16 kHz mono), `POST /api/demo/upload provider=whisper` → 202 + job_id, polling `/api/demo/jobs/{id}` reached `completed` after ~10 s; `/api/demo/jobs/{id}/result` returned `provider="whisper"`, `asr_model_version="tiny.en"`, `enhancer_version="bypass"`, `degradation_applied=false`, `raw.transcript="Mr. Quilter is the apostle of the middle classes, and we are glad to welcome his gospel."` — Whisper end-to-end intact. **Important methodological note**: the static guard test proves `submitCached` in `page.tsx` ships `enhancer_version: "bypass"`; the proxy curl smoke proves that the same canonical body returns `cache_hit` through the Next.js proxy. Together they validate the FE→BE contract; **they do not prove the browser executed the click handler**, which is what the operator-driven mobile validation in step 7 covers. Mobile/visual validation **was not performed in this iteration** — RP5 has no host browser, and the operator-driven 360 px viewport checklist (initial load < 5 s, no horizontal scroll, audio play/pause for clean and degraded, GT textarea usable with virtual keyboard, submit button not covered, comparison panel readable, pipeline `<details>` collapses, warning banners legible, curated cached run shows "Loaded from cache" rather than the previous `cache_miss` banner, 2–3 screenshots if Gabriel approves committing to `assets/screenshots/b11_2_mobile_*.png`) remains pending. Per CLAUDE.md §21 and the agreed Case A/Case B branching, B11.2 stays blocked pending Gabriel mobile visual validation and screenshots: tracker keeps `current_task="B11.2"`, `last_completed_task="B11.1c"`, `blocked: true`, blocker recorded; `tasks["B11.2"]` is **not** marked done. AssemblyAI live UI smoke deliberately not run (no spend authorization; CLAUDE.md §15, §21). Cloudflare Tunnel setup deliberately not run — that is plan §B14.1 and gates on B11.2 passing first. Privacy / runtime / secret guards before commit: targeted scan against `git diff` for `ASSEMBLYAI_API_KEY|OPENAI_API_KEY|ANTHROPIC_API_KEY|sk_live|sk-[A-Za-z0-9]{20,}|aai_[A-Za-z0-9]{20,}` empty; no `console.*` added in `services/frontend/app/demo/page.tsx` diff; no GT identifier (`manualGt|ground_truth|"gt"`) added inside any `append`/`FormData`/`JSON.stringify` line in the diff; no forbidden public field (`spend|key_configured|ledger|session_id_hash|raw_payload`) added in the diff; no runtime artifacts (`*.wav|.mp3|.m4a|.flac|.db|.sqlite|.log|.tsbuildinfo`) staged after a transient `services/frontend/tsconfig.tsbuildinfo` from `next build` was removed and a transient `services/frontend/next-env.d.ts` doc-comment churn from `next build` was reverted; no frontend `.env`; `package-lock.json` md5 byte-identical to HEAD; protected paths empty against `git diff --name-only HEAD -- services/api/ services/worker/ libs/ scripts/ infra/ config/ reports/ pyproject.toml services/frontend/app/page.tsx services/frontend/app/layout.tsx services/frontend/package.json services/frontend/package-lock.json`. Diff scope confined to `services/frontend/app/demo/page.tsx`, `tests/demo/test_b11_2_cached_mode_enhancer_pin.py`, `docs/progress/demo_platform_progress.md`, `docs/progress/demo_platform_progress.yaml` — no screenshots committed in this iteration. Mobile validation checklist Gabriel must complete to close B11.2: (1) load `/demo` at 360 px viewport (real phone or Chrome devtools mobile emulation) over LAN — no Cloudflare in B11.2; (2) initial load < 5 s; (3) no horizontal scroll; (4) play/pause clean and degraded audio; (5) selectors usable; (6) GT textarea usable with virtual keyboard, submit button not covered; (7) comparison panel readable; (8) pipeline details `<details>` collapses; (9) warning banners legible; (10) curated cached run displays "Loaded from cache" (not `cache_miss`); (11) optionally capture 2–3 screenshots into `assets/screenshots/b11_2_mobile_*.png` after confirming each contains no API keys, session strings, raw upload filenames, secrets in address bar, or private local filesystem paths — paths only, do not text-grep PNG bytes. Once Gabriel confirms validation passed, advance tracker to Case A: `current_task="B11.5"`, `last_completed_task="B11.2"`, `blocked: false`, `blocker: null`, `tasks["B11.2"]: done`.

- B11.1c: done on 2026-05-05. §33 closes B11.1 by surfacing raw vs enhanced transcripts, frontend-only Word Accuracy / WER when ground truth is available, and a mobile-friendly collapsed Pipeline details panel — strictly frontend-only: no backend, schema, result_json, jobs, cache_entries, usage_ledger, processing, ASR, enhancer, worker, cleanup, proxy header, or dependency change; no new endpoint (no `/demo/score`); no live AssemblyAI call; manual ground truth never leaves the browser, never enters any HTTP body, FormData, browser storage (localStorage/sessionStorage/IndexedDB/cookies), log line, or backend computation. Created: services/frontend/app/demo/scoring.ts (pure TS module mirroring libs/audio/metrics.py — `normalizeText` lowercases, strips `/[^\p{L}\p{N}_\s]/gu` (Unicode-aware approximation of Python `\w`), collapses whitespace and trims; `wordEditDistance` two-row word-level Levenshtein DP semantically identical to `_word_edit_distance`; `computeMetrics` returns `available=false` on null/whitespace-only reference, otherwise `wer = dist/len(refTokens)` and `wordAccuracy = clamp(1-wer, 0, 1)`; empty hypothesis with non-empty reference yields `wer=1.0`, `wordAccuracy=0.0`; pure functions only — no fetch/XMLHttpRequest, no localStorage/sessionStorage/indexedDB/document.cookie, no navigator.sendBeacon, no console.*); services/frontend/app/demo/versions.ts (frozen TS mirror of libs/common/versions.py — `DEGRADATION_VERSION="degradation_v1"`, `METRICS_VERSION="1.0"`, `DEFAULT_ENHANCER_VERSION="1.0"`; explicitly NOT a synonym for bypass — bypass detection in page.tsx uses literal string equality `enhancer_version === "bypass"` matching `BYPASS_ENHANCER_VERSION` in libs/audio/enhancement.py). Modified: services/frontend/app/demo/page.tsx (imports `computeMetrics` from `./scoring` and `DEGRADATION_VERSION` + `METRICS_VERSION` from `./versions`; replaced the small ResultPanel with a wider one rendering, in order: B9.3 warnings rendering preserved, B10.2 errorMessage preserved, **Raw transcript** block whenever a raw hypothesis exists with the hypothesis read as `result.raw?.transcript ?? result.hypothesis ?? null` to bridge upload vs cached payload shapes — fixes the latent gap where every `cache_hit` used to render "Transcript is not available yet." because `cachedResult.raw?.transcript` was undefined for cached entries; **Enhanced transcript** block whenever an enhanced hypothesis exists or the cached/bypass path maps it to the same hypothesis (`result.enhanced?.transcript ?? result.hypothesis ?? null`); a non-blocking warning card rendering `result.enhanced_error` when present (raw is still shown alongside, plan decision rule 2); the bypass note `Enhancer is bypass — audio is not enhanced. Raw and enhanced transcripts are identical.` when and only when `enhancerVersion === "bypass"` (literal-string equality only, **not** via DEFAULT_ENHANCER_VERSION); a `<details><summary>Pipeline details</summary>` collapsed-by-default block (mobile-first) listing exactly `job_id` (upload only), `cache status` (`Loaded from cache` for cache_hit / `computed (whisper)` / `computed (assemblyai)`), `provider`, `asr_model_version`, `enhancer_version` (real value, not hardcoded), `degradation_id`, `degradation_version` (from `result.degradation_version` for upload, fallback to the `DEGRADATION_VERSION` const for cached), `degradation_applied` (upload-only — row omitted when undefined), `metrics_version` (the const), `latency_seconds` (read `result.raw?.latency_seconds ?? result.latency_seconds`); the Pipeline details block contains **none** of `cache_key`, `session_id_hash`, `ledger_id`, `key_configured`, `audio_sha256`, `source_report`, `input_audio_path`, `degraded_audio_path`, `enhanced_audio_path`, `spend`, `caps`, raw provider payload, or any other forbidden field (B11.1a/B11.1b privacy discipline). Both call sites updated: cached path passes `selectedExample.ground_truth` (only when non-whitespace) as the verified reference; upload path passes `manualGt.trim() || null` as the optional manual reference; neither value travels in any HTTP request. The polling/job-status label is shown externally above the comparison panel as a `Job status: …` `.demo-subtle` line — kept out of Pipeline details per plan field discipline. Existing FormData logic byte-identical: only `file`, `provider`, optional `degradation_id`; `X-Demo-Session-Id` still travels only on AssemblyAI uploads. services/frontend/app/demo/types.ts extended `DemoResult` with safe optional fields only — `hypothesis?: string`, `wer?: number | null`, `word_accuracy?: number | null`, `latency_seconds?: number | null`, `degradation_applied?: boolean`, `degradation_version?: string | null`, `enhanced?: { transcript: string; latency_seconds: number; preset_applied?: string | null; enhanced_flag?: boolean; enhancement_fallback?: boolean } | null`, `enhanced_error?: string | null`; the type stays a deliberate subset of the wire format so accidental access to forbidden fields fails typecheck. services/frontend/app/globals.css appended `.demo-compare`, `.demo-compare-block`, `.demo-compare-label`, `.demo-metric-line`, `.demo-bypass-note`, `.demo-pipeline-details`, `.demo-pipeline-details summary`, `.demo-pipeline-details[open] summary` — single-column flex, no media queries, parent `.demo-main` 720 px cap handles narrow viewports. Created: tests/demo/test_b11_1c_versions_drift.py (4 tests — DEGRADATION_VERSION / METRICS_VERSION / DEFAULT_ENHANCER_VERSION pinned via regex to `libs.common.versions`, plus a guard that DEFAULT_ENHANCER_VERSION must never become `"bypass"` so a future refactor would force a re-audit of the bypass detection); tests/demo/test_b11_1c_scoring_executable.py (1 test — compiles `services/frontend/app/demo/scoring.ts` with `tsc --target ES2020 --module commonjs` inside a `node:20-bookworm-slim` container and asserts 11 canonical scoring cases — basic / apostrophe / whitespace / empty normalization, exact match WAcc=1, empty hypothesis WER=1 WAcc=0, one substitution / one deletion / one insertion all WER=0.5, WER>1 clamps WAcc to 0, empty reference unavailable, whitespace-only reference unavailable, case+punctuation fold equals lowercased; skips when docker is not on PATH so non-RP5 hosts don't false-fail); tests/demo/test_b11_1c_frontend_comparison_static.py (regex guards over page.tsx, scoring.ts, versions.ts — privacy line still verbatim; no GT appended to FormData; no console.* logging in any of page.tsx/scoring.ts/versions.ts; no GT-related localStorage/sessionStorage/indexedDB/document.cookie writes; page.tsx makes no `localStorage.setItem` call (the only allowed setItem stays in services/frontend/app/demo/session.ts); scoring.ts contains no fetch/XMLHttpRequest/localStorage/sessionStorage/indexedDB/cookie/sendBeacon/console.* (TS comments stripped before scanning so the documentation header that mentions the forbidden APIs by name does not self-trip); page.tsx must not reference any forbidden field substring; visible `Raw transcript` / `Enhanced transcript` / `Pipeline details` labels present; `Word Accuracy` and `WER` labels present; `computeMetrics` is imported from `./scoring`; Pipeline details uses native `<details>`/`<summary>`; pipeline keys provider, asr_model_version, enhancer_version, degradation_id, degradation_version, metrics_version, latency_seconds, cache status all rendered; bypass detection uses literal string `enhancer_version === "bypass"`, not DEFAULT_ENHANCER_VERSION; bypass note text "Enhancer is bypass" present). Untouched: services/frontend/app/page.tsx, services/frontend/app/layout.tsx, services/frontend/app/api/[...path]/route.ts, services/frontend/app/demo/session.ts, services/frontend/app/demo/degradations.ts, services/frontend/package.json, services/frontend/package-lock.json (md5 `cdfd804f19e464e0cdaff1557bfc0ea8` unchanged before/after `npm ci`), services/frontend/next.config.mjs, services/frontend/next-env.d.ts (transient doc-comment churn from `next build` reverted), services/frontend/tsconfig.tsbuildinfo (transient build artifact removed), services/api/, services/worker/, libs/, scripts/, infra/compose/, config/, reports/, pyproject.toml, requirements.lock.arm64, requirements.lock.x86_64, .env.demo.example, all earlier B11.1a/B11.1b tests. Compose-first validation inside ephemeral `demo-api` container with `tests/`, `services/frontend/`, `config/`, `scripts/`, `reports/` bind-mounted RO: `pytest -q tests/demo tests/asr tests/audio tests/common` → **666 passed + 2 skipped** in 26.99 s, exactly +19 from the B11.1b baseline of 647 passed (+1 skipped is the docker-requiring scoring self-test which skips inside the demo-api container without DinD; the executable self-test was instead exercised on the host RP5 directly via the same `docker run node:20-bookworm-slim ...` command, exit 0, stdout `scoring self-test passed`). Frontend toolchain validation in a one-shot `node:20-bookworm-slim` container (RP5 host has no Node): `npm ci --no-audit --no-fund` → 331 packages added; `services/frontend/package-lock.json` md5 unchanged before/after; `npm run lint` → "✔ No ESLint warnings or errors"; `npm run typecheck` → exit 0; `npm run build` → "✓ Compiled successfully", routes table now `○ /` (3.94 kB / 91.1 kB First Load JS, byte-untouched), `ƒ /api/[...path]` (proxy, byte-untouched), `○ /demo` (5.85 kB / 93 kB First Load JS — up from B11.1b 5.04 kB / 92.2 kB, the 0.81 kB / 0.8 kB delta is the new comparison panel, scoring import + computeMetrics call sites, Pipeline details `<details>` block, version-pin imports, and external job-status line). Headless smoke against the demo backend brought up via `docker compose -f infra/compose/docker-compose.demo.yml up -d demo-api demo-worker` (`/demo/health` returned `{"status":"ok","mode":"demo","db_ok":true,"queue_depth":0}`); the demo frontend started in a one-shot detached `node:20-bookworm-slim` container named `asr-demo-frontend-b11-1c` with `BACKEND_API_BASE_URL=http://host.docker.internal:8001` and `--add-host=host.docker.internal:host-gateway`, ready in ~14 s. Cached path: `GET /demo` → 200; `GET /` → 200 (legacy untouched); `GET /api/demo/examples` → 200 with all five examples populated and ground_truth strings non-empty; `POST /api/demo/run-cached` against ex001/muffled returned `cache_hit` with the cached `result_json` shape (top-level keys: `asr_model_version`, `asr_provider`, `audio_path_relative`, `audio_sha256`, `degradation_id`, `enhancer_version`, `example_id`, `hypothesis`, `latency_seconds`, `source_report`, `wer`, `word_accuracy`) **only when the request body explicitly carried `enhancer_version: "bypass"`**. Without the explicit `enhancer_version`, run-cached returned `cache_miss`: this is a pre-existing FE/BE inconsistency outside B11.1c scope — `scripts/prewarm_cache.py` (B8.1) bakes `enhancer_version="bypass"` into every prewarmed cache_key while `libs/demo/cache.build_cache_key` falls back to `versions.DEFAULT_ENHANCER_VERSION = "1.0"` when no override is provided, and the frontend `submitCached` request body does not pass `enhancer_version`. The frontend code path that B11.1c repairs (`result.raw?.transcript ?? result.hypothesis`) is fully validated against the cached wire format above; the FE→BE cache_key mismatch is independent and warrants a follow-up task before the public demo cutover (recommended: add `enhancer_version: "bypass"` to the frontend run-cached body OR change `cache.build_cache_key` to use `BYPASS_ENHANCER_VERSION` as the default, and re-run `scripts/validate_cache.py`). Upload path (Whisper local): downloaded `/api/demo/examples/ex001/audio/clean` to `/tmp/b11_1c_upload.wav` (187 404 bytes), `POST /api/demo/upload` with `provider=whisper` returned 202 + `job_id`; polling `/api/demo/jobs/{id}` reached `completed` after ~4 s; `/api/demo/jobs/{id}/result` returned `provider="whisper"`, `asr_model_version="tiny.en"`, `enhancer_version="bypass"`, `degradation_applied=false`, `raw.transcript="Mr. Quilter is the apostle of the middle classes…"`, `enhanced.transcript` byte-identical to raw (bypass), `enhanced_error=null`. The temporary upload audio was deleted from `/tmp` immediately after the smoke run; the frontend container was torn down with `docker rm -f asr-demo-frontend-b11-1c` and the demo backend with `docker compose ... down`. AssemblyAI live UI smoke deliberately not run because Gabriel did not authorize spend (CLAUDE.md §15, §21). Manual visual/mobile browser smoke deferred to B11.2 / Gabriel browser validation — RP5 has no host browser by design, the page is a client component shell, the comparison panel and Pipeline details rendering are guarded by the static Python tests (visible labels, Word Accuracy / WER labels, `<details>`/`<summary>` collapse, pipeline keys, bypass detection) and the executable scoring self-test (numerical correctness end-to-end), so deferral does not block closure (plan §B11.1c). Privacy / runtime / secret guards before commit: `git diff | grep -iE "ASSEMBLYAI_API_KEY|sk_live|aai_"` empty; `git diff -- services/frontend/app/demo/ | grep -E "console\.(log|debug|info|warn|error)"` empty; `git diff -- services/frontend/app/demo/page.tsx | grep -E "append\\s*\\(\\s*['\"](ground[_ ]?truth|gt|reference|reference_text)['\"]"` empty; `git diff -- services/frontend/app/demo/page.tsx | grep -E "localStorage|sessionStorage|indexedDB|document\\.cookie" | grep -iE "ground|gt|reference"` empty; `git diff -- services/frontend/app/demo/page.tsx | grep -E "cache_key|session_id_hash|ledger_id|key_configured|audio_sha256|source_report|input_audio_path|degraded_audio_path\\b|enhanced_audio_path"` empty; `git status --short | grep -E '\\.(wav|mp3|m4a|flac|db|sqlite|log)$'` empty; `git status --short | grep -E '\\.(tsbuildinfo)$|\\.js$'` empty after the transient `services/frontend/tsconfig.tsbuildinfo` and `services/frontend/next-env.d.ts` doc-comment churn from `next build` were removed/reverted; `git diff --name-only` confined to the eight allowed source paths plus the tracker pair (services/frontend/app/demo/page.tsx, services/frontend/app/demo/types.ts, services/frontend/app/demo/scoring.ts, services/frontend/app/demo/versions.ts, services/frontend/app/globals.css, tests/demo/test_b11_1c_versions_drift.py, tests/demo/test_b11_1c_scoring_executable.py, tests/demo/test_b11_1c_frontend_comparison_static.py, docs/progress/demo_platform_progress.md, docs/progress/demo_platform_progress.yaml). Preserved invariants: B9.3 non-English warning still surfaces above transcripts; B10.2 AssemblyAI session gate, exact rejection messages, and `X-Demo-Session-Id` proxy header allowlist (`FORWARDED_REQUEST_HEADERS = ["content-type", "x-demo-session-id"]`) untouched; B10.1 provider-state public projection (state + cap_state, no spend/caps) untouched; B11.1a privacy/forbidden-fields discipline extended to the new comparison panel and Pipeline details; B11.1b audio playback object-URL lifecycle (created on file change, revoked on next change / unmount, NOT revoked on submit success or job completion) and verified-GT block byte-identical; manual GT remains in React state only and is cleared exclusively on file change. Word Accuracy is in [0, 1] (clamped against WER>1); WER is unclamped (the cached `wer` column on prewarmed entries is read-only and unchanged). Advance to B11.2 (mobile/visual validation + Cloudflare Tunnel cutover acceptance) per docs/plans/demo_platform_plan.md §33.

- B11.1b: done on 2026-05-04. §33 extends the existing `/demo` UI shipped in B11.1a with audio playback for curated examples and uploads, a verified ground-truth display for curated examples, and a frontend-only optional ground-truth textarea for uploads with the exact CLAUDE.md §16 privacy line. Strictly frontend-only: no backend, schema, result_json, jobs, cache_entries, usage_ledger, cleanup, ASR, worker, provider-state, proxy-header, or dependency change. Word Accuracy / WER / pipeline-detail comparison remain B11.1c. Modified: services/frontend/app/demo/page.tsx (added two `useState` slots — `uploadObjectUrl: string | null` and `manualGt: string` — plus a `useEffect([uploadFile])` that calls `URL.createObjectURL(uploadFile)` when the file becomes non-null, stores the resulting `blob:` URL in state, and revokes it from the effect cleanup so a new file always replaces the previous URL atomically; revocation also runs on unmount; revocation never runs on submit success or job completion, so the user can still play their original audio after seeing the transcript; `onFileChange` clears `manualGt` so a new file does not carry a stale GT but submit success and job completion leave the textarea visible; cached-example section gained two `<audio controls preload="metadata">` rows wired to same-origin proxy URLs `/api/demo/examples/{example_id}/audio/clean` and `/api/demo/examples/{example_id}/audio/degraded/{degradation_id}`, both `encodeURIComponent`-wrapped, plus a `Ground truth (verified)` block rendering `selectedExample.ground_truth` from the existing `/demo/examples` payload — when `audio_available` is false or the `clean_audio_path` / `degraded_audio_paths[id]` field is missing/empty, an explicit "Audio unavailable for this example" or "Audio unavailable for this example/degradation" `.demo-subtle` line is shown instead of a broken `<audio>` element; upload-mode form gained an optional `<textarea>` labelled "Optional ground truth" bound to `manualGt`/`setManualGt` plus the verbatim privacy line "Optional ground truth is used only to calculate accuracy for this session. It is not stored." rendered in a `.demo-gt-privacy.demo-subtle` paragraph; an upload-original `<audio>` row appears whenever `uploadObjectUrl !== null`, sourced directly from the blob URL; `submitUpload` is byte-identical aside from the placement of the textarea — the FormData still contains exactly `file`, `provider`, optional `degradation_id` and never `ground_truth`/`gt`/`reference`/`reference_text`; the AssemblyAI gating, X-Demo-Session-Id handling, polling, result rendering, B9.3 warnings rendering, and `result_json.metrics` hidden status are all preserved). Modified: services/frontend/app/globals.css (appended `.demo-audio-row`, `.demo-audio-label`, `.demo-gt`, `.demo-gt-privacy`, `.demo-textarea` — plain CSS, mobile-first, no new media queries; reuses the pre-existing `audio { width: 100%; }`). Created: tests/demo/test_b11_1b_examples_payload_for_audio_ui.py (6 backend tests over the public projection the new UI consumes — `DemoExample.model_fields` contains ground_truth, audio_available, clean_audio_path, degraded_audio_paths; `config/demo_examples.json` loads non-empty; every example has a non-empty `ground_truth`; `audio_available=true` requires non-empty `clean_audio_path`; every advertised `degradation_ids` entry has a matching non-empty `degraded_audio_paths` key; `degraded_audio_paths` has no orphan keys not advertised in `degradation_ids`; gracefully skips when the config is absent on a fresh checkout, never failing a CI without examples). Created: tests/demo/test_b11_1b_frontend_privacy_static.py (6 static regex checks over services/frontend/app/demo/page.tsx — exact privacy line present verbatim; no `.append(...)` of any GT identifier (`ground_truth`, `ground truth`, `gt`, `reference`, `reference_text`, case-insensitive); no `console.log/debug/info/warn/error` anywhere in page.tsx; both `URL.createObjectURL` and `URL.revokeObjectURL` present; no GT-related write to `localStorage`/`sessionStorage`/`indexedDB`/`document.cookie`; page.tsx makes no direct `localStorage.setItem` call (the only allowed setItem stays in `services/frontend/app/demo/session.ts` for the B10.2/B11.1a `demo_session_id` cookie-equivalent); deliberately does NOT assert revocation on submit success — playback must remain available after submit). Untouched: services/frontend/app/api/[...path]/route.ts, services/frontend/app/demo/session.ts, services/frontend/app/demo/types.ts, services/frontend/app/demo/degradations.ts, services/frontend/app/page.tsx, services/frontend/app/layout.tsx, services/frontend/package.json, services/frontend/package-lock.json (md5 unchanged before/after `npm ci`), services/frontend/next.config.mjs, services/api/, services/worker/, libs/, scripts/, infra/compose/, config/, reports/, pyproject.toml, requirements.lock.arm64, requirements.lock.x86_64, .env.demo.example, tests/demo/test_b11_1a_degradation_id_drift.py. Compose-first validation inside ephemeral `demo-api` container with `tests/`, `services/frontend/`, `config/`, `scripts/`, `reports/` bind-mounted RO so the freshly-added test files and edited TSX/CSS were visible (Dockerfile.demo bakes the source tree at image build time and does not COPY `scripts/`, so this mount set is the canonical broad-regression invocation): `pytest -q tests/demo tests/asr tests/audio tests/common` → **647 passed + 1 skipped** in 21.79 s, exactly +12 from the B11.1a baseline of 635 passed (6 new B11.1b payload tests + 6 new B11.1b privacy-static tests). Frontend toolchain validation in a one-shot `node:20-bookworm-slim` container (RP5 host has no Node): `npm ci --no-audit --no-fund` succeeded, `services/frontend/package-lock.json` md5 `cdfd804f19e464e0cdaff1557bfc0ea8` unchanged before/after; `npm run lint` → "✔ No ESLint warnings or errors"; `npm run typecheck` → exit 0; `npm run build` → "✓ Compiled successfully", routes table now `○ /` (3.94 kB, 91.1 kB First Load JS, untouched), `ƒ /api/[...path]` (proxy, byte-untouched), `○ /demo` (5.04 kB / 92.2 kB First Load JS — up from B11.1a 4.52 kB / 91.7 kB, the 0.52 kB / 0.5 kB delta is the new audio rows, GT block, textarea, privacy line, and `useEffect`/state for the object URL). Headless smoke against the demo backend brought up via `docker compose -f infra/compose/docker-compose.demo.yml up -d demo-api demo-worker` (`/demo/health` returned `{"status":"ok","mode":"demo","db_ok":true,"queue_depth":0}`); the demo frontend was started in a one-shot detached `node:20-bookworm-slim` container named `asr-demo-frontend-b11-1b` with `BACKEND_API_BASE_URL=http://host.docker.internal:8001` and `--add-host=host.docker.internal:host-gateway` (host-gateway worked on this RP5 Docker version, no host-IP fallback was needed); the next-start container was ready in ~2 s; `GET /demo` → 200 (HTML); `GET /api/demo/examples` → 200 with the ex001 payload including the verified `ground_truth`, all 5 `degradation_ids`, and matching `degraded_audio_paths`; `GET /api/demo/examples/ex001/audio/clean` → 200 + `audio/wav`; `GET /api/demo/examples/ex001/audio/degraded/muffled` → 200 + `audio/wav`; `GET /` → 200 (existing platform UI byte-unchanged); the frontend container was torn down with `docker rm -f asr-demo-frontend-b11-1b` and the demo backend with `docker compose ... down`. The headless smoke does not inspect HTML for selected-state UI — `/demo` is a client route and the audio rows / GT block / textarea / privacy line / warning rendering are state-driven; their presence is enforced by the static Python tests (privacy line, GT-leak, console-log, object-URL lifecycle, storage-write guards) and by `npm run typecheck`/`npm run build`. Visual layout / mobile-viewport / click-through verification is left for Gabriel's manual browser smoke (RP5 has no host browser). AssemblyAI live UI smoke deliberately not run because Gabriel did not authorize spend in this turn (CLAUDE.md §15, §21). Privacy and runtime guards run against the working tree before commit: `git diff | grep -iE "ASSEMBLYAI_API_KEY|sk_live|aai_"` empty (no key-like literal); `git diff -- services/frontend/app/demo/page.tsx | grep -E "console\.(log|debug|info|warn|error)"` empty; `git diff -- services/frontend/app/demo/page.tsx | grep -E "append\\s*\\(\\s*['\"](ground[_ ]?truth|gt|reference|reference_text)['\"]"` empty (no GT field added to FormData); `git diff -- services/frontend/app/demo/page.tsx | grep -E "localStorage|sessionStorage|indexedDB|document\\.cookie" | grep -iE "ground|gt|reference"` empty (manual GT is not persisted to any browser store); `git status --short | grep -E '\\.(wav|mp3|m4a|flac|db|sqlite|log)$'` empty; `git diff --name-only` confined to the four allowed source paths plus the tracker pair (services/frontend/app/demo/page.tsx, services/frontend/app/globals.css, tests/demo/test_b11_1b_examples_payload_for_audio_ui.py, tests/demo/test_b11_1b_frontend_privacy_static.py, docs/progress/demo_platform_progress.md, docs/progress/demo_platform_progress.yaml); a transient `services/frontend/next-env.d.ts` doc-comment update emitted by `next build` and a transient `services/frontend/tsconfig.tsbuildinfo` artifact were both reverted/removed before commit so the diff stayed strictly inside the allowed footprint. Net diff footprint: 1 modified frontend page (+110 -0), 1 modified frontend stylesheet (+37 -0), 2 new backend test files, plus this tracker pair. Operational note for future tracker readers: the in-container broad-regression `pytest tests/demo tests/asr tests/audio tests/common` requires `${REPO}/scripts:/app/scripts:ro` and `${REPO}/reports:/app/reports:ro` in addition to the `tests/`, `services/frontend/`, and `config/` mounts because Dockerfile.demo does not COPY those paths — without them 67 cache/cleanup tests error out at fixture setup. Object URL lifecycle pinned: created on `uploadFile != null`, revoked on file-change effect cleanup (so a new file revokes the previous URL atomically), revoked on unmount, NOT revoked on submit success or job completion — playback remains available after the user sees the transcript. Manual GT is held only in React state and is cleared exclusively when the user picks a different file (via `onFileChange`); submit success and job completion leave the textarea visible. Manual GT never enters FormData, never enters any HTTP body, never enters logs, and is never persisted to any browser storage; the static test file pins all of those properties. Word Accuracy / WER / pipeline detail comparison remain B11.1c per docs/plans/demo_platform_plan.md §1914-1923; `result_json.metrics` continues to be ignored in the UI. Backward-compat with B9.3 (non-English warnings) and B10.2 (AssemblyAI session gate, exact rejection messages) preserved — same-named state slots, same-shaped FormData, same `X-Demo-Session-Id` injection only on AssemblyAI uploads, same `FORWARDED_REQUEST_HEADERS = ["content-type", "x-demo-session-id"]` proxy allowlist. Advance to B11.1c (Implement comparison and pipeline details) per docs/plans/demo_platform_plan.md §33.

- B11.1a: done on 2026-05-04. §33 ships the public `/demo` UI on top of the already-shipped `/demo/*` API: a new Next.js client route at `/demo` exposes two modes (curated examples, upload), provider/degradation/example selectors, provider-state-aware AssemblyAI gating, a tiny same-origin proxy header allowlist for `X-Demo-Session-Id`, and a minimal result panel — strictly within the B11.1a scope (no audio playback, no GT field, no comparison panel, no metrics; those remain B11.1b/B11.1c). Created: services/frontend/app/demo/page.tsx (client component "use client"; mode tabs `cached_example` (default) / `upload`; cached_example consumes `POST /api/demo/run-cached` with `provider="whisper"` and renders cache_hit transcripts plus an honest cache_miss message — no silent fallback to a live run; upload consumes `POST /api/demo/upload` with optional `degradation_id`, polls `GET /api/demo/jobs/{id}` every 2 s up to ~120 s, then `GET /api/demo/jobs/{id}/result`; provider selector locked to whisper (label "whisper (cached only)") in cached mode; provider selector in upload mode shows whisper always-on plus assemblyai gated on `state==="available" && cap_state ∉ {soft_reached, hard_reached}`; degradation selector restricted to the selected example's `degradation_ids` in cached mode and to the 5 frozen IDs plus an explicit "none" in upload mode; pre-checks file extension/size/duration client-side using the same 5 MB / 30 s policy as the backend so the UX surfaces obvious errors before the round-trip; provider-state refreshed on mount and after each upload submit; renders only job_id (upload only) + status + origin (`cache_hit` / `computed (whisper)` / `computed (assemblyai)`) + provider + asr_model_version + read-only "enhancer: bypass" + raw transcript + `result_json.warnings[].message` info banners + `error_message` verbatim on failed jobs; never renders cap_state, spend, caps, key_configured, ledger info, session id, or any per-browser hash). Created: services/frontend/app/demo/types.ts (TS contracts for the public projection only — provider/cap state vocabularies, DemoExample, DemoExamplesResponse, DemoHealthResponse, DemoWarning, DemoResult, RunCachedRequest/Response, DemoJobView, DemoJobResultResponse, UploadAcceptedResponse, BackendDetail; deliberately excludes any backend internal-only field). Created: services/frontend/app/demo/degradations.ts (frozen `DEGRADATION_IDS = ["far_field_room","cafe_background","phone_call","muffled","broadband_hiss"] as const` mirroring `libs/audio/degradations.py::DEGRADATION_REGISTRY` order, plus `DEGRADATION_LABELS` for human display). Created: services/frontend/app/demo/session.ts (lazy `getOrCreateDemoSessionId()` using `crypto.randomUUID()` with `getRandomValues` fallback; persisted in `localStorage` under key `demo_session_id`; never logs or renders the value; only sent on AssemblyAI uploads). Modified: services/frontend/app/api/[...path]/route.ts (added a strict `FORWARDED_REQUEST_HEADERS = ["content-type", "x-demo-session-id"]` allowlist; the proxy now forwards exactly those headers when present and never echoes the session header back in responses; arbitrary headers are still not passed through; no logging anywhere in the proxy). Modified: services/frontend/app/globals.css (appended demo-only classes — `.demo-main`, `.demo-header`, `.demo-subtle`, `.dot/.dot-ok/.dot-bad/.dot-unknown`, `.demo-banner/.demo-banner-warn/.demo-banner-info`, `.demo-badge`, `.demo-tabs/.demo-tab/.demo-tab-active`, `.demo-section`, `.demo-form`, `.demo-result`, `.demo-meta`, `.demo-mono`, `.demo-warnings`, `.demo-transcript`; plain CSS, mobile-friendly defaults). Created: tests/demo/test_b11_1a_degradation_id_drift.py (4 tests — backend `DEGRADATION_REGISTRY` matches the expected 5-id set, has exactly 5 entries, the frontend `DEGRADATION_IDS` const lists the same 5 IDs in the same order as the backend, and the `DEGRADATION_LABELS` map covers all 5 IDs; the test parses degradations.ts via regex so future renames/reorderings on either side fail loudly). Untouched: services/frontend/app/page.tsx, services/frontend/app/layout.tsx, services/api/app/demo_main.py, services/api/app/main.py, services/worker/, libs/, scripts/, infra/compose/, .env.demo.example, config/, reports/, pyproject.toml, services/frontend/package.json, services/frontend/package-lock.json. Compose-first validation inside `docker compose -f infra/compose/docker-compose.demo.yml ...` against the demo stack: brought up demo-api + demo-worker, `/demo/health` returned `{"status":"ok","mode":"demo","db_ok":true,"queue_depth":0}`, `/demo/examples` returned ex001 with all 5 degradation IDs, `/demo/providers/assemblyai/status` returned `{"assemblyai":{"state":"disabled","cap_state":"below"}}` (no key configured on this RP5; UI must and does handle "disabled"); ran the affected backend slice in an ephemeral container (`pytest -q tests/demo/{test_demo_api,test_demo_jobs_api,test_b9_2_upload_processing,test_b9_3_language_warning,test_b10_1_usage_ledger,test_b10_2_upload_assemblyai_rules,test_demo_examples,test_demo_examples_config}.py`) → 192 passed + 1 skipped; ran the new drift-guard test in an ephemeral container with `tests/` and `services/frontend/` bind-mounted RO so the freshly-added file and TS constant were visible (`pytest -q tests/demo/test_b11_1a_degradation_id_drift.py`) → 4 passed; ran broad regression with `scripts/`, `tests/`, and `services/frontend/` bind-mounted RO (`pytest -q tests/demo tests/asr tests/audio tests/common`) → 635 passed + 1 skipped (B10.2 baseline 631 + 4 new drift-guard tests). Frontend toolchain validation in a one-shot `node:20-bookworm-slim` container (RP5 host has no Node): `npm ci --no-audit --no-fund` succeeded, `services/frontend/package-lock.json` md5 unchanged before/after; `npm run lint` → "✔ No ESLint warnings or errors"; `npm run typecheck` → exit 0; `npm run build` → "✓ Compiled successfully", routes table shows `○ /` (existing platform UI, untouched), `ƒ /api/[...path]` (proxy), `○ /demo` (new B11.1a route, 4.52 kB, 91.7 kB First Load JS). Headless UI smoke via `next start` against the demo backend (no browser available on RP5): `GET /demo` returned the full HTML with header "ASR Enhancement — Public Demo", backend status dot, mode tabs, the cached-example section with the locked "whisper (cached only)" provider option; `GET /api/demo/health` and `GET /api/demo/providers/assemblyai/status` proxied 1:1; `POST /api/demo/run-cached` returned `cache_miss` for ex001/far_field_room (cache empty on this install — the UI's honest cache_miss message path is correct) and `{"detail":"Example not found"}`/`{"detail":"Degradation not available for this example"}` for the two error paths; uploaded ex001 clean.wav via the proxy → 202 with job_id, polled to `completed` in ~2 s, `/result` returned the expected `result_json` with `provider="whisper"`, `asr_model_version="tiny.en"`, `enhancer_version="bypass"`, `metrics={}`, `warnings=[]`, `raw.transcript="Mr. Quilter is the apostle of the middle classes, and we are glad to welcome his gospel."`; `GET /demo/jobs/{id}` keys verified to NOT include any per-browser hash field (B10.2 public projection intact); `POST /api/demo/upload provider=assemblyai` (no header) returned HTTP 400 with `{"detail":"AssemblyAI is not configured. Use Whisper local instead."}` (state=disabled short-circuits before the session-header check, exactly matching the B10.2 contract); `GET /` still serves the existing platform UI byte-identical to pre-B11.1a (transcribe_only/enhance_and_transcribe selectors); the smoke frontend container was torn down. Visual layout / mobile-viewport / click-through verification is left for Gabriel — RP5 has no browser; the headless smoke confirms HTML structure, proxy wiring, and end-to-end Whisper pipeline. AssemblyAI live UI smoke deferred pending Gabriel spend authorization (per CLAUDE.md §15, §21). A real non-English upload smoke is intentionally not a closure gate (Whisper tiny.en's language detection on real audio is not deterministic enough to trigger the warning branch); B9.3 backend tests already exercise the warnings payload, and the UI renders any `result_json.warnings[].message` as info banners. Privacy and runtime guards run against the working tree before commit: no `ASSEMBLYAI_API_KEY` or key-like literal in the diff or any new file; no protected-field substring in any frontend file (the literals `X-Demo-Session-Id` and `demo_session_id` are required by B10.2 and intentionally allowed); no `console.log/debug/info/warn/error` in any frontend file; no runtime artifacts (`*.wav|.mp3|.m4a|.flac|.db|.sqlite|.log`) in the working tree; no `services/frontend/.env`; no diff at all under `libs/`, `services/api/`, `services/worker/`, `scripts/`, `infra/`, `config/`, `reports/`, `pyproject.toml`, `services/frontend/app/page.tsx`, `services/frontend/app/layout.tsx`; `services/frontend/package.json` and `package-lock.json` untouched (no new deps). Net diff footprint: 4 created frontend modules + 1 created backend test + this tracker pair, 2 modified frontend files (services/frontend/app/api/[...path]/route.ts, services/frontend/app/globals.css). Operational note for future tracker readers: because Dockerfile.demo bakes the source tree into the image and the demo compose only mounts the runtime root + config, the new test and the frontend constant must be bind-mounted into the demo-api container for in-container pytest runs (`-v $REPO/tests:/app/tests:ro -v $REPO/services/frontend:/app/services/frontend:ro`); CI image rebuilds carry them naturally. Advance to B11.1b (Implement audio playback and ground truth UI) per docs/plans/demo_platform_plan.md §33.

- B10.2: done on 2026-05-04. §32 lifts the B9.2 AssemblyAI upload gate behind structured guards, wires `transcribe_with_ledger` into the upload worker, adds a per-session 3-uses-in-24h policy with **leak-proof de-duplicated counting** that includes still-pending queued/running jobs, and preserves every B10.1 privacy contract. Modified: libs/demo/usage.py (promoted user-facing strings into module-level constants `MSG_DISABLED`, `MSG_DAILY_QUOTA_REACHED`, `MSG_HARD_QUOTA_EXHAUSTED`, `MSG_SESSION_LIMIT_REACHED`, `MSG_SESSION_HEADER_REQUIRED`, `MSG_LEDGER_UNAVAILABLE`; rewrote `CapBlockedError` defaults to read from those constants so API and worker render byte-identical text; added `rolling_24h_start_iso(now=None)` helper that returns `(now-24h).isoformat()`; cap vocabulary, provider-state mapping, and `compute_public_view` payload all unchanged from B10.1). Modified: libs/demo/persistence.py (extended `jobs` DDL with nullable `session_id_hash TEXT`; idempotent `init_schema` migration via `PRAGMA table_info(jobs)` + `ALTER TABLE jobs ADD COLUMN session_id_hash TEXT` if absent — pre-B10.2 deployed DBs migrate forward on first start, post-B10.2 fresh DBs get the column directly; extended `try_create_job(...)` signature with `session_id_hash: Optional[str] = None` and the INSERT statement; added three read-only count helpers: `count_session_assemblyai_usage` (ledger rows, `started`/`completed`, in window), `count_pending_session_assemblyai_jobs` (jobs rows, `queued`/`running`, in window), and the de-dup gate `count_effective_session_assemblyai_uses` whose single SQL fetch returns `ledger_count + pending_jobs_count_excluding_already_in_ledger`; usage_ledger DDL untouched, the de-dup correlates on `usage_ledger.job_id`). Modified: libs/demo/processing.py (replaced the AssemblyAI branch in `default_asr_factory` so when `settings.assemblyai_api_key` is set it returns `DemoAssemblyAIAdapter(api_key=settings.assemblyai_api_key)`, else raises `ProviderDisabledError(MSG_DISABLED)`; replaced the unsupported-provider message with `"Unsupported provider {value!r}. Use 'whisper' or 'assemblyai'."`; in `process_upload_job`, when `provider == PROVIDER_ASSEMBLYAI`, routes the raw transcribe call through `transcribe_with_ledger` with `audio_duration_seconds=probe_audio_duration(raw_input_path)` and `session_id_hash=job.get("session_id_hash")`, translating `CapBlockedError(soft|hard)` to `ProcessingOutcome(status="failed", error_message=<canonical cap message>)` and `LedgerWriteError` to `ProcessingOutcome(status="failed", error_message=MSG_LEDGER_UNAVAILABLE)`, never silently switching to Whisper; added bypass-aware second-call optimization: when `enhancement.output_path == raw_input_path` (the BypassEnhancer case), the worker reuses `raw_result` and `raw_latency` for the enhanced block instead of calling `asr.transcribe` a second time, avoiding a duplicate billable AssemblyAI call and a second ledger row per upload, applied to both Whisper and AssemblyAI; introduced `ASSEMBLYAI_MODEL_VERSION = "universal"` and made `_asr_model_version` return the literal string `"universal"` for `PROVIDER_ASSEMBLYAI`). Modified: services/api/app/demo_main.py (added `_ALLOWED_UPLOAD_PROVIDERS = {"whisper","assemblyai"}` and `_PUBLIC_JOB_COLUMNS` whitelist tuple; added `_public_job_view(row)` projection that drops `session_id_hash` and any other non-public column before returning; added `_hash_session_id(session_id)` returning SHA-256 hex; rewrote `POST /demo/upload`: after `validate_and_save_upload`, validates `provider` against the allowed set (HTTP 422 with `"Unsupported provider {!r}. Use 'whisper' or 'assemblyai'."` and unlink the saved file otherwise), then for `provider == "assemblyai"` runs the pre-queue gate in this exact order — (1) `compute_public_view(...)` then `state == disabled` ⇒ HTTP 400 `MSG_DISABLED`, (2) `state == daily_quota_reached` ⇒ HTTP 503 `MSG_DAILY_QUOTA_REACHED`, (3) `state == quota_exhausted` ⇒ HTTP 503 `MSG_HARD_QUOTA_EXHAUSTED`, (4) `X-Demo-Session-Id` header missing/empty ⇒ HTTP 400 `MSG_SESSION_HEADER_REQUIRED`, (5) hash via SHA-256, (6) defense-in-depth `duration <= 30.0` re-check ⇒ HTTP 422 with the existing 30 s message, (7) `count_effective_session_assemblyai_uses(...) >= 3` ⇒ HTTP 429 `MSG_SESSION_LIMIT_REACHED`, (8) persist `session_id_hash` via the new `try_create_job(..., session_id_hash=...)` kwarg; every rejection path unlinks the saved file and creates no `jobs` or `usage_ledger` row; rewrote `GET /demo/jobs/{job_id}` to project through `_public_job_view`; `GET /demo/jobs/{job_id}/result`, `GET /demo/providers/assemblyai/status`, `GET /admin/stats` all byte-identical to B10.1; Whisper path unchanged — no header required, no provider-state check, no ledger interaction, `session_id_hash=None` stored on the job). Modified: tests/demo/test_b9_2_upload_processing.py (flipped `test_rejects_assemblyai_provider_with_b9_2_message` → `test_rejects_assemblyai_provider_when_no_key` so it asserts `MSG_DISABLED` when no key is configured; added sibling `test_assemblyai_factory_returns_demo_adapter_when_key_configured` confirming the factory returns `DemoAssemblyAIAdapter` when the env key is set; updated two pre-B10.2 call-count assertions in `test_no_degradation_bypass_returns_raw_and_enhanced` and `test_known_degradation_writes_file_and_persists_path` from 2 calls to 1 to reflect the bypass optimization; rewrote `test_enhanced_asr_failure_keeps_raw_completed` to use a fake distinct enhancer that writes a separate file because the second-ASR-failure scenario is unreachable through BypassEnhancer post-B10.2). Modified: tests/demo/test_b9_3_language_warning.py (updated `test_warning_only_computed_once_for_completed_job` from `len(asr.calls) == 2` to `== 1` and rewrote `test_warning_present_when_enhanced_path_fails` with a fake distinct enhancer for the same reason). Created: tests/demo/test_b10_2_upload_assemblyai_rules.py (52 tests across 8 sections: provider validation and gate ordering — unsupported provider 422, no-key 400 disabled, soft cap 503, hard cap 503, state-checks-fire-before-session-header for both disabled and hard, missing/empty header 400, Whisper does-not-require-header 202; de-duplicated session count — 0+0 allows, 2+0 allows, 3+0 rejects 429, 2+1 rejects, 0+3 rejects, **1 started ledger + same job_id running counts as 1**, **1 completed + 1 started for running A + A running counts as 2**, **2 completed + 1 started for running A + A running blocks next upload with 429**, queued AssemblyAI sharing job_id with completed ledger counts once, queued Whisper jobs do not count, queued AssemblyAI from another `session_id_hash` does not count, 3 failed ledger rows do not count, 3 completed jobs without ledger rows do not count, 3 ledger rows >24h do not count, boundary at exactly `now-24h` counted, half-helpers behave correctly in isolation, helpers strictly scoped to `session_id_hash`, `count_pending` excludes completed and failed; migration and idempotency — old-style jobs table without `session_id_hash` migrates on first `init_schema`, second `init_schema` is idempotent and does not duplicate the column or raise, `try_create_job` round-trips `session_id_hash="abc"` through `get_job`, fresh DB has the column directly, migrated NULL rows do not match an active session hash; privacy / leak-proofing — `GET /demo/jobs/{id}` body has no `session_id_hash` even after an AssemblyAI upload, `GET /demo/jobs/{id}/result` body (202 path) has no `session_id_hash`, public provider-state remains exactly `{state, cap_state}` with values from the allowed sets, caplog assertion that neither the plain `X-Demo-Session-Id` value nor its hash appears in any log record; worker / `transcribe_with_ledger` integration — exactly one `usage_ledger` row transitions started→completed for an AssemblyAI upload, bypass optimization yields exactly one ledger row, `AdapterTimeoutError` marks the row failed and the job failed, `CapBlockedError(soft_reached)` raised mid-call marks the job failed with `MSG_DAILY_QUOTA_REACHED` and creates no new ledger row, `LedgerWriteError` marks the job failed with `MSG_LEDGER_UNAVAILABLE`, `_asr_model_version` returns `"universal"` for AssemblyAI; result_json shape — top-level keys identical to Whisper, `provider == "assemblyai"`, `asr_model_version == "universal"`, `metrics == {}`, no `session_id`/`session_id_hash`/`ledger_id`/`ground_truth`/`gt`/`reference_text`/`wer`/`cer`/`word_accuracy` at any depth, no mention of `whisper` in the JSON; defense-in-depth — 31 s WAV is rejected by B9.1 422 before the AssemblyAI gate runs; Whisper sanity — Whisper path remains 202, stores `session_id_hash=None`, `GET /demo/jobs/{id}` for a Whisper job has no `session_id_hash` field, factory returns `DemoAssemblyAIAdapter` when key set, factory raises with `MSG_DISABLED` when key absent, factory raises for unknown provider). `DemoAssemblyAIAdapter` constructed with `DemoAssemblyAIAdapter(api_key=settings.assemblyai_api_key)` and no `quota_exhausted=` kwarg; the cap precheck is owned exclusively by `transcribe_with_ledger`. `quota_exhausted` (the public state) is derived from `cap_state` via `derive_provider_state(api_key, cap_state)` — `hard_reached → quota_exhausted`. Audio duration is re-probed inside `process_upload_job` via `probe_audio_duration(raw_input_path)` so the worker is independent of API-stored values. Compose-first validation inside ephemeral `demo-api` containers via `docker compose -f infra/compose/docker-compose.demo.yml ...`: `compose config` exit 0; `compose build` clean; in-container `python -m compileall libs/common/demo_settings.py libs/demo/persistence.py libs/demo/usage.py libs/demo/processing.py services/api/app/demo_main.py tests/demo/test_b10_2_upload_assemblyai_rules.py tests/demo/test_b9_2_upload_processing.py` clean; `pytest tests/demo/test_b10_2_upload_assemblyai_rules.py -q` 52 passed in 6.10 s; adjacent regression `pytest tests/demo tests/asr` (excluding the five script-importing files that need `scripts/` bind-mounted) 392 passed + 1 skipped in 16.73 s; broad regression `pytest tests/demo tests/asr tests/audio tests/common` with `${REPO}/scripts:/app/scripts:ro` 631 passed + 1 skipped in 27.14 s (529 baseline post-B10.1 plus 50 new test rows from B10.1 bring the pre-B10.2 baseline to 579; B10.2 adds 52 new tests and the modified B9.2/B9.3 tests stay green). Live RP5 dry smoke (no AssemblyAI network call): pre-baseline counts jobs=27, usage_ledger=0, cache_entries=30, uploads/ has 5 files; brought up demo-api + demo-worker via `docker compose up -d`; `/demo/health` returned `{"status":"ok","mode":"demo","db_ok":true,"queue_depth":0}`; `/demo/providers/assemblyai/status` returned `{"assemblyai":{"state":"disabled","cap_state":"below"}}` — payload validated by an inline Python walker against the allowed `state` and `cap_state` sets and a forbidden-key set covering `spend`, `total_usd`, `daily_usd`, `reserved_usd`, `daily_soft_cap_usd`, `warning_cap_usd`, `hard_cap_usd`, `usd_per_second_estimate`, `as_of`, `key_configured`, `remaining`, `remaining_usd`, `job_id`, `session_id`, `session_id_hash`, `ledger_id`, `row_count`, `rows`, `count`; generated `/tmp/short.wav` (1.0 s mono 16-bit silence at 16 kHz) via stdlib `wave`; `POST /demo/upload provider=assemblyai` with no `X-Demo-Session-Id` returned HTTP 400 with body `{"detail":"AssemblyAI is not configured. Use Whisper local instead."}` (matches `MSG_DISABLED` byte-for-byte because `state == disabled` short-circuits before the header check); post-call counts unchanged (jobs=27, usage_ledger=0, cache_entries=30, uploads/ unchanged at 5 files); demo-worker logs contained no `assemblyai` or `adapter` lines; brought stack down cleanly with `docker compose down`; removed `/tmp/short.wav` and `/tmp/upload_resp.json`. Live AssemblyAI smoke deliberately not run because Gabriel did not authorize spend in this turn; the FastAPI TestClient suite plus the helper-level dedup tests are the hard gate for cap behavior, session-limit behavior, and worker integration. Protected diff empty (libs/asr/, libs/audio/, libs/common/versions.py, libs/demo/cache.py, libs/demo/cleanup.py, libs/demo/examples.py, libs/demo/upload.py, services/api/app/main.py, services/worker/app/demo_worker_main.py, scripts/, infra/compose/Dockerfile.demo, infra/compose/docker-compose.yml, infra/compose/docker-compose.demo.yml, pyproject.toml, requirements.lock.arm64, requirements.lock.x86_64, config/, reports/, .env.demo.example all untouched). Runtime artifact guard: `git status --short | grep -E '\.(wav|mp3|m4a|flac|db|sqlite|log)$'` returned no matches. Secret guard: `git diff --cached | grep -E "ASSEMBLYAI_API_KEY=[A-Za-z0-9]{10,}"` returned no matches. Footprint matches plan: 4 source modules, 1 new test file, 2 modified test files (B9.2 and B9.3 to reflect the bypass optimization and the new MSG_DISABLED contract), plus this tracker pair. Decision rules from plan §32 mapping: Whisper available by default; AssemblyAI allowed only when (key configured AND `cap_state ∈ {below, warning_reached}` AND duration ≤ 30 s AND `X-Demo-Session-Id` present AND effective_session_uses < 3); AssemblyAI failure (gate, ledger, adapter) ends the job in `failed` with the appropriate canonical message — never auto-fall-back to Whisper; if session tracking is unavailable (header missing) AssemblyAI returns `MSG_SESSION_HEADER_REQUIRED` so the user can pick Whisper. Backend never sees ground truth: no `ground_truth` field added anywhere, no `/demo/score` endpoint, no WER or word_accuracy computed for uploads (`metrics == {}` unchanged); no transcript bodies, GT, original filenames, API keys, plain session IDs, `session_id_hash` values, raw payloads, or ledger IDs in any log line. Public provider-state remains exactly `{assemblyai: {state, cap_state}}`. `/admin/stats` unchanged; B10.1 admin block continues to expose `key_configured`, daily/total/reserved spend, caps, and `as_of` under Basic auth, and never echoes the API key string. SMTP delivery for the warning cap remains deferred to phase B12. Advance to B11.1a (Implement demo modes and selectors) per docs/plans/demo_platform_plan.md §33.

- B10.1: done on 2026-05-04. §32 AssemblyAI cost-control foundation in place: usage_ledger persistence helpers, cap-state policy, atomic reservation flow, and a UI-safe public provider-state endpoint plus an admin-auth detail block. Created: libs/demo/usage.py (vocabulary contract — cap_state ∈ {below, warning_reached, soft_reached, hard_reached}, state ∈ {available, daily_quota_reached, quota_exhausted, disabled}, hard > soft > warning > below; pure helpers `estimate_cost_usd` (TypeError on non-numeric, ValueError on negative duration or rate, 0 for zero duration, 6dp rounding, no silent clamping), `derive_cap_state`, `derive_provider_state` (key-missing always disabled), `assemblyai_quota_exhausted`; `compute_public_view` returns exactly `{assemblyai: {state, cap_state}}`; `compute_admin_view` returns the full block with `state, cap_state, key_configured, daily_usd, total_usd, reserved_usd, daily_soft_cap_usd, warning_cap_usd, hard_cap_usd, usd_per_second_estimate, as_of`; `LedgerWriteError` for usage_ledger DB I/O failures only; `CapBlockedError` for soft_reached/hard_reached only — warning_reached is non-blocking and triggers a single structured log line `assemblyai_warning_cap_projected` with numeric fields only; `transcribe_with_ledger(adapter, audio_path, job_id, *, db_path, settings, audio_duration_seconds, session_id_hash=None, now=None)` opens one SQLite connection, runs `BEGIN IMMEDIATE`, reads completed daily/total + reserved exposure, computes projected_cap_state, blocks soft/hard before INSERT and before adapter call, INSERTs status='started' with cap_state=projected, COMMITs, then calls `adapter.transcribe(...)`, updates row to 'completed' on success or 'failed' on exception, re-raises the original adapter exception; if the post-call status update fails, an accounting error is logged but the original adapter exception is preserved). Modified: libs/demo/persistence.py (added narrow usage_ledger helpers — `insert_usage_ledger`, `update_usage_ledger_status` (no-op when ledger_id absent, leaves cap_state/created_at/estimated_cost_usd intact), `get_usage_ledger_row`, `_sum_usage_cost_in_conn` (in-transaction sum used by reservation), `sum_completed_usage_cost`, `sum_reserved_usage_cost`, `count_usage_rows`; non-finite or negative `estimated_cost_usd` cells are treated as 0 with a single process-lifetime warning, never auto-deleted; existing DDL untouched). Modified: libs/common/demo_settings.py (added `demo_assemblyai_daily_soft_cap_usd: float = 5.0`, `demo_assemblyai_warning_cap_usd: float = 35.0`, `demo_assemblyai_hard_cap_usd: float = 45.0`, `demo_assemblyai_usd_per_second: float = 0.000103`; values labelled in module docstring as estimates, configurable via env). Modified: services/api/app/demo_main.py (added `GET /demo/providers/assemblyai/status` returning `compute_public_view(...)` — exactly two-key payload, no totals/caps/key flag/job/session/row counts; extended `/admin/stats` (Basic auth) with `provider_state` block from `compute_admin_view(...)` — never echoes the API key string). Modified: infra/compose/docker-compose.demo.yml (extended the `x-demo-env` whitelist with `DEMO_ASSEMBLYAI_DAILY_SOFT_CAP_USD: ${...:-5.0}`, `DEMO_ASSEMBLYAI_WARNING_CAP_USD: ${...:-35.0}`, `DEMO_ASSEMBLYAI_HARD_CAP_USD: ${...:-45.0}`, `DEMO_ASSEMBLYAI_USD_PER_SECOND: ${...:-0.000103}` — explicit numeric defaults so empty host envs cannot reach pydantic and break float parsing). Modified: .env.demo.example (added the four new keys commented out so the example file does not overwrite container defaults with empty strings). Created: tests/demo/test_b10_1_usage_ledger.py (50 tests covering schema regression, persistence helper roundtrip + status-only update + provider/status filters + since_iso + non-finite/negative-cost handling, pure-logic helpers including duration-validation contract and cap_state precedence at exact boundaries (4.99/5.0/34.99/35.0/44.99/45.0), provider_state mapping for all key+cap_state combinations, transcribe_with_ledger happy path, soft-cap block, hard-cap block, warning continues with one log line and warning cap_state stamped on the new row, DB failure raises LedgerWriteError without calling adapter, adapter failure marks row failed and re-raises original exception, ValueError/TypeError on bad duration block adapter, CapBlockedError refuses warning/below in constructor; FastAPI TestClient public endpoint disabled/available/soft/hard cases plus exact-key-set assertion that forbids spend totals/caps/remaining/key_configured/as_of/job/session/row counts; admin endpoint 401 without auth, expected admin keys present with auth, secret API key never echoed in response body even when set via env). Reservation behavior: B10.1 ships the helper but no production code path invokes it on a live adapter. The B9.2 AssemblyAI upload gate stays in place; `process_upload_job` is unchanged; the worker is unchanged; cleanup is unchanged; cache scripts are unchanged; `libs/asr` is unchanged. SMTP delivery for the warning cap is deferred to phase B12 (observability); B10.1 emits one structured log per warning crossing. Compose-first validation inside ephemeral demo-api containers: `compose config` exit 0; `compose build` clean; `python -m compileall libs/common/demo_settings.py libs/demo/persistence.py libs/demo/usage.py services/api/app/demo_main.py` clean; new test file syntax-checked via `ast.parse`; in-container `DemoSettings()` prints 5.0/35.0/45.0/0.000103 with all four asserts passing (compose env propagation verified end-to-end); `pytest tests/demo/test_b10_1_usage_ledger.py` 50 passed; adjacent suites `test_persistence.py + test_demo_settings.py + test_demo_api.py + test_demo_jobs_api.py + test_b9_2_upload_processing.py + test_b9_4_cleanup_uploads.py + test_demo_assemblyai_provider.py + test_assemblyai_provider.py` 160 passed; broad regression `tests/demo + tests/asr + tests/audio + tests/common` 579 passed (529 baseline + 50 new). Live RP5 smoke: pre-baseline counts usage_ledger=0, jobs=27, cache_entries=30; brought up demo-api + demo-worker; `/demo/health` ok; `/demo/providers/assemblyai/status` returned `{"assemblyai":{"state":"disabled","cap_state":"below"}}`; payload validated by Python script — exactly two keys at each level, state ∈ allowed set, cap_state ∈ allowed set, no spend/caps/remaining/key flag/job/session/row counts present; post counts unchanged (usage_ledger=0, jobs=27, cache_entries=30); demo-worker did not initiate any AssemblyAI network call; brought stack down cleanly. /admin/stats live smoke deliberately not run as a hard blocker because it depends on `ADMIN_STATS_PASSWORD` from the operator's `.env.demo`; the test suite is the gate for the admin block. Protected diff empty (libs/asr/, libs/audio/, libs/common/versions.py, libs/demo/cache.py, libs/demo/cleanup.py, libs/demo/processing.py, libs/demo/upload.py, libs/demo/examples.py, services/worker/, services/api/app/main.py, scripts/, infra/compose/Dockerfile.demo, infra/compose/docker-compose.yml, pyproject.toml, requirements.lock.arm64, requirements.lock.x86_64, config/, reports/ all untouched). Runtime artifact guard: `git status --short | grep -E '\.(wav|mp3|m4a|flac|db|sqlite|log)$'` returned no matches. Secret guard: `git diff --cached | grep -E "ASSEMBLYAI_API_KEY=[A-Za-z0-9]{10,}"` returned no matches. Footprint matches plan exactly: 7 files (libs/common/demo_settings.py, libs/demo/persistence.py, libs/demo/usage.py, services/api/app/demo_main.py, infra/compose/docker-compose.demo.yml, .env.demo.example, tests/demo/test_b10_1_usage_ledger.py) plus this tracker pair. Decision rules from plan §32 mapping: soft cap → CapBlockedError(soft_reached) before adapter call → state=daily_quota_reached; warning cap → single structured log + cap_state=warning_reached on new row, non-blocking → state=available; hard cap → CapBlockedError(hard_reached) before adapter call → state=quota_exhausted; ledger write failure → LedgerWriteError before adapter call. B10.1 deliberately does not lift the B9.2 AssemblyAI upload gate, does not wire transcribe_with_ledger into process_upload_job, and does not invoke AssemblyAI live — those steps belong to B10.2. Advance to B10.2 (Enforce upload-specific AssemblyAI rules) per docs/plans/demo_platform_plan.md §32.

- B9.3: done on 2026-05-03. §31 non-English language warning emitted from upload result_json. Modified: libs/demo/processing.py (added module constants `NON_ENGLISH_LANGUAGE_THRESHOLD = 0.5`, `NON_ENGLISH_WARNING_MESSAGE = "Detected language is not English. This demo is designed for English speech, so results may be unreliable. Continue?"` — byte-for-byte the canonical text from CLAUDE.md §16 — and `NON_ENGLISH_WARNING_CODE = "non_english_language"`; added pure helper `_language_warning(asr_result) -> Optional[dict]` that returns `None` unless `asr_result.language` is a non-empty string different from `"en"` AND `_language_probability(asr_result)` returns a numeric value strictly greater than the threshold, in which case it returns the structured payload `{"code": "non_english_language", "message": NON_ENGLISH_WARNING_MESSAGE, "detected_language": <language>, "language_probability": <float>}`; in `process_upload_job`, after the raw ASR succeeds and the `result_payload` dict is constructed with `"warnings": []`, computes the warning once from `raw_result` and replaces the empty list with `[warning]` when the helper returns one — enhanced ASR is not consulted because both passes run on the same audio and would yield the same language; failed-raw jobs return `result is None` so no warnings surface there; the existing `default_asr_factory`, `default_enhancer_factory`, AssemblyAI gate, MetricGAN+ gate, missing-input handling, decision-rule-3 enhanced-failure handling, log lines, and 240-char `_safe_message` truncation are all byte-identical). Created: tests/demo/test_b9_3_language_warning.py (18 unit tests via pytest parametrization on a `_FakeASR` mock — direct unit tests on `_language_warning` cover English / above-threshold / missing-probability / non-numeric-probability / missing-or-blank-language; end-to-end `process_upload_job` tests cover warning emitted for non-English above 0.5, no warning for English regardless of probability, parametrized boundary [0.0, 0.1, 0.49, 0.5] all yielding `warnings == []`, no warning when probability missing, no warning when language missing, warning emitted independent of `degradation_id="muffled"`, warning still present when enhanced ASR raises (status stays `completed`, `enhanced is None`, `enhanced_error` populated), warning computed only once even though raw + enhanced both ran ASR, failed raw ASR returns `result is None` so no warnings field exists, canonical message text matches byte-for-byte; no real faster-whisper, no real audio content beyond a 0.5 s 16 kHz sine tone, no AssemblyAI, no GT — `_FakeASR.text` is a neutral `"hello world"` placeholder, never a transcript of the synthetic input). Extended: tests/demo/test_demo_jobs_api.py (one new assertion `assert isinstance(body["result"]["warnings"], list)` appended to the existing `test_get_job_result_200_for_completed_upload_b9_2_schema` test, confirming the `warnings` array round-trips through `/demo/jobs/{id}/result` without API changes). The warning lives at the top level of `result_json` and is returned verbatim by the existing `/demo/jobs/{id}/result` endpoint; no API code, no schema migration, no new endpoint, no frontend, no ASR provider modification, no cache touch, no dependency, no lockfile, no Dockerfile, no compose change. Backend never sees ground truth: no `ground_truth` field added anywhere, no `/demo/score` endpoint, no WER or word_accuracy computation for uploads (`metrics == {}` unchanged). Compose-first validation inside ephemeral `demo-api` containers via `docker compose -f infra/compose/docker-compose.demo.yml run --rm demo-api ...`: `docker compose build` clean; `python -m compileall libs/demo/processing.py tests/demo/test_b9_3_language_warning.py` clean; `pytest tests/demo/test_b9_3_language_warning.py -q` 18/18 passed in 2.53 s; `pytest tests/demo/test_b9_2_upload_processing.py tests/demo/test_b9_1_upload_validation.py tests/demo/test_demo_jobs_api.py tests/asr/test_whisper_provider.py -q` 66/66 passed in 5.46 s; `pytest tests/demo tests/audio tests/asr tests/common -q` reports 462 passed + 47 errors solely from the five script-importing test files (`test_b8_1_cache_scripts.py`, `test_b6_5_2_enhancer.py`, `test_b6_5_1_runner.py`, `test_b6_5_1_compare.py`, `test_demo_examples_config.py` / `test_demo_candidates.py` are absent here because the run was performed without bind-mounting `${REPO}/scripts:/app/scripts:ro` — pre-existing demo image limitation noted in B9.2 hand-off, unrelated to B9.3); excluding those five script-importing files yields 430/430 passed (412 baseline post-B9.2 in this no-bind-mount mode + 18 new B9.3 tests). Live smoke explicitly skipped per the plan because Whisper tiny.en is forced to English in production via `model.transcribe(..., language="en")` so real non-English detection cannot fire end-to-end; the production warning path is exercised by mocked unit tests only, and no non-English audio fixture was uploaded or committed. Protected diff empty (libs/asr/, libs/audio/, libs/common/, libs/demo/cache.py, libs/demo/examples.py, libs/demo/persistence.py, libs/demo/upload.py, services/api/app/demo_main.py, services/worker/app/demo_worker_main.py, services/frontend/, scripts/, config/, reports/, pyproject.toml, requirements.lock.arm64, requirements.lock.x86_64, infra/compose/Dockerfile.demo, infra/compose/docker-compose.demo.yml all untouched). Audio guard: `git status --short | grep -E '\.(wav|flac|mp3)$'` returned no matches — no audio files in git. Footprint matches plan: 3 files changed (libs/demo/processing.py +33 lines, tests/demo/test_demo_jobs_api.py +1 line, new tests/demo/test_b9_3_language_warning.py). Tiny.en limitation: production adapter forces `language="en"`, so `info.language` always returns `"en"` and live non-English detection through `WhisperAdapter` cannot fire; enabling real detection (e.g., switching to multilingual `tiny` for the detect step) is intentionally out of B9.3 scope and contradicts CLAUDE.md §12 ("faster-whisper tiny.en for short demo audio"); the warning contract is in place for future adapters or a separate detect step. Threshold rule: warning emitted iff language is non-empty, language != "en", language_probability is numeric, and probability > 0.5 (strict inequality, matches the existing `_NOT_ENGLISH_THRESHOLD` in `libs/asr/whisper_provider.py`); plan §B9.3 decision rule 3 (no false warning when probability is unavailable) enforced by the numeric-only gate in `_language_probability`. Warning shape: structured dict with `code`, `message`, `detected_language`, `language_probability` for forward-compatible UI rendering in B11. Advance to B9.4 (Implement cleanup job) per docs/plans/demo_platform_plan.md §31.

- B9.2: done on 2026-05-03. §31 worker-only upload processing flow wired end-to-end with faster-whisper tiny.en + BypassEnhancer; backend never sees ground truth (frontend-only / browser-session GT contract). Created: libs/demo/processing.py (`process_upload_job(job, settings, *, asr_factory, enhancer_factory, update_artifacts) -> ProcessingOutcome` per-job pipeline that resolves provider/enhancer/degradation from the jobs row, fails fast with `ProcessingOutcome(status="failed", error_message="Uploaded audio file is missing.")` when `input_artifact_path` is missing or the file is gone, rejects unsupported enhancers up front (`metricgan_plus_pretrained` returns the T4.1 message, anything other than `bypass` returns `Unsupported enhancer`), gates AssemblyAI for uploads with the explicit B9.2 message and never silently falls back to Whisper, creates a per-job artifact directory `<demo_artifacts_dir>/jobs/<job_id>/` to avoid filename collisions when `apply_degradation` writes `<degradation_id>.wav`, persists `degraded_artifact_path` immediately via the injected `update_artifacts` callback, runs raw ASR on the degraded audio (or directly on the upload when `degradation_id` is None), runs enhanced ASR via `enhancer.enhance(...)` then `asr.transcribe(enhanced_output)`, captures any enhanced-path failure as `enhanced_error` and keeps the job `completed` with raw + safe enhanced message per Decision rule 3, fails the job only when raw ASR raises, and emits structured INFO logs (`job=<id> provider=… enhancer=… degradation=… start`, `raw_asr=succeeded latency=… language=…`, `enhanced_asr=succeeded latency=… preset=… enhanced_flag=… fallback=…`, `result=completed`) that never include transcripts, ground truth, or original upload filenames; module also exposes `default_asr_factory(settings)` returning `WhisperAdapter()` for `whisper` and raising `ProviderDisabledError` for `assemblyai` / unknown providers, `default_enhancer_factory(settings)` returning `BypassEnhancer()` for `bypass` and raising `EnhancerNotSupportedError` for `metricgan_plus_pretrained` / unknown values, plus `_safe_message` truncation helper that caps error strings at 240 chars to avoid leaking large diagnostic blobs). Modified: services/worker/app/demo_worker_main.py (replaced the B3.2 stub `update_job_status(..., "failed", error_message="processing not yet implemented (B5)")` with the real loop: kept signal handling, `ensure_runtime_dirs`, `init_schema`, single-worker concurrency, 1 s poll, and the original log lines; for each claimed job constructs an `update_artifacts` closure bound to the `job_id`, calls `process_upload_job` with the default factories, translates the outcome to a single `update_job_status(..., "completed", result_json=json.dumps(outcome.result, ensure_ascii=False))` or `update_job_status(..., "failed", error_message=outcome.error_message)`, and on any unexpected exception logs via `log.exception` and marks the job failed with `error_message=f"Worker error: {type(exc).__name__}."` so the worker never dies on a single bad job). Modified: libs/demo/persistence.py (added narrow `update_job_artifacts(db_path, job_id, *, degraded_artifact_path=None, enhanced_artifact_path=None) -> None` helper that builds a parameterized partial UPDATE over only the non-None columns plus `updated_at`; both arguments None is a no-op so the worker can call it unconditionally without spuriously bumping the timestamp; existing `try_create_job`, `claim_next_job`, `update_job_status`, `get_job`, schema/DDL all byte-identical). Created: tests/demo/test_b9_2_upload_processing.py (16 unit + integration tests using a `_FakeASR` returning fixed deterministic `ASRResult` and the real `BypassEnhancer` + real `apply_degradation` against tiny 0.5 s 16 kHz sine-tone WAV fixtures, no faster-whisper, no AssemblyAI, no network: `test_no_degradation_bypass_returns_raw_and_enhanced` asserts `degraded_audio_path is None`, `enhanced_audio_path is None`, both transcripts equal, `metrics == {}`, `warnings == []`, two ASR calls on the same upload path, no `update_artifacts` calls; `test_known_degradation_writes_file_and_persists_path` asserts `<artifacts>/jobs/<id>/muffled.wav` exists, `degradation_version` filled from `libs.audio.degradations`, both ASR calls happen on the degraded audio, single `update_artifacts(degraded_artifact_path=...)` call; `test_unknown_degradation_id_fails_clean` returns `failed` with `Unknown degradation_id` and writes nothing; `test_missing_input_file_fails` and `test_blank_input_path_fails` both return `failed("Uploaded audio file is missing.")`; `test_rejects_metricgan_enhancer_with_t41_message` and `test_rejects_unknown_enhancer` exercise the default enhancer factory; `test_rejects_assemblyai_provider_with_b9_2_message` and `test_rejects_unknown_provider` exercise the default ASR factory; `test_raw_asr_failure_marks_job_failed` flips `_FakeASR.raise_on_call=1` and asserts `failed` with `Raw ASR failed:` prefix; `test_enhanced_asr_failure_keeps_raw_completed` flips `raise_on_call=2` and asserts the job stays `completed` with `enhanced is None` and `enhanced_error.startswith("Enhanced path unavailable")`; `test_enhancer_raise_keeps_raw_completed` uses a `_BoomEnhancer` that raises in `enhance()` and asserts the job still completes with raw transcript and enhanced_error set, and that only one ASR call happened; `test_result_json_schema_top_level_keys` asserts the exact 15 top-level keys, no `ground_truth` substring anywhere in the serialized JSON, `metrics == {}`, `warnings == []`; `test_logs_do_not_contain_transcripts_or_filenames` runs with a `secret_transcript` fixture, captures all `demo-worker.processing` INFO records via `caplog`, and asserts the transcript bytes, the `u9.wav` filename, and the strings `ground_truth` / `ground truth` are absent; `test_update_job_artifacts_writes_only_provided_columns` asserts partial-column update behavior; `test_update_job_artifacts_noop_when_both_none` asserts the no-op + unchanged-timestamp semantics). Extended: tests/demo/test_demo_jobs_api.py (1 new TestClient test `test_get_job_result_200_for_completed_upload_b9_2_schema` that inserts a synthetic completed upload row with the full B9.2 result_json payload and asserts `GET /demo/jobs/{id}/result` returns it verbatim, no `ground_truth` anywhere in the response, `metrics == {}`; the existing 17 jobs-API tests left untouched). Compose-first validation inside the demo container via `docker compose -f infra/compose/docker-compose.demo.yml run --rm demo-api ...`: image rebuilt cleanly; `python -m compileall services/worker/app/demo_worker_main.py libs/demo/processing.py libs/demo/persistence.py` clean; `pytest tests/demo/test_b9_2_upload_processing.py -q` 16/16 passed in 2.92 s; `pytest tests/demo/test_persistence.py tests/demo/test_b9_1_upload_validation.py tests/demo/test_demo_jobs_api.py tests/demo/test_demo_upload_helper.py -q` 73/73 passed; `pytest tests/demo tests/audio tests/asr tests/common -q` (with `${REPO}/scripts:/app/scripts:ro`, `${REPO}/config:/app/config:ro`, `${REPO}/reports:/app/reports:ro` mounted only because `Dockerfile.demo` does not COPY scripts/, a pre-existing condition unrelated to B9.2 that errors out the script-loading test files at fixture setup) 491/491 passed (474 baseline post-B9.1 + 16 new B9.2 + 1 new jobs-API schema assertion). Live smoke against running demo-api + demo-worker on `http://127.0.0.1:8001`: brought up via `docker compose up -d demo-api demo-worker` after `compose config` clean; (1) clean upload of `/home/gbibbo/asr_enhancement_runtime/artifacts/examples/ex001/clean.wav` returned `{"job_id":"d386f77e-ca1b-4ce5-8329-c414359e399c","status":"queued"}`, transitioned to `completed` within ~7 s on RP5 (Whisper model already cached on host), `result_json` matched the B9.2 schema exactly: `source_type=upload`, `provider=whisper`, `asr_model_version=tiny.en`, `enhancer_version=bypass`, `degradation_id=null`, `degradation_applied=false`, `degraded_audio_path=null`, `enhanced_audio_path=null`, `raw.transcript="Mr. Quilter is the apostle of the middle classes, and we are glad to welcome his gospel."`, `raw.language=en`, `raw.language_probability=1.0`, `raw.latency_seconds=5.301`, `enhanced.transcript` equal to raw, `enhanced.preset_applied=bypass`, `enhanced.enhanced_flag=false`, `enhanced.enhancement_fallback=false`, `enhanced.latency_seconds=1.429`, `enhanced_error=null`, `metrics={}`, `warnings=[]`; (2) muffled-degradation upload of the same file returned job_id `505a8d19-…`, completed in ~5 s, wrote `/home/gbibbo/asr_enhancement_runtime/artifacts/jobs/505a8d19-0915-413f-b600-26970415b03c/muffled.wav` (187,404 bytes), `degraded_audio_path` populated to that absolute path, `degradation_version="degradation_v1"`, `degradation_applied=true`, both transcripts produced (`Mr. Crowder is the apostle …` showing the muffled filter altered ASR output), schema otherwise identical; (3) AssemblyAI upload returned `failed` with exact message `"AssemblyAI is not enabled for uploads in B9.2. Switch provider to 'whisper'."`; (4) MetricGAN+ upload returned `failed` with exact message `"Enhancer 'metricgan_plus_pretrained' is owned by training task T4.1 and is not enabled in the demo runtime. Use 'bypass'."`; (5) unknown-degradation upload returned `failed` with `"Unknown degradation_id: 'nonexistent'."`. Privacy verification: `docker compose logs demo-worker | grep -iE 'ground[_ ]?truth|quilter|crowder|apostle|gospel|middle classes'` returned no matches → no transcript bodies, no GT in worker logs; `grep -E 'clean\.wav|ex001'` returned no matches → no original upload filename in worker logs (saved files use uuid hex names so even paths logged would not leak filenames, but the worker also does not log paths). Services brought down cleanly with `docker compose down`. Protected diff empty (libs/common/versions.py, libs/audio/, libs/asr/, libs/demo/cache.py, libs/demo/examples.py, libs/demo/upload.py, services/api/app/demo_main.py, services/frontend/, scripts/, config/, reports/, pyproject.toml, requirements.lock.arm64, requirements.lock.x86_64, infra/compose/Dockerfile.demo, infra/compose/docker-compose.demo.yml all untouched). Audio guard: `git status --short | grep -E '\.(wav|flac|mp3)$'` returned no matches — no audio files in git. Scope locks honored: backend never sees GT; no `/demo/score` endpoint; no `ground_truth` field added to `/demo/upload`; no GT column added to `jobs`; no WER or word_accuracy computed for uploads (`metrics={}`); no GT logging anywhere; no API/frontend/schema/cache/dependency/infra changes; AssemblyAI gated, no fallback; MetricGAN+ rejected pending T4.1; no cleanup of uploads or per-job artifacts (deferred to B9.4); cache_entries not read or written for uploads (curated cache stays disjoint per CLAUDE.md §16 rule 6); no non-English warning logic (deferred to B9.3). Advance to B9.3 (Add non-English warning) per docs/plans/demo_platform_plan.md §31.

- B9.1: done on 2026-05-03. §31 backend-only upload validation for POST /demo/upload. Modified: libs/demo/upload.py (added `InvalidAudioError`; added `probe_audio_duration(path: Path) -> float` that calls `soundfile.info(str(path))` to read the container header without decoding samples and raises `InvalidAudioError("Audio could not be decoded.")` on any header read failure or non-positive samplerate; added async `validate_and_save_upload(upload, upload_dir, limit_bytes, max_duration_seconds) -> tuple[Path, str, float]` that calls `save_upload` first, then probes the saved file, unlinks the saved file on probe failure or when duration exceeds the cap, and raises `InvalidAudioError("Audio exceeds 30 seconds maximum.")` for over-length audio; existing `save_upload`, `UnsupportedExtensionError`, `FileTooLargeError`, and `DEMO_ALLOWED_EXTENSIONS` left unchanged). Modified: services/api/app/demo_main.py (POST /demo/upload now calls `validate_and_save_upload(file, settings.demo_upload_dir, settings.demo_upload_limit_bytes, settings.demo_upload_max_duration_seconds)` and maps `InvalidAudioError` to HTTP 422 with the exception message as the detail; existing 415 / 413 / 503 / 202 mappings byte-for-byte unchanged; validation runs strictly before `try_create_job`, so the queue is never charged for 415/413/422 paths and the saved file is unlinked before any 4xx response; queue-full path still unlinks and returns 503 with the same body). Created: tests/demo/test_b9_1_upload_validation.py (13 helper-level tests covering `probe_audio_duration` on valid 1 s and 30 s WAVs, corrupt audio, random bytes with a `.wav` extension, empty file; `validate_and_save_upload` accepting a 1 s WAV and exactly 30 s WAV, rejecting just-over-30 s and 31 s WAVs as `InvalidAudioError("Audio exceeds 30 seconds maximum.")` and corrupt audio as `InvalidAudioError("Audio could not be decoded.")`, unlinking the saved file on both rejection paths; and a back-compat assertion that `save_upload` alone still does not probe duration). Extended: tests/demo/test_demo_jobs_api.py (4 new TestClient tests against POST /demo/upload: corrupt-audio bytes return 422 with `could not be decoded` detail, 31 s WAV returns 422 with `exceeds 30 seconds` detail, 422 path leaves no file in `demo_upload_dir`, queue depth in `jobs` is unchanged after a 422; existing 5 upload tests — 202, `input_artifact_path`, 413, 415, 503-without-orphan — left untouched). Extended: tests/demo/test_demo_upload_helper.py (1 back-compat assertion that `save_upload` still saves random bytes with `.wav` extension successfully — content decodability is the responsibility of `validate_and_save_upload`). HTTP mapping after B9.1: unsupported extension → 415 "Unsupported file type." (existing); file too large → 413 "File too large." (existing); undecodable audio header → 422 "Audio could not be decoded." (new); duration > 30 s → 422 "Audio exceeds 30 seconds maximum." (new); queue full → 503 "Queue full. Try again later." (existing); success → 202 `{"job_id": ..., "status": "queued"}` (existing). Validation runs before `try_create_job` in every code path. Compose-first validation inside ephemeral `demo-api` containers via `docker compose -f infra/compose/docker-compose.demo.yml run --rm demo-api ...` (with `${REPO}/scripts:/app/scripts:ro` mounted only for the cache regression run, since `Dockerfile.demo` does not copy `scripts/`): demo image rebuilt cleanly; `python -m compileall libs/demo/upload.py services/api/app/demo_main.py tests/demo/test_b9_1_upload_validation.py` clean; `pytest tests/demo/test_b9_1_upload_validation.py -q` 13 passed; `pytest tests/demo/test_demo_upload_helper.py tests/demo/test_demo_jobs_api.py -q` 32 passed; `pytest tests/demo/test_cache_key.py tests/demo/test_persistence.py tests/demo/test_b8_1_cache_scripts.py -q` 55 passed; `pytest tests/demo tests/audio tests/asr tests/common -q` 474 passed (456 baseline post-B8.1 + 18 new B9.1 tests). Protected diff empty (libs/common/versions.py, libs/audio/, libs/asr/, libs/demo/cache.py, libs/demo/examples.py, libs/demo/persistence.py, services/worker/, services/api/app/upload_validation.py, services/api/app/main.py, services/frontend/, scripts/, config/, reports/, pyproject.toml, requirements.lock.arm64, requirements.lock.x86_64, infra/compose/Dockerfile.demo, infra/compose/docker-compose.demo.yml all untouched). Audio guard: `git status --short | grep -E '\.(wav|flac|mp3)$'` returned no matches — no audio files in git. B9.1 explicitly does not change the frontend, the worker, the job or cache schema, persistence helpers, ASR providers, the enhancer, the cache key contract, or any cache_entries row; SHA256, deduplication, cleanup, processing flow, and the non-English warning are reserved for B9.2 / B9.3 / B9.4. No dependency added; `soundfile` was already pinned in `requirements.lock.arm64` (transitive via `faster-whisper`). Advance to B9.2 (Implement upload processing flow) per docs/plans/demo_platform_plan.md §31.

- B8.1: done on 2026-05-03. §30 Reproducible cache scaffolded with three top-level CLI scripts that read from the existing B6.5.1 baseline report and never run live ASR or contact any network service. Created: scripts/prewarm_cache.py (CLI: --examples, --artifacts-root, --baseline-report, --db, --asr-provider, --asr-model, --enhancer, --source, --dry-run; rejects --asr-provider assemblyai with exit 2 and message "AssemblyAI cache prewarm is deferred to a later quota-aware task."; rejects --source run with exit 2 and message pointing back to --source reports; rejects any --enhancer other than bypass with exit 2 and a T4.1 reference; reads the planned (example_id, variant) set from config/demo_examples.json including the implicit clean variant; for each pair looks up reports/demo/b6_5_1_rp5_results.json by (example_id, degradation_id), recomputes audio_sha256 from disk and aborts the entire run with exit 1 if any pair drifts from the baseline; computes cache_key via libs.demo.cache.build_cache_key with explicit enhancer_version="bypass"; stores artifact_root as the absolute artifacts root and result_json.audio_path_relative including the example_id directory; upserts via libs.demo.persistence.upsert_cache_entry; writes nothing under --dry-run). Created: scripts/validate_cache.py (CLI: --db, --artifacts-root, --report-json, --report-md, --strict, --mark-validated; per-row outcomes ok / stale_version / key_mismatch / missing_artifact / sha256_mismatch / bad_payload; checks stale_version before key_mismatch so version drift surfaces correctly; resolves audio as Path(row["artifact_root"]) / payload["audio_path_relative"] and asserts containment + sha256; writes JSON+MD report; --mark-validated only updates validated_at on ok rows; --strict exits 1 if any non-ok row exists). Created: scripts/invalidate_cache.py (CLI: --db, --by-degradation-version, --by-metrics-version, --by-enhancer-version, --by-asr-provider, --by-asr-model-version, --by-example-id (repeatable), --mode {match,not-match}, --apply, --quiet; refuses to run with no filter; default is dry-run; never touches audio files; transactional DELETE only with --apply). Modified: libs/demo/persistence.py (added narrow cache_entries helpers: upsert_cache_entry returning "inserted" or "updated", list_cache_entries, delete_cache_entries with where_sql/params/dry_run, mark_cache_entry_validated; existing get_cache_entry, init_schema, and DDL untouched). Created: tests/demo/test_b8_1_cache_scripts.py (20 tests covering prewarm row count and key/columns invariants, --dry-run zero-write, sha mismatch abort, missing-in-report skip, AssemblyAI/--source-run/MetricGAN+ deferral guards, artifact_root contract — value equals the artifacts root and audio_path_relative includes the example_id directory; validate ok/stale_version/missing_artifact/sha256_mismatch outcomes, --strict exit, --mark-validated only on ok; invalidate refuse-without-filter, dry-run no-op, --apply removes only matching rows, audio files survive deletion, --mode not-match; persistence helpers roundtrip including ValueError on empty where clause). Generated: reports/demo/b8_1_cache_validation.json and .md (host=ede511dfa6a0, generated_at=2026-05-03T..., db_path=/home/gbibbo/asr_enhancement_runtime/db/demo.db, artifacts_root=/home/gbibbo/asr_enhancement_runtime/artifacts/examples, versions snapshot DEGRADATION_VERSION=degradation_v1 / METRICS_VERSION=1.0 / DEFAULT_ENHANCER_VERSION=1.0, total_rows=30, outcomes ok=30 stale_version=0 key_mismatch=0 missing_artifact=0 sha256_mismatch=0 bad_payload=0). Validation inside the demo container: container syntax check (ast.parse) clean for all four files; 20/20 new B8.1 tests pass; tests/demo/test_cache_key.py + tests/demo/test_persistence.py pass alongside (55/55 combined); 456/456 targeted regression tests pass (tests/demo + tests/audio + tests/asr + tests/common = 436 pre-B8.1 baseline + 20 new). Real RP5 SQLite end-to-end inside ephemeral demo-api containers: prewarm wrote 30 rows (inserted=30, updated=0, missing_in_report=0, sha_mismatch=0); validate --mark-validated --strict reported ok=30 with zeros across stale_version/key_mismatch/missing_artifact/sha256_mismatch/bad_payload and exit 0; dry-run invalidate --by-degradation-version degradation_v0 --mode match matched 0 rows. Protected diff empty (libs/common/versions.py, libs/audio/, libs/demo/cache.py, libs/demo/examples.py, libs/asr/, services/, config/, reports/demo/b6_5_1_rp5_results.json, reports/demo/b6_5_1_comparison.json, reports/demo/b6_5_2_enhancer_validation.json, reports/demo/b6_5_1_comparison.md, reports/demo/b6_5_2_enhancer_validation.md, pyproject.toml, requirements.lock.arm64, requirements.lock.x86_64, infra/compose/Dockerfile.demo, infra/compose/docker-compose.demo.yml all untouched). No audio files in git status. AssemblyAI prewarm and --source run remain explicitly deferred to a future quota-aware task; usage_ledger is not read or written by B8.1. Validation commands used `docker compose -f infra/compose/docker-compose.demo.yml run --rm` with `${REPO}/scripts:/app/scripts:ro` for all script invocations and `${REPO}/reports:/out/reports` for the validate run that wrote b8_1_cache_validation.json/.md back to the host filesystem. Advance to B9.1 (Implement upload validation) per docs/plans/demo_platform_plan.md §31.

- B6.5.2: done on 2026-05-03. §29 RP5 enhancer behavior validation closed with decision `default_enhancer="bypass"`. Created: scripts/demo/run_enhancer_validation.py (CLI runner: `--examples`, `--candidates`, `--artifacts`, `--baseline-rp5`, `--output`, `--md`, `--enhanced-output-root`; per (example_id, degradation_id) pair calls `BypassEnhancer().enhance()`, hashes both input and reported output, transcribes the enhanced output via faster-whisper tiny.en `WhisperAdapter`, looks up the matching B6.5.1 row by (example_id, degradation_id), computes `delta_wa_vs_baseline` to 6 decimals; for `MetricGANPlusEnhancer` makes a single probe call on `ex001/clean.wav` and records `status="not_implemented"` + `error_class` + `error_message`; emits both JSON and Markdown in one invocation; exits 1 if any bypass row breaks strict tolerance, exits 2 if MetricGAN+ does not raise NotImplementedError; reads versions dynamically from libs.common.versions; opens the B6.5.1 baseline read-only and never modifies it). Created: tests/demo/test_b6_5_2_enhancer.py (9 tests covering the 30-row contract + frozen-version stamps, per-row `input_equals_output`/sha equality/preset/`enhanced_flag`/`enhancement_fallback`/`delta_wa_vs_baseline==0.0`, MetricGAN+ probe records `not_implemented` + `NotImplementedError` + T4.1 reference, decision section pins `default_enhancer="bypass"` and `blocked_on_upstream_task="T4.1 in feature/training-datamove1-v1"`, `main()` returns 0 on success and writes both JSON+MD, `main()` returns 1 when a single perturbed baseline row breaks strict tolerance and the offending row appears in `enhancers.bypass.offenders`, baseline file is opened read-only and is byte-stable across runs, `_resolve_variant_path` for clean+degraded, `_baseline_index` ignores rows with missing keys; all tests load the runner via importlib.util.spec_from_file_location). Generated: reports/demo/b6_5_2_enhancer_validation.json (host=ede511dfa6a0, generated_at=2026-05-03T02:01:43Z, asr_engine=faster-whisper, asr_model=tiny.en, degradation_version=degradation_v1, metrics_version=1.0, default_enhancer_version=1.0, scope=[ex001,ex003,ex004,ex007,ex010], variant_ids=[clean,far_field_room,cafe_background,phone_call,muffled,broadband_hiss], baseline_rp5_path=/out/reports/demo/b6_5_1_rp5_results.json with baseline_rp5_generated_at=2026-05-03T00:58:12Z); BypassEnhancer aggregates: n_rows=30, n_input_equals_output=30, n_rows_with_nonzero_delta=0, max_abs_delta_wa_vs_baseline=0.0, mean_delta_wa_vs_baseline=0.0, mean_enhance_latency_seconds=1.1e-05, p95_enhance_latency_seconds=1.4e-05, mean_asr_latency_seconds=1.76236, p95_asr_latency_seconds=1.950359; MetricGANPlusEnhancer probe: status=not_implemented, error_class=NotImplementedError, error_message="MetricGAN+ implementation is owned by task T4.1 in the training branch. Use BypassEnhancer until T4.1 is merged into demo-rp5-v1." Decision: default_enhancer=bypass, ui_label_for_bypass="honest passthrough", ui_label_for_metricgan_plus="unavailable", blocked_on_upstream_task="T4.1 in feature/training-datamove1-v1". Generated: reports/demo/b6_5_2_enhancer_validation.md (mirrors the report contents in human-readable form; explicit footnote that DSP presets `light_clean`, `denoise`, `denoise_dereverb` are out of scope for B6.5.2 because they are pipeline presets in `apply_preset()` rather than `EnhancerAdapter` subclasses, and that real MetricGAN+ inference depends on T4.1). Done-when criteria: (1) RP5 enhancer path "works or fails explicitly" satisfied — bypass path works honestly with byte-identical input/output and zero delta on every row; MetricGAN+ path fails explicitly with NotImplementedError; (2) improvement or limitation documented in reports/demo/b6_5_2_enhancer_validation.{json,md}; (3) latency recorded — bypass enhance latency is sub-microsecond (mean 11 µs, p95 14 µs) which is the expected near-zero overhead for a no-op, and ASR latency on RP5 is ~1.76 s mean / 1.95 s p95 per row; (4) UI wording can be honest — default_enhancer=bypass with ui_label_for_bypass="honest passthrough" and ui_label_for_metricgan_plus="unavailable". Plan §29 Decision rule 3 applied (enhancer too slow or broken / not yet available → keep bypass default and document limitation). Validation inside the demo container: 9/9 new B6.5.2 unit tests pass; 436/436 targeted regression tests pass (tests/demo + tests/audio + tests/asr + tests/common = 427 B6.5.1 baseline + 9 new). Protected diff empty (libs/audio/enhancement.py, libs/common/versions.py, libs/audio/degradations.py, libs/audio/metrics.py, libs/asr/, libs/demo/, services/, config/, reports/demo/b6_5_1_rp5_results.json, reports/demo/b6_5_1_surrey_results.json, reports/demo/b6_5_1_comparison.json, reports/demo/b6_5_1_comparison.md, reports/demo/demo_examples_asr_drop.json, reports/demo/demo_examples_asr_drop.md, pyproject.toml, requirements.lock.arm64, requirements.lock.x86_64, infra/compose/Dockerfile.demo, infra/compose/docker-compose.demo.yml all untouched). No audio files in git status (only the 4 new files: scripts/demo/run_enhancer_validation.py, tests/demo/test_b6_5_2_enhancer.py, reports/demo/b6_5_2_enhancer_validation.json, reports/demo/b6_5_2_enhancer_validation.md). No new dependency added; pyproject.toml and lockfiles unchanged; speechbrain not introduced (real MetricGAN+ remains owned by T4.1 in feature/training-datamove1-v1 per CLAUDE.md §10.4). Validation commands used the `docker compose run --rm` ephemeral pattern with `${REPO}/scripts:/app/scripts:ro`, `${REPO}/tests:/app/tests:ro` for the test runs and `${REPO}/reports:/out/reports` for the validation run. Advance to B8.1 (Implement cache scripts) per docs/plans/demo_platform_plan.md §30.

- B6.5.1: done on 2026-05-02. §29 RP5 vs Surrey ASR cross-validation closed with verdict `comparable`; no docs/model_card.md update required. Inputs: reports/demo/b6_5_1_rp5_results.json (faster-whisper tiny.en on RP5, host a6488873933a, 30 rows, generated_at 2026-05-03T00:58:12Z) and reports/demo/b6_5_1_surrey_results.json (openai-whisper tiny.en on Surrey/datamove1, host aisurrey03.surrey.ac.uk, 30 rows, generated_at 2026-05-03T01:29:54Z). Surrey JSON delivered manually via Windows bridge from datamove1 (datamove1 cannot reach asr-rp5 over SSH and the Anaconda 3.9 env on datamove1 is intentionally not used for openai-whisper); openai-whisper executed inside the Surrey Apptainer image (torch + openai-whisper provisioned). Surrey JSON validated against the runner contract: engine=openai-whisper, model=tiny.en, degradation_version=degradation_v1, metrics_version=1.0, len(results)==30 with 30 unique (example_id, degradation_id) pairs. Generated reports/demo/b6_5_1_comparison.json and reports/demo/b6_5_1_comparison.md inside the demo-api container via scripts/demo/compare_rp5_surrey.py. Successful completion of the comparator implies audio_sha256 matched on every (example_id, degradation_id) pair across the two reports — the script raises CompareError on any mismatch. Aggregates: degraded_mean_delta_wa=-0.0024 over the 25 degraded rows (RP5 minus Surrey), degraded_mean_abs_delta_wa=0.0024, degraded_max_abs_delta_wa=0.0588 driven by a single (ex007, muffled) pair where openai-whisper scored 0.8235 vs faster-whisper 0.7647; clean_mean_delta_wa=0.0000 across all 5 clean rows; per-degradation mean_delta_wa: broadband_hiss/cafe_background/far_field_room/phone_call all 0.0000, muffled -0.0118; verdict thresholds COMPARABLE_THRESHOLD=0.02 and DISCREPANT_THRESHOLD=0.05 (in [0,1] WA units); abs(degraded_mean_delta_wa)=0.0024 << 0.02 → verdict comparable; requires_model_card_update=false. Latency: total RP5 53.24 s vs total Surrey 37.66 s on the 30 rows (informational; not part of the §29 verdict). Validation inside the demo container: 19/19 B6.5.1 unit tests pass (tests/demo/test_b6_5_1_runner.py + tests/demo/test_b6_5_1_compare.py); 427/427 targeted regression tests pass (tests/demo + tests/audio + tests/asr + tests/common). Protected diff empty (libs/common/versions.py, libs/audio/degradations.py, libs/audio/metrics.py, libs/audio/enhancement.py, libs/demo/cache.py, libs/asr/whisper_provider.py, libs/asr/demo_assemblyai_provider.py, services/api/app/demo_main.py, services/worker/app/demo_worker_main.py, config/demo_examples.json, config/demo_example_candidates.json, reports/demo/demo_examples_asr_drop.json, reports/demo/demo_examples_asr_drop.md, scripts/demo/materialize_demo_examples.py, scripts/demo/select_demo_candidates.py, pyproject.toml, requirements.lock.arm64, requirements.lock.x86_64, infra/compose/Dockerfile.demo, infra/compose/docker-compose.demo.yml all untouched). No audio files in git status. Decision branch taken: comparable → no model card update; B6.5.1 marked done; advance to B6.5.2 (Validate enhancer behavior on RP5).

- B6.5.1 (Milestone A, blocked): on 2026-05-02. RP5-side artifacts for the §29 RP5 vs Surrey ASR cross-validation pushed; task remains blocked until Surrey/datamove1 returns the openai-whisper reference report. Created: scripts/demo/run_reference_whisper.py (CLI runner with --engine faster-whisper / --engine openai-whisper; openai-whisper is imported lazily so the module loads cleanly on RP5 without it; reads config/demo_examples.json + config/demo_example_candidates.json; processes 5 public examples × 6 audio variants = 30 rows; records hypothesis, WER, word_accuracy, latency_seconds, audio_sha256, example_id, source_recording_id, degradation_id; stamps engine, model, host, generated_at, examples_config, candidates_config, artifacts_root, degradation_version, metrics_version, default_enhancer_version, degradation_parameters_signature, scope, variant_ids; --resume reuses already-present rows; uses libs.audio.metrics.compute_metrics; reads versions dynamically from libs.common.versions). Created: scripts/demo/compare_rp5_surrey.py (verifies matching degradation_version, metrics_version, (example_id, degradation_id) row set, and audio_sha256 per row; computes signed delta_wa and abs_delta_wa per pair; emits primary aggregates over the 25 degraded rows only — degraded_mean_delta_wa, degraded_mean_abs_delta_wa, degraded_max_abs_delta_wa, degraded_per_degradation_mean_delta_wa; auxiliary aggregates over clean rows and all rows; thresholds COMPARABLE_THRESHOLD=0.02 and DISCREPANT_THRESHOLD=0.05 in [0,1] units; verdict comparable / caution / discrepant from abs(degraded_mean_delta_wa); aux warnings if clean rows disagree above the comparable threshold or if degraded_mean_abs_delta_wa exceeds discrepant threshold while signed mean stays comparable; renders Markdown with verdict, per-degradation table, and per-pair detail; CompareError on mismatch returns exit code 2). Created: tests/demo/test_b6_5_1_runner.py (8 tests covering 30-row contract, frozen-version stamps, audio_sha256 + non-negative latency on every row, --resume skip behavior, openai-whisper module not imported when faster-whisper engine is selected, openai-whisper code path uses the whisper module, _resolve_variant_path for clean and degraded variants, _degradation_parameters_signature stability) and tests/demo/test_b6_5_1_compare.py (11 tests covering version mismatches, row-set mismatches, audio_sha256 mismatches, all three verdicts, clean-disagreement aux warning, threshold constants, Markdown render, and main() exit codes); both test modules load the scripts via importlib.util.spec_from_file_location so they do not require /app/scripts to be on sys.path inside the demo container. Generated: reports/demo/b6_5_1_rp5_results.json with 30 rows from a live faster-whisper tiny.en run inside the demo-worker container against /home/gbibbo/asr_enhancement_runtime/artifacts/examples; degradation_version=degradation_v1, metrics_version=1.0, default_enhancer_version=1.0; word_accuracy values match the B6.2 audit (reports/demo/demo_examples_asr_drop.json) within 0.001 on all 30 overlapping rows. Validation inside the demo container: 19/19 new B6.5.1 unit tests pass; 427/427 targeted regression tests pass (tests/demo + tests/audio + tests/asr + tests/common); full suite 676 passed (B7.1 baseline 657 + 19 new) / 10 failed / 11 errors with all failures and errors being the pre-existing platform/observability/db/storage/smoke tests requiring Postgres+MinIO+Grafana, unchanged from B7.1; build_cache_key sanity returned ex001|phone_call|degradation_v1|whisper|tiny.en|1.0|1.0; protected diff empty (libs/common/versions.py, libs/audio/degradations.py, libs/audio/metrics.py, libs/audio/enhancement.py, libs/demo/cache.py, libs/asr/whisper_provider.py, libs/asr/demo_assemblyai_provider.py, services/api/app/demo_main.py, services/worker/app/demo_worker_main.py, config/demo_examples.json, config/demo_example_candidates.json, reports/demo/demo_examples_asr_drop.json, reports/demo/demo_examples_asr_drop.md, scripts/demo/materialize_demo_examples.py, scripts/demo/select_demo_candidates.py, pyproject.toml, requirements.lock.arm64, requirements.lock.x86_64, infra/compose/Dockerfile.demo, infra/compose/docker-compose.demo.yml all untouched); no audio files in git status. Blocker: awaiting Surrey/datamove1 openai-whisper reference run producing reports/demo/b6_5_1_surrey_results.json. The other window must (1) check out scripts/demo/run_reference_whisper.py + scripts/demo/compare_rp5_surrey.py + the two config files from origin/feature/demo-runtime-rp5-v1, (2) source the same audio (preferably rsync from asr-rp5:/home/gbibbo/asr_enhancement_runtime/artifacts/examples for ex001/ex003/ex004/ex007/ex010), (3) run scripts/demo/run_reference_whisper.py --engine openai-whisper --model tiny.en, and (4) return reports/demo/b6_5_1_surrey_results.json. After the Surrey JSON arrives, Milestone B will run scripts/demo/compare_rp5_surrey.py on the demo-api container to produce reports/demo/b6_5_1_comparison.{json,md}, apply the §29 verdict, edit docs/model_card.md only if the verdict is discrepant, and only then mark B6.5.1 done.

- B7.1: done on 2026-05-02. Identifier-only freeze of the demo degradation contract. Modified: libs/common/versions.py (DEGRADATION_VERSION: str = "1.0" → "degradation_v1"). Modified: tests/common/test_versions_imports.py (added test_degradation_version_is_frozen_at_v1 pinning the frozen value). METRICS_VERSION and DEFAULT_ENHANCER_VERSION unchanged (both "1.0"). Scope: libs/audio/degradations.py untouched (parameters, registry, dispatch, DegradationResult shape all frozen); libs/audio/metrics.py, libs/audio/enhancement.py, libs/demo/cache.py untouched (build_cache_key already reads versions.DEGRADATION_VERSION at call time, so cache-key behavior changes only through the constant); services/, config/demo_examples.json, config/demo_example_candidates.json, reports/demo/demo_examples_asr_drop.json, reports/demo/demo_examples_asr_drop.md untouched (B6.2 5-example public set and ASR drop audit unchanged; the audit's scope_note remains a faithful record of the pre-freeze identifier under which it ran); pyproject.toml and both lockfiles untouched; no audio regenerated; no ASR drop rerun. Validation: docker image rebuilt to pick up the source change; smoke import inside ephemeral demo-api container returned `degradation_v1 1.0 1.0`; 75/75 targeted tests pass (tests/common/, tests/audio/test_audio_degradations.py, tests/audio/test_audio_degradations_imports.py, tests/audio/test_audio_metrics_imports.py, tests/demo/test_cache_key.py) including the new pin test and the dynamic-read tests that monkeypatch DEGRADATION_VERSION; full suite 657 passed (B6.2 baseline 656 + new pin test) / 10 failed / 11 errors with all failures and errors being pre-existing platform/observability/db/storage/smoke tests requiring Postgres+MinIO+Grafana not present in the demo container, unchanged from B6.2 baseline; build_cache_key sanity returned `ex001|phone_call|degradation_v1|whisper|tiny.en|1.0|1.0`; protected diff empty (libs/audio/degradations.py, libs/audio/metrics.py, libs/audio/enhancement.py, libs/demo/cache.py, services/, config/demo_examples.json, config/demo_example_candidates.json, reports/demo/demo_examples_asr_drop.json, reports/demo/demo_examples_asr_drop.md, pyproject.toml, requirements.lock.arm64, requirements.lock.x86_64 all untouched). Phase B7 gate cleared: training branch is unblocked for dry-run and full training. Next task is B6.5.1 (Run public examples on RP5 and Surrey reference) per docs/plans/demo_platform_plan.md §29; plan order places Phase B6.5 after Phase B7 and before Phase B8, and B6.5 declares B7 complete as a dependency.

- B6.2: done on 2026-05-02. Implementation: scripts/demo/materialize_demo_examples.py (downloads dev-clean.tar.gz selectively, verifies per-FLAC SHA256 from candidates JSON, converts FLAC → 16 kHz mono PCM_16 WAV via soundfile, applies five registered degradations via libs.audio.degradations.apply_degradation with seed=0, writes config/demo_examples.json; --asr-only mode runs WhisperAdapter tiny.en on every clean and degraded WAV and computes word_accuracy via libs.audio.metrics.compute_metrics; --source-dir uses standard LibriSpeech layout first then recursive search for {source_recording_id}.flac; --allow-download downloads dev-clean.tar.gz from OpenSLR to DEMO_RUNTIME_ROOT/cache then extracts only the 10 needed members and deletes the tarball; --asr-only and main mode use separate /app/config:ro and /out/config writable mounts so runtime services keep config read-only). Modified: infra/compose/docker-compose.demo.yml (added ../../config:/app/config:ro to x-demo-volumes so /demo/examples can read config/demo_examples.json from inside the container without rebuilding). Modified: tests/demo/test_demo_candidates.py (removed obsolete test_demo_examples_json_does_not_exist gate). Created: tests/demo/test_demo_examples_config.py (12 tests for the final 5-example config; preserves existing tests/demo/test_demo_examples.py). Generated: config/demo_examples.json with 5 entries (ex001, ex003, ex004, ex007, ex010). Generated reports: reports/demo/demo_examples_asr_drop.json and .md. Scope amendment: B6.1 candidate pool was 10 datamove1 reserved examples; ASR drop verification with faster-whisper tiny.en under DEGRADATION_VERSION 1.0 produced overall_pass on 5 of the 10 candidates and no measurable drop on the other 5 (ex002 clean WA=0.818 with possessive apostrophe transcribed wrong, all degradations produce equal or higher WA; ex005/ex006/ex008/ex009 clean WA=1.000 and all degradations produce 1.000 because sentences are too clear or too short to cause tiny.en errors at current degradation parameters). Decision: keep degradation parameters unchanged, do not bump DEGRADATION_VERSION, do not replace examples; final B6.2 public demo set is the 5 passing examples. Excluded examples ex002, ex005, ex006, ex008, ex009 remain reserved in candidates JSON, remain excluded from training, but are not exposed via /demo/examples. Original example IDs preserved (no renumbering) for traceability back to candidates JSON and ASR drop report. Validation: 12/12 new config tests pass; full suite inside demo container 651 passed / 0 new failures (pre-existing platform failures in observability+db+storage+smoke unrelated to B6.2 still present); GET /demo/examples returned 5 entries; GET /demo/examples/ex001/audio/clean → HTTP 200; GET /demo/examples/ex001/audio/degraded/phone_call → HTTP 200; reports/demo/demo_examples_asr_drop.json overall_pass=true with results section listing the 5 passing examples and excluded_examples section listing the 5 with reason and best observed drop; protected diff empty (services/api/, services/worker/, libs/demo/persistence.py, libs/demo/examples.py, libs/common/demo_settings.py, libs/asr/, libs/audio/, libs/common/versions.py, pyproject.toml, both lockfiles, infra/compose/Dockerfile.demo all untouched); no audio files committed.

- B6.1: done on 2026-05-02. Revised implementation: initial attempt scanned a local LibriSpeech test-clean root (blocked because that path was absent on RP5); revised implementation imports the 10 reserved examples from the datamove1 branch instead. No LibriSpeech download required. Source of truth: origin/feature/training-datamove1-v1:configs/training/reserved_public_demo_examples.yaml (commit 73808ed). Note: stale current_task B5.4 was corrected — no B5.4 exists in docs/plans/demo_platform_plan.md; phase B5 ended at B5.3. Revised: scripts/demo/select_demo_candidates.py (replaced LibriSpeech+soundfile scanner with git-show importer; reads YAML via subprocess, parses with yaml.safe_load, validates 10 entries, maps transcript→ground_truth, source_split=dev-clean, source_dataset=librispeech, excluded_from_training=True, speaker_gender="unknown" for all entries — gender metadata not tracked in datamove1; assigns example_id ex001–ex010; includes audio_sha256 from datamove1; records datamove1 ref, commit SHA, selection policy, and gender limitation in selection_notes; all CLI args have defaults). Revised: tests/demo/test_demo_candidates.py (20 tests: updated duration range [3.0,10.0], exactly 10 distinct speakers, source_split=dev-clean, added test_all_recording_ids_match_datamove1_reserved_set with exact ordered list, added test_all_speaker_genders_unknown, added test_audio_sha256_present, added test_demo_examples_json_does_not_exist, added audio_sha256 to required provenance fields). Generated: config/demo_example_candidates.json (10 entries, all public_content_review=passed_manual_review after Gabriel's explicit approval). Scope: no worker/API/ASR/audio changes; pyproject.toml and both lock files unchanged; Dockerfile.demo and docker-compose.demo.yml unchanged; config/demo_examples.json not created (audio sourcing is B6.2). Validation: 20/20 candidate tests pass including gate test; 654 pass / 0 new failures (pre-existing platform/infra errors unchanged); GET /demo/examples → {"examples":[],"total":0,"note":"No curated examples loaded. Run Phase B6 to populate."} (correct — demo_examples.json not yet created); protected diff empty.

- B5.3: implemented enhancer interface, bypass, and MetricGAN+ hook on 2026-05-02. Modified: libs/audio/enhancement.py (added `import abc`; added `BYPASS_ENHANCER_VERSION = "bypass"` and `METRICGAN_PLUS_ENHANCER_VERSION = "metricgan_plus_pretrained"` constants; added `EnhancerAdapter(abc.ABC)` abstract base class with `enhancer_version: str` abstract property and `enhance(audio_path, output_dir, job_id) -> EnhancementResult` abstract method; added `BypassEnhancer(EnhancerAdapter)` — honest no-op, delegates to `apply_preset(BYPASS_PRESET_ID, audio_path, output_dir)`, enhancer_version="bypass"; added `MetricGANPlusEnhancer(EnhancerAdapter)` — empty hook, enhance() raises NotImplementedError with T4.1 ownership message, enhancer_version="metricgan_plus_pretrained"; PRESET_REGISTRY unchanged — still 4 entries; DEFAULT_ENHANCER_VERSION in libs/common/versions.py unchanged; cache-key/default-version alignment deferred to worker-integration task). Modified: libs/audio/__init__.py (added EnhancerAdapter, BypassEnhancer, MetricGANPlusEnhancer, BYPASS_ENHANCER_VERSION, METRICGAN_PLUS_ENHANCER_VERSION to imports and __all__). Created: tests/audio/test_audio_enhancer_interface.py (16 pure unit tests: EnhancerAdapter instantiation raises TypeError, BypassEnhancer/MetricGANPlusEnhancer issubclass checks, enhancer_version constants and property values, BypassEnhancer.enhance() returns EnhancementResult with enhanced=False/fallback=False/output_path=input_path, MetricGANPlusEnhancer.enhance() raises NotImplementedError, both share callable enhance method, package-level exports from libs.audio validated). Scope: demo-worker unchanged (B3.2 placeholder); no API route changes; no new PyPI deps; pyproject.toml and both lock files unchanged; libs/common/versions.py unchanged; all ASR providers unchanged. Validation: 16/16 new tests pass; 164/164 audio tests pass including test_registry_has_exactly_four_entries; 625 total tests pass / 0 failed (pre-existing platform/infra tests in observability+db+storage+smoke are unrelated to B5.3 and require platform infrastructure not present in demo container); protected diff empty (demo_worker_main.py, demo_main.py, main.py, cache.py, persistence.py, versions.py, libs/asr/, pyproject.toml, both lock files, Dockerfile.demo, docker-compose.demo.yml all untouched); docker compose config exit 0. All B5.3 done-when criteria satisfied.

- B5.2: adapted AssemblyAI provider for demo mode on 2026-05-02. New files: libs/asr/demo_assemblyai_provider.py (DemoAssemblyAIAdapter subclasses AssemblyAIAdapter; shorter demo defaults: max_wait_seconds=120.0, upload_timeout_seconds=30.0; quota_exhausted: bool=False constructor arg — raises AdapterTranscriptionError(_QUOTA_MESSAGE) before calling super() when True; transcribe() overrides with retry-once policy: catches AdapterHTTPError with status_code>=500 or AdapterTimeoutError, sleeps 2s via self._sleep, retries once — on second failure raises AdapterTranscriptionError(_SAFE_ERROR_MESSAGE); HTTP 4xx re-raises without retry; InputFileNotFoundError re-raises without retry; plain AdapterTranscriptionError from provider errors / poll budget propagates unchanged; get_status(api_key) staticmethod returns STATUS_DISABLED for None/"", STATUS_AVAILABLE otherwise), tests/asr/test_demo_assemblyai_provider.py (14 mock-based tests: subclass checks, status disabled/available, success no retry, 5xx retry, timeout retry, retry sleep=2s, 4xx no retry, missing file no retry, double 5xx safe message, quota guard blocks super(), no silent fallback). Modified: libs/asr/errors.py (added AdapterHTTPError(AdapterTranscriptionError) with status_code: int attribute; added AdapterTimeoutError(AdapterTranscriptionError)), libs/asr/assemblyai_provider.py (added AdapterHTTPError and AdapterTimeoutError to imports; in _upload/_submit/_poll: added except httpx.TimeoutException → AdapterTimeoutError before except httpx.HTTPStatusError → AdapterHTTPError with status_code; message strings preserved unchanged; all 31 original tests pass), libs/asr/__init__.py (added DemoAssemblyAIAdapter import and __all__ entry; added AdapterHTTPError, AdapterTimeoutError exports), tests/asr/test_assemblyai_provider.py (added 3 new typed-error tests at end: upload 4xx → AdapterHTTPError.status_code==401, submit 5xx → AdapterHTTPError.status_code==503, upload timeout → AdapterTimeoutError; all 31 original tests unchanged and passing), tests/asr/test_asr_package_imports.py (added DemoAssemblyAIAdapter, AdapterHTTPError, AdapterTimeoutError imports; extended test_error_hierarchy with subclass assertions; added test_demo_assemblyai_adapter_is_asr_adapter). Scope: demo worker unchanged (B3.2 placeholder); no API route changes; no usage_ledger or cost tracking; no live AssemblyAI calls; no new dependencies; pyproject.toml and both lock files unchanged; .env.demo.example unchanged. Validation: 14/14 new demo adapter tests pass; 34/34 assemblyai tests pass (31 original + 3 new); 360 passed / 0 failed in tests/asr+demo+audio+common; protected diff empty (main.py, tasks.py, demo_main.py, demo_worker_main.py, pyproject.toml, both lock files untouched); docker compose config exit 0. All B5.2 done-when criteria satisfied.

- B5.1: implemented Whisper local ASR provider on 2026-05-02. New files: libs/asr/whisper_provider.py (WhisperAdapter implementing ASRAdapter; MODEL_NAME = "tiny.en"; conditional import try/except with _FASTER_WHISPER_AVAILABLE flag so module imports cleanly without faster-whisper; _model injection point for mocked tests; is_file() guard rejects missing paths and directories; lazy model load on first transcribe() via _get_model(); model cache controlled via model_cache_dir param or WHISPER_MODEL_CACHE env var using faster-whisper download_root kwarg; language_warning added to raw_payload when info.language != "en" and language_probability > 0.5 using exact plan-specified message; normalizes to ASRResult with provider="whisper", provider_job_id=None, words=[]), tests/asr/test_whisper_provider.py (14 mock-based tests: subclass check, MODEL_NAME constant, missing file, directory input, English transcription, schema shape, segment normalization, empty segments, raw_payload model key, non-English warning above threshold, non-English below threshold, English above threshold, unavailability guard, live import check with skipif). Modified: tests/asr/test_asr_package_imports.py (added WhisperAdapter import and test_whisper_provider_is_asr_adapter; reordered imports alphabetically), pyproject.toml (added [project.optional-dependencies] demo = ["faster-whisper>=1.0"]), requirements.lock.arm64 (regenerated with --extra=dev --extra=demo; adds faster-whisper, ctranslate2, onnxruntime, huggingface-hub, tokenizers, tqdm, av and transitive deps; no changes to requirements.lock.x86_64), .env.demo.example (added WHISPER_MODEL_CACHE= placeholder). demo-worker unchanged (B3.2 placeholder still fails all jobs with "processing not yet implemented (B5)"). Validation: 14/14 new tests pass; full suite tests/demo+tests/asr all pass (no regressions); git diff requirements.lock.x86_64 empty; docker compose config exit 0; docker compose build exit 0 (faster-whisper visible in build output); GET /demo/health → {"status":"ok","mode":"demo","db_ok":true,"queue_depth":0}; smoke transcription inside demo container (1-second 440 Hz sine WAV, model downloaded to /home/gbibbo/asr_enhancement_runtime/cache/whisper) → smoke PASSED, text='', language='en', duration_seconds=1.0, segments=0; Dockerfile.demo unchanged (no missing shared library error); protected diff empty (main.py, tasks.py, db.py, models.py, docker-compose.yml, demo_main.py, demo_worker_main.py all untouched). All B5.1 done-when criteria satisfied.

- B4.3: implemented job submission endpoints on 2026-05-02. New files: libs/demo/upload.py (DEMO_ALLOWED_EXTENSIONS frozenset {.wav,.mp3,.m4a,.flac}; UnsupportedExtensionError; FileTooLargeError; async save_upload() with temp-then-rename pattern: writes to uuid.hex+".tmp", renames to uuid.hex+ext on success, unlinks .tmp on any exception; upload_dir.mkdir(parents=True, exist_ok=True) defensive; 65 536 B chunk streaming with byte limit enforcement), tests/demo/test_demo_upload_helper.py (9 unit tests using asyncio.run() + MagicMock UploadFile: wav/mp3 accepted, .txt rejected, no-extension rejected, size limit enforced, original filename absent from saved path, no .tmp after success, no partial file after FileTooLargeError), tests/demo/test_demo_jobs_api.py (18 API integration tests across 3 fixtures: GET /demo/jobs/{id} 404/200, GET /demo/jobs/{id}/result 404/202/200-completed/200-failed, POST /demo/run-cached 404-unknown-example/404-unknown-degradation/200-cache-miss/200-cache-miss-no-job-created/200-cache-hit, POST /demo/upload 202-queued/202-input-artifact-path/413/415/503-no-orphan, GET /admin/stats 401-no-credentials/401-no-password). Modified: libs/demo/persistence.py (extended try_create_job with input_artifact_path: Optional[str] = None and extended INSERT; added get_cache_entry, get_jobs_stats, set_admin_state, get_admin_state_value), services/api/app/demo_main.py (lifespan records startup_time via set_admin_state; added _RunCachedRequest Pydantic model; _ASR_MODEL_DEFAULTS dict; _basic = HTTPBasic(auto_error=False); 5 new routes: GET /demo/examples/{id}/audio/clean already existed from B4.2; POST /demo/run-cached, POST /demo/upload with file cleanup on QueueFullError/unexpected error, GET /demo/jobs/{id}, GET /demo/jobs/{id}/result, GET /admin/stats with uniform 401 for all auth failures), tests/demo/test_persistence.py (6 new tests: input_artifact_path stored, get_cache_entry none/dict, get_jobs_stats all zeros, set+get admin_state, admin_state missing returns None). Validation: 117 tests passed / 0 failed (84 pre-existing + 33 new); both Compose configs exit 0; build exit 0 (no new pip packages); GET /demo/health → {"status":"ok","mode":"demo","db_ok":true,"queue_depth":0}; POST /demo/run-cached with no examples config → 404; GET /admin/stats without auth → 401 {"detail":"Unauthorized"} with WWW-Authenticate: Basic; POST /demo/upload minimal WAV → 202 {"job_id":...,"status":"queued"}; GET /demo/jobs/{id} → job row with input_artifact_path set; GET /demo/jobs/{id}/result → {"status":"failed","error":"processing not yet implemented (B5)"} (worker B3.2 placeholder as expected); platform isolation diff empty (main.py, tasks.py, db.py, models.py, docker-compose.yml, pyproject.toml, both lock files untouched); docker compose down clean. All B4.3 done-when criteria satisfied.

- B4.2: implemented demo audio endpoints on 2026-05-02. Modified: libs/demo/examples.py (added get_safe_audio_path(audio_root, path_str) helper — rejects None, empty string, absolute paths, and traversal attempts via Path.is_relative_to(); returns resolved Path if file exists, None otherwise), services/api/app/demo_main.py (added GET /demo/examples/{example_id}/audio/clean and GET /demo/examples/{example_id}/audio/degraded/{degradation_id}; both use FileResponse with media_type="audio/wav"; audio root derived as settings.demo_artifacts_dir / "examples"; 404 for unknown example, missing clean_audio_path, unknown degradation_id, absolute path in config, traversal attempt, missing file). Created: tests/demo/test_demo_audio.py (21 new tests: 7 unit tests for get_safe_audio_path covering None, empty, absolute, traversal, missing, valid, subdir; 7 API tests for clean audio endpoint; 7 API tests for degraded audio endpoint). Validation: 84 tests passed / 0 failed (63 previous + 21 new); demo Compose config exit 0; platform Compose config exit 0; build exit 0 (no new pip packages, requirements.lock.arm64 unchanged); GET /demo/health → {"status":"ok","mode":"demo","db_ok":true,"queue_depth":0}; GET /demo/examples → explicit empty response; GET /demo/examples/nonexistent/audio/clean → 404; protected diff empty (platform files untouched); docker compose down clean. All B4.2 done-when criteria satisfied.

- B4.1: implemented demo health and example listing on 2026-05-02. New files: libs/demo/examples.py (DemoExample Pydantic model with example_id, title, description, duration_seconds, degradation_ids, ground_truth, audio_available, clean_audio_path, degraded_audio_paths; load_examples() returns [] when config absent), tests/demo/test_demo_examples.py (8 new tests covering absent file, empty array, valid JSON, all default field values, platform isolation check). Modified: libs/common/demo_settings.py (added demo_examples_config: Path = Path("config/demo_examples.json")), services/api/app/demo_main.py (GET /demo/health expanded to report db_ok and queue_depth by calling count_active_jobs inside try/except; GET /demo/examples new route reading settings.demo_examples_config via load_examples()), tests/demo/test_demo_api.py (added DEMO_EXAMPLES_CONFIG to env isolation list, updated test_health_returns_ok to check key fields, added test_health_db_ok_is_true, test_health_queue_depth_is_integer, test_examples_returns_200, test_examples_returns_empty_list_when_no_config, test_examples_note_is_set_when_empty, test_examples_note_is_null_when_populated), tests/demo/test_demo_settings.py (added DEMO_EXAMPLES_CONFIG to env isolation list, added test_default_examples_config). Corrected stale tracker entry: B3.3 does not exist in the plan; phase B3 ended at B3.2; tracker corrected to current_task B4.2. Validation: 63 tests passed / 0 failed (48 previous + 15 new); demo Compose config exit 0; platform Compose config exit 0; build exit 0 (no new pip packages, requirements.lock.arm64 unchanged); GET /demo/health → {"status":"ok","mode":"demo","db_ok":true,"queue_depth":0}; GET /demo/examples → {"examples":[],"total":0,"note":"No curated examples loaded. Run Phase B6 to populate."}; protected diff empty (platform files untouched); docker compose down clean. All B4.1 done-when criteria satisfied.

- B3.1: created demo Compose runtime on 2026-05-02. New files: infra/compose/Dockerfile.demo (python:3.11-slim, installs from requirements.lock.arm64), infra/compose/docker-compose.demo.yml (Compose project name asr-demo, two services: demo-api on host port 8001, demo-worker; no platform services; Python stdlib urllib.request healthcheck; bind-mount /home/gbibbo/asr_enhancement_runtime), .env.demo.example (placeholder only, no secrets), libs/common/demo_settings.py (DemoSettings with model_validator filling derived paths from DEMO_RUNTIME_ROOT), services/api/app/demo_main.py (minimal FastAPI, no platform imports, GET /demo/health stub), services/worker/app/demo_worker_main.py (SIGTERM+SIGINT handlers, clean shutdown). Added .env.demo to .gitignore. Validation: docker compose config exit 0; docker compose build exit 0 (arm64 native, all packages from requirements.lock.arm64); docker compose up -d: demo-api healthy, demo-worker Up; curl http://localhost:8001/demo/health → {"status":"ok","mode":"demo"}; worker log: "Demo worker started (idle, waiting for B3.2 job queue)"; platform config: exit 0; tests/demo/ inside demo image: 19 passed / 0 failed (8 cache key + 11 new demo settings); docker compose down: clean exit. Seven protected diffs all empty. All B3.1 done-when criteria satisfied.



- B2.5: generated requirements.lock.arm64 on RP5 on 2026-05-02. Docker container: python:3.11-slim linux/arm64 (native aarch64, no emulation). pip-tools: 7.5.3. Generation command: `docker run --rm --platform linux/arm64 -v "$(pwd)":/workspace -w /workspace python:3.11-slim sh -c "pip install --quiet pip-tools==7.5.3 && pip-compile --extra=dev --generate-hashes --output-file=requirements.lock.arm64 --strip-extras pyproject.toml"`. Container architecture check: platform.machine()=aarch64, Python 3.11.15. Sanity checks: no file:// paths, no secrets, fastapi==0.136.1 / celery==5.6.3 / pytest==9.0.3 pinned with hashes (1364 hash lines). Install from lock in bare arm64 container: install OK. Pure-Python test suites: tests/audio/ + tests/common/ + tests/demo/ + tests/asr/ = 218 passed / 0 failed in 3.05 s. Protected diff checks: requirements.lock.x86_64, pyproject.toml, libs/, services/, tests/, infra/, .github/ all empty (no unintended changes). All B2.5 done-when criteria satisfied.
- B2.2: Git and VS Code Remote SSH verified on 2026-05-02. SSH to asr-rp5: ok. Repo ~/code/asr_enhancement fetched and fast-forwarded from 99495ad to f3400cf (19 commits). Branch: feature/demo-runtime-rp5-v1. Remote: git@github.com:gbibbo/asr_enhancement.git. GitHub SSH auth: ok (Hi gbibbo! authenticated via id_ed25519_github_rp5). Working tree: clean. Git identity: Gabriel Bibbó <gabobibbo@gmail.com>. merge.ours.driver: set to true. VS Code Remote SSH: Gabriel confirmed repo opens in VS Code remote window with branch feature/demo-runtime-rp5-v1 and tip f3400cf. All B2.2 done-when criteria satisfied.
- B2.1: base system dependencies verified on 2026-05-02. apt-get update: exit 0, fetched 707 kB in 3 s from deb.debian.org (trixie, trixie-updates, trixie-security) and archive.raspberrypi.com (trixie). Git: 2.47.3. Docker: 26.1.5+dfsg1 build a72d7cd. Docker Compose: 2.26.1-4. gbibbo already in docker group (gid 105) — no usermod required. docker ps without sudo: exit 0. docker run --rm alpine echo docker-ok: pulled alpine:latest from Docker Hub, printed docker-ok. All B2.1 done-when criteria satisfied.
- B1.1: OS headless verified on 2026-05-02. RP5 was already installed and running before B0.x checks. SSH confirmed as gbibbo@asr-rp5. OS: Debian GNU/Linux 13 (trixie) with Raspberry Pi kernel 6.12.75+rpt-rpi-2712 (BCM2712 / RP5). Architecture: aarch64. Hostname: asr-rp5. User: gbibbo. SSH service: active. Headless confirmed (lightdm inactive, DISPLAY not set). Timezone: America/Montevideo. Locale: en_GB.UTF-8. Disk: mmcblk0p2 ext4 rootfs 50.2 GB, 9% used, mounted at /. eth0 UP 192.168.1.241/24 is runtime path. wlan0 UP 192.168.1.240/24 is active but not runtime path per B1.1 decision rule 3 (keep Ethernet as runtime path). All B1.1 done-when criteria satisfied.
- S0.1: inspected repository state and preserved MVP tag.
- S0.2: created integration and demo feature branches.
- S0.3: added split plan files and root plan router.
- S0.4: created demo/platform trackers.
- S0.5: confirmed all four split branches on origin (master, demo-rp5-v1, feature/demo-runtime-rp5-v1, feature/training-datamove1-v1) and verified RP5 host, Docker 26.1.5, and Compose 2.26.1 on asr-rp5.
- R0.1: verified platform mode locally on 2026-05-01 from feature/demo-runtime-rp5-v1 (tip 7f0d98a). Built and brought up infra/compose/docker-compose.yml; all 8 services running, postgres/redis/minio healthy, api and worker started cleanly. Pytest inside the api container: general suite (tests excluding tests/smoke and tests/integration) 377 passed / 0 failed / 0 skipped; smoke suite 1 passed. Host-side checks: GET /health 200, GET /metrics 200, POST /v1/transcribe round-trip returned the deterministic fake transcript. Classification P (all green). Stack torn down with docker compose down (volumes preserved).
- R0.2: added migration status note to README.md ## Status section on 2026-05-01. Note references platform-mvp-v0 tag (verified present) and demo-rp5-v1 branch (verified present). No live demo URL added.
- R1.1: created libs/asr/ package (7 files: __init__.py, schema.py, errors.py, base.py, fake_provider.py, assemblyai_provider.py, factory.py) and tests/asr/test_asr_package_imports.py on 2026-05-01. Import smoke inside api container: OK. tests/asr/ 5 passed. tests/asr_adapter/ 51 passed (old package untouched). Full non-smoke/non-integration suite 382 passed / 0 failed. Safety diffs confirmed libs/asr_adapter/, tasks.py, main.py, and pyproject.toml unchanged. Stack torn down with docker compose down (volumes preserved).
- R1.2: updated imports and tests for ASR package on 2026-05-01. services/worker/app/tasks.py line 15 updated from libs.asr_adapter.factory to libs.asr.factory. 5 worker test files updated to libs.asr.errors / libs.asr.schema. tests/integration/test_live_assemblyai.py updated to libs.asr.assemblyai_provider / libs.asr.schema (gate still skips in CI). 3 new behavioral test files created: tests/asr/test_fake_provider.py (17 tests), tests/asr/test_assemblyai_provider.py (31 tests), tests/asr/test_asr_factory.py (3 tests). Safety diffs: libs/asr_adapter/, services/api/, pyproject.toml, infra/, .github/workflows/ all empty (no unintended changes). tests/asr/ 56 passed. tests/asr_adapter/ 51 passed (old package untouched). tests/worker/ 52 passed. Integration gate: 1 skipped / 0 failed. Full non-smoke/non-integration suite 433 passed / 0 failed (382 baseline + 51 new). Smoke suite 1 passed. Endpoint round-trip: /v1/transcribe returned deterministic fake transcript. Final import search: no libs.asr_adapter outside libs/asr_adapter/ and tests/asr_adapter/. Stack torn down with docker compose down (volumes preserved).
- R1.3: removed legacy libs/asr_adapter/ (7 files) and tests/asr_adapter/ (4 files) on 2026-05-01. Pre-deletion and post-deletion host-side Select-String confirmed zero active imports of libs.asr_adapter in services/, tests/, libs/. Removed 3 completed ASR mapping rows from CLAUDE.md §9 and docs/profiles/CLAUDE.demo.md §9; audio pipeline rows (libs/audio_pipeline/) retained for R2.x. pytest inside api container: tests/asr/ 56 passed, tests/worker/ 52 passed, full suite 382 passed / 0 failed (433 − 51 deleted), smoke 1 passed. GET /health: {"status": "ok"}. /v1/transcribe round-trip: deterministic fake transcript confirmed. Stack torn down with docker compose down (volumes preserved).
- B0.2: account and connectivity checklist completed on 2026-05-02. AssemblyAI latency from RP5: 0.707 s / 0.715 s / 0.710 s — classification: ok (all < 5 s, AssemblyAI viable in v1). AssemblyAI account: ok (logged in, API key available). Credits: $49.97 remaining on free plan. Autopay: off. Cloudflare account: ok (logged in, account active). Domain: trycloudflare.com for development (plan default). RP5 → api.assemblyai.com: reachable (HTTP 401 × 3). No blockers. All B-phase tasks may continue.
- B0.1: hardware and LAN checklist completed on 2026-05-02. Windows-to-RP5 LAN reachability confirmed (ping and SSH TCP/22, IPv6 2800:a4:951:4f00:da3a:ddff:fea5:199e, round-trip 7–17 ms). SSH confirmed as gbibbo@asr-rp5. SD card: mmcblk0 59.5 GB; root partition 58 GB (5.4 GB used / 51 GB free, 10%). eth0: UP, IPv4 192.168.1.241/24, IPv6 2800:a4:951:4f00:da3a:ddff:fea5:199e/64. CPU temperature: 40.6°C at idle (vcgencmd confirms). Docker 26.1.5+dfsg1, Docker Compose 2.26.1-4, Git 2.47.3. Internet from RP5: HTTP/2 200 (github.com). Repo ~/code/asr_enhancement exists on RP5. Manual hardware: USB-C power supply 65 W (adequate, >27 W requirement); cooling: official Raspberry Pi 5 Active Cooler (fan + heatsink); Ethernet cable physically connected to router/switch (eth0 primary, Wi-Fi also active); UPS: none currently, planned for future (not required by plan, accepted). No blockers.
- R4.1: created libs/audio/degradations.py on 2026-05-02 with DegradationSpec, DegradationResult, DEGRADATION_REGISTRY, KNOWN_DEGRADATION_IDS, UnknownDegradationError, get_degradation, and apply_degradation. Five deterministic DSP degradations: far_field_room (decaying exponential reverb + gain reduction), cafe_background (band-limited Gaussian noise at SNR 10 dB), phone_call (bandpass Butterworth 300–3400 Hz), muffled (lowpass Butterworth 1000 Hz), broadband_hiss (white noise at SNR 30 dB). All noise functions use np.random.default_rng(seed); DEGRADATION_VERSION read via import libs.common.versions as versions at call time; monkeypatch test confirms no hardcoding. Updated libs/audio/__init__.py to re-export all seven new symbols. Created tests/audio/test_audio_degradations_imports.py (11 tests) and tests/audio/test_audio_degradations.py (40 tests, including parametrized suite over DEGRADATION_IDS ordered list and np.allclose determinism checks). Safety diffs: pyproject.toml, requirements.lock.x86_64, services/api/app/main.py, services/worker/app/tasks.py, libs/demo/cache.py, libs/common/versions.py, infra/, .github/workflows/ all empty. tests/audio/ 148 passed / 0 failed. Full non-smoke/non-integration suite 487 passed / 0 failed (436 baseline + 51 new). Smoke 1 passed. Stack torn down with docker compose down (volumes preserved).
- R3.1: created libs/audio/metrics.py on 2026-05-02 with MetricsResult (frozen dataclass), normalize_text, compute_metrics, and _word_edit_distance. Pure-Python token-level Levenshtein WER — no third-party metric library added, pyproject.toml and requirements.lock.x86_64 unchanged. METRICS_VERSION read via `import libs.common.versions as versions` inside compute_metrics() at call time; monkeypatch test confirms no hardcoding. Updated libs/audio/__init__.py to re-export MetricsResult, compute_metrics, normalize_text. Created tests/audio/test_audio_metrics_imports.py (9 tests including monkeypatch) and tests/audio/test_audio_metrics.py (25 tests covering normalization, WER, Word Accuracy bounds, unavailable reference, frozen dataclass). Safety diffs: pyproject.toml, requirements.lock.x86_64, services/api/app/main.py, services/worker/app/tasks.py, infra/, .github/workflows/ all empty. tests/audio/ 97 passed / 0 failed. Full non-smoke/non-integration suite 436 passed / 0 failed (402 baseline + 34 new). Smoke 1 passed. Stack torn down with docker compose down (volumes preserved).
- R2b.2: attempted arm64 dependency lock on 2026-05-02. Docker buildx on Windows host does not support linux/arm64 — both builders (default, desktop-linux) report linux/amd64 only; `docker run --platform linux/arm64 python:3.11-slim` fails with `exec format error` (QEMU binfmt not configured). Deferral to B2.5 is the plan-defined path. Task B2.5 ("Generate arm64 lock on RP5 if missing") is explicitly required and will execute during Phase B2 RP5 setup. arm64 lock generation command recorded for B2.5: `docker run --rm --platform linux/arm64 -v $(pwd):/workspace -w /workspace python:3.11-slim sh -c "pip install --quiet pip-tools==7.5.3 && pip-compile --extra=dev --generate-hashes --output-file=requirements.lock.arm64 --strip-extras pyproject.toml"`. Install verification command for B2.5: `docker run --rm --platform linux/arm64 -v $(pwd):/workspace -w /workspace python:3.11-slim sh -c "pip install --quiet -r requirements.lock.arm64 && python -c 'import fastapi, celery, pytest; print(\"OK\")'". No requirements.lock.arm64 committed at this stage.
- R2b.1: generated requirements.lock.x86_64 at repo root on 2026-05-02 using pip-compile 7.5.3 inside a linux/amd64 python:3.11-slim container. Lock includes main + dev dependencies with --generate-hashes and --strip-extras. Architecture check confirmed x86_64. Sanity checks: no file:// paths, no secrets, fastapi==0.136.1 / celery==5.6.3 / pytest==9.0.3 pinned with hashes. Install from lock in bare container: OK. Import check (fastapi, celery, pytest, libs.demo.cache, libs.common.versions, libs.audio.enhancement, libs.asr.factory): OK. Pure-Python test suites: tests/audio/ 63 passed, tests/common/ 6 passed, tests/demo/ 8 passed, tests/asr/ 56 passed = 133 passed / 0 failed.
- R2.5.2: created libs/demo/__init__.py, libs/demo/cache.py, tests/demo/__init__.py, tests/demo/test_cache_key.py on 2026-05-01. build_cache_key() uses import libs.common.versions as versions and reads versions.DEGRADATION_VERSION, versions.METRICS_VERSION, and versions.DEFAULT_ENHANCER_VERSION at call time. enhancer_version=None defaults to versions.DEFAULT_ENHANCER_VERSION. Output is pipe-delimited: example_id|degradation_id|DEGRADATION_VERSION|asr_provider|asr_model_version|enhancer_version|METRICS_VERSION. Host import smoke: OK. tests/demo/ 8 passed (including 2 monkeypatch tests that fail if versions are hardcoded). tests/common/ 6 passed (unchanged). Full non-smoke/non-integration suite 402 passed / 0 failed (394 baseline + 8 new). Smoke 1 passed. Stack torn down with docker compose down (volumes preserved).
- R2.5.1: created libs/common/versions.py on 2026-05-01. 3 new constants: DEGRADATION_VERSION="1.0", METRICS_VERSION="1.0", DEFAULT_ENHANCER_VERSION="1.0". Created tests/common/__init__.py and tests/common/test_versions_imports.py (6 tests). Import smoke inside api container: all three constants OK. Duplicate constant search confirmed no definitions outside libs/common/versions.py. tests/common/ 6 passed. Full non-smoke/non-integration suite 394 passed / 0 failed (388 baseline + 6 new). Smoke 1 passed. Stack torn down with docker compose down (volumes preserved).
- R2.3: removed legacy libs/audio_pipeline/ (4 files) and tests/audio_pipeline/ (3 files) on 2026-05-01. Pre-deletion and post-deletion Select-String confirmed zero active imports of libs.audio_pipeline in services/, tests/, libs/. Removed completed audio_pipeline mapping block from CLAUDE.md §9 and docs/profiles/CLAUDE.demo.md §9. pytest inside api container: tests/audio/ 63 passed, tests/worker/ 52 passed, full suite 388 passed / 0 failed (445 − 57 deleted), smoke 1 passed. GET /health: {"status": "ok"}. /v1/transcribe round-trip: deterministic fake transcript confirmed. /v1/enhance-and-transcribe round-trip (preset=bypass): deterministic fake transcript confirmed. /v1/enhance-and-transcribe (preset=bad_preset): 400 unknown_preset confirmed. Stack torn down with docker compose down (volumes preserved).
- R2.2: updated imports and tests for audio package on 2026-05-01. services/api/app/main.py lines 20-21 updated from libs.audio_pipeline.errors/presets to libs.audio.enhancement (combined import). services/worker/app/tasks.py line 16 updated from libs.audio_pipeline.pipeline to libs.audio.enhancement. tests/worker/test_enhance_task.py and test_worker_metrics.py updated to libs.audio.enhancement. 2 new behavioral test files created: tests/audio/test_audio_enhancement.py (35 tests), tests/audio/test_audio_presets.py (22 tests). Safety diff confirmed zero libs.audio_pipeline imports in services/ or tests/worker/. tests/audio/ 63 passed. tests/audio_pipeline/ 57 passed (unchanged). tests/worker/ 52 passed. Full non-smoke/non-integration suite 445 passed / 0 failed (388 baseline + 57 new). Smoke suite 1 passed. Endpoint checks: GET /health 200, POST /v1/transcribe 202, POST /v1/enhance-and-transcribe (preset=bypass) 202, POST /v1/enhance-and-transcribe (preset=bad_preset) 400 unknown_preset. Stack torn down with docker compose down (volumes preserved).
- R2.1: created libs/audio/ package and enhancement module on 2026-05-01. 4 new files: libs/audio/__init__.py, libs/audio/enhancement.py (self-contained: UnknownPresetError + preset registry + EnhancementResult + DSP pipeline, no import from libs.audio_pipeline), tests/audio/__init__.py, tests/audio/test_audio_enhancement_imports.py. Active imports in services/ and tests/worker/ unchanged on libs.audio_pipeline (copy-only phase). Import smoke inside api container: libs.audio.enhancement OK, libs.audio __init__ OK. tests/audio/ 6 passed. tests/audio_pipeline/ 57 passed (unchanged). tests/worker/ 52 passed. Full non-smoke/non-integration suite 388 passed / 0 failed (382 baseline + 6 new). Smoke suite 1 passed. Host-side /v1/transcribe round-trip: deterministic fake transcript confirmed. Host-side /v1/enhance-and-transcribe round-trip (preset=bypass): deterministic fake transcript confirmed. Safety diffs: libs/audio_pipeline, services/api/app/main.py, services/worker/app/tasks.py, pyproject.toml, infra/, .github/workflows all empty (no unintended changes). libs.audio_pipeline import baseline identical before and after (15 matches, same files and lines). Stack torn down with docker compose down (volumes preserved).

- B3.2: implemented demo persistence and queue limit on 2026-05-02. New files: libs/demo/persistence.py (stdlib sqlite3 only; no SQLAlchemy; QueueFullError; ensure_runtime_dirs creates 5 runtime dirs; init_schema runs CREATE TABLE IF NOT EXISTS for all 4 tables with exact plan-contract column names and TIMESTAMP type names; count_active_jobs for diagnostics; try_create_job with BEGIN IMMEDIATE for atomic queue-max enforcement; get_job; update_job_status; claim_next_job with BEGIN IMMEDIATE), tests/demo/test_persistence.py (21 new tests covering schema idempotency, create/get/update/claim, queue-full boundary, runtime dirs, isolation checks), tests/demo/test_demo_api.py (7 new API tests using TestClient with isolated tmp_path DB: health, 202 create, DB persistence, 503 queue full, completed/failed rows do not block, platform-settings isolation). Modified: services/api/app/demo_main.py (lifespan now calls ensure_runtime_dirs + init_schema, stores settings on app.state; added POST /demo/jobs stub with atomic queue check), services/worker/app/demo_worker_main.py (startup calls ensure_runtime_dirs + init_schema; polling loop claims jobs via claim_next_job and immediately marks them failed with error_message="processing not yet implemented (B5)" — deliberate B3.2 lifecycle placeholder, no ASR/enhancer/audio imports). Also fixed libs/common/demo_settings.py: fill_derived_paths now treats Path(".") (from empty-string env vars in Compose) as unset, using _unset() helper; added test_empty_string_db_path_falls_back_to_derived. Fixed infra/compose/docker-compose.demo.yml: changed demo-worker command from python -u script.py to python -m module.path so Python adds /app to sys.path. Validation: tests/demo/ 48 passed / 0 failed (8 cache key + 12 settings + 7 API + 21 persistence); demo-api healthy; demo-worker Up; GET /demo/health → {"status":"ok","mode":"demo"}; POST /demo/jobs → 202 with job_id; worker log shows lifecycle placeholder messages; queue-max=10 enforced: 10×202 then 503 on 11th; SQLite tables: admin_state, cache_entries, jobs, usage_ledger; protected diff empty (platform files untouched); docker compose down clean. All B3.2 done-when criteria satisfied.

## Current blocker

None.

## Training handoff status

Training branch feature/training-datamove1-v1 already exists on origin from demo-rp5-v1. Split workflow active.

- B9.4: implemented cleanup script for uploads and per-job artifact dirs on 2026-05-03. New files: scripts/cleanup_uploads.py (CLI: dry-run by default, --apply required for deletion, --apply --dry-run rejected, --retention-hours overrides default, --now for deterministic tests), libs/demo/cleanup.py (pure helpers: discover_upload_candidates / discover_job_dir_candidates / load_in_flight_input_paths / load_job_status_lookup / delete_upload_safely / delete_job_dir_safely / record_last_cleanup_at / run_cleanup), tests/demo/test_b9_4_cleanup_uploads.py (20 tests). Modified: libs/common/demo_settings.py (added demo_upload_retention_hours: int = 24, env DEMO_UPLOAD_RETENTION_HOURS). Retention default 24 h. Safe allowlist enforced: deletes only files directly under DEMO_RUNTIME_ROOT/uploads matching ^[0-9a-f]{32}\\.(wav|mp3|m4a|flac|tmp)$ and per-job dirs DEMO_RUNTIME_ROOT/artifacts/jobs/<uuid4>; refuses anything under cache, db, logs, tmp, artifacts/examples, config, reports, source tree, or outside DEMO_RUNTIME_ROOT; refuses symlinks. Files under uploads/ with unrecognized basenames are skipped (counted as skipped_unrecognized) and not deleted, not fatal. File-only deletion: no jobs/cache_entries/usage_ledger rows pruned, no job statuses changed, no artifact path columns nulled. The only DB write is upserting admin_state.last_cleanup_at on a successful --apply that reaches the end. Cron is documented in the script docstring (host-level crontab line) and must be installed manually on RP5 — no cron files committed. Compose-first validation: pytest tests/demo/test_b9_4_cleanup_uploads.py inside demo-api with scripts/ bind-mounted → 20 passed; adjacent suites (B9.1+B9.2+B9.3+demo_jobs_api+demo_settings+persistence+B8.1) → 130 passed; broader regression tests/demo+tests/audio+tests/asr+tests/common → 529 passed. Live RP5 dry-run smoke: identified 1 stale upload (Friday's 44-byte file) + 5 recent uploads kept + 3 recent job dirs kept; nothing under examples/cache/db/logs/tmp listed. Live RP5 --apply smoke: 1 upload deleted, all other state preserved; verified jobs row count unchanged (27), cache_entries unchanged (30), usage_ledger unchanged (0), artifacts/examples count unchanged (10), and admin_state grew from 1 → 2 keys with last_cleanup_at added and startup_time unchanged. Known op note: scripts/ must be bind-mounted into the demo container because Dockerfile.demo does not COPY scripts/.

## Next task

Task B11.2.
